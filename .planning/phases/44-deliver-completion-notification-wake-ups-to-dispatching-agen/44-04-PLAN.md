---
phase: 44
plan: 04
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/openclaw/tool-invocation-context.ts
autonomous: true
tags: [phase-44, wake-up, ttl, invocation-context]
requirements: [D-44-TTL]

must_haves:
  truths:
    - "The default OpenClawToolInvocationContextStore no longer evicts routes/tool-calls based on wall-clock time"
    - "Eviction happens via explicit clearSessionRoute calls (fired by session_end hook at adapter.ts:72-73) and the LRU cap"
    - "The explicit-TTL override path still works (test-only seam at lines 37-58 of the test file)"
    - "All existing tests pass; Plan 01 Task 3 RED test turns GREEN"
  artifacts:
    - path: src/openclaw/tool-invocation-context.ts
      provides: "Default constructor produces a store with no time-based TTL; pruneExpired becomes a no-op unless routeTtlMs is explicitly supplied"
      contains: "routeTtlMs"
  key_links:
    - from: "src/openclaw/adapter.ts:72-73"
      to: "src/openclaw/tool-invocation-context.ts::clearSessionRoute"
      via: "session_end event -> clearSessionRoute(sessionKey)"
      pattern: "clearSessionRoute"
---

<objective>
Remove the default 1-hour wall-clock TTL from `OpenClawToolInvocationContextStore` so captured dispatcher routes survive long-running tasks (> 1h between `before_tool_call` and task transition to `done`). Retain the explicit `routeTtlMs` override for tests. Rely on the existing `session_end` hook and LRU cap for memory bounds. Turns Plan 01 Task 3 (the `default-constructor store retains a captured tool-call past 24h of simulated clock time` RED test) GREEN.

Purpose: D-44-TTL — a root-cause fix for the F1 failure mode in 44-RESEARCH.md. The 1h TTL was arbitrary and actively drops wake-ups for any task that takes >1h.
Output: One edited file. Default constructor behavior changes (no more default time-based eviction). Explicit override path unchanged. No new files; no barrel edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-deliver-completion-notification-wake-ups-to-dispatching-agen/44-RESEARCH.md
@.planning/phases/44-deliver-completion-notification-wake-ups-to-dispatching-agen/44-PATTERNS.md
@CLAUDE.md

<interfaces>
Existing TTL constants (src/openclaw/tool-invocation-context.ts lines 24-26):
```ts
const DEFAULT_ROUTE_TTL_MS = 60 * 60 * 1000;
const DEFAULT_MAX_SESSION_ROUTES = 2048;
const DEFAULT_MAX_TOOL_CALLS = 2048;
```

Existing expiry logic (src/openclaw/tool-invocation-context.ts lines 277-286):
```ts
private pruneExpired(): void {
  const now = this.now();
  for (const map of [this.bySessionKey, this.bySessionId, this.byToolCallId]) {
    for (const [key, entry] of map.entries()) {
      if (entry.expiresAt <= now) {
        map.delete(key);
      }
    }
  }
}
```

Existing constructor option (verified in 44-PATTERNS.md):
```ts
constructor(opts?: { routeTtlMs?: number; maxSessionRoutes?: number; maxToolCalls?: number; now?: () => number });
```

Session-end cleanup hook (src/openclaw/adapter.ts:72-73):
```ts
api.on("session_end", (e) => invocationContextStore.clearSessionRoute(e.sessionKey));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Disable default time-based TTL, preserve explicit override</name>
  <files>src/openclaw/tool-invocation-context.ts</files>
  <read_first>
    - src/openclaw/tool-invocation-context.ts (FULL FILE — you must understand every place `routeTtlMs`, `DEFAULT_ROUTE_TTL_MS`, `expiresAt`, and `pruneExpired` are referenced. The file is ~287 LOC.)
    - src/openclaw/__tests__/tool-invocation-context.test.ts (existing test at lines 37-58 — the OVERRIDE path that MUST keep working; plus the Plan 01 Task 3 new RED test "default-constructor store retains a captured tool-call past 24h of simulated clock time")
    - src/openclaw/adapter.ts (lines 60-95 — confirm the session_end + session_start hooks that cleanup routes are still wired)
    - .planning/phases/44-deliver-completion-notification-wake-ups-to-dispatching-agen/44-PATTERNS.md §`src/openclaw/tool-invocation-context.ts`
    - .planning/phases/44-deliver-completion-notification-wake-ups-to-dispatching-agen/44-RESEARCH.md §11 Q2 (recommended option C — no TTL; rely on session_end + LRU)
  </read_first>
  <behavior>
    - Default constructor (no `routeTtlMs` passed) produces a store that NEVER evicts entries based on `this.now()` — the time-based check is skipped.
    - Constructor with explicit `routeTtlMs: 100` still evicts entries after 100ms of `now()` advance (test-only seam).
    - LRU cap (`DEFAULT_MAX_SESSION_ROUTES = 2048`, `DEFAULT_MAX_TOOL_CALLS = 2048`) still enforces memory bounds.
    - `clearSessionRoute(sessionKey)` still drops the matching route immediately (unchanged from today).
    - Plan 01 Task 3 RED test (`default-constructor store retains a captured tool-call past 24h of simulated clock time`) GREEN.
    - Existing test at lines 37-58 of the test file (`expires stale routes and tool calls after the configured TTL`) STILL PASSES (uses explicit override).
  </behavior>
  <action>
    Read the full file first, then make this surgical change.

    Recommended approach: make `DEFAULT_ROUTE_TTL_MS` an "infinite" sentinel and update `pruneExpired` to short-circuit when the TTL is infinite.

    Concrete edits:

    1. Change the constant at line 24:
       ```ts
       // BEFORE:
       const DEFAULT_ROUTE_TTL_MS = 60 * 60 * 1000;
       // AFTER:
       const DEFAULT_ROUTE_TTL_MS = Number.POSITIVE_INFINITY;
       ```

    2. Find the constructor block that assigns `this.routeTtlMs`. It likely looks like:
       ```ts
       this.routeTtlMs = opts?.routeTtlMs ?? DEFAULT_ROUTE_TTL_MS;
       ```
       This line needs NO change — `Number.POSITIVE_INFINITY` propagates naturally.

    3. Find every `expiresAt = now + this.routeTtlMs` assignment (captureMessageRoute, captureToolCall — search for `expiresAt`). With `routeTtlMs === Infinity`, `now + Infinity === Infinity`, which is already >= any future `now` value. pruneExpired's comparison `entry.expiresAt <= now` becomes `Infinity <= now` → false, so no eviction. This means `pruneExpired` becomes a no-op organically without code changes.

    4. BUT: if any code path computes `new Date(expiresAt)` or serializes `expiresAt` to JSON, `Infinity` is not JSON-safe (`JSON.stringify(Infinity) === "null"`). Grep the file for `expiresAt` uses — if any serialization exists (e.g., a telemetry log or a debug dump), add an `isFinite(entry.expiresAt)` guard before the comparison.

    5. Alternatively (if the `Infinity` approach conflicts with any existing usage), convert `pruneExpired` to an explicit no-op when `this.routeTtlMs === DEFAULT_ROUTE_TTL_MS` AND `DEFAULT_ROUTE_TTL_MS` is a special "unlimited" marker (e.g., `0` or a named constant `NO_TTL`). If you take this path, make the marker a `readonly` class field rather than a module constant so tests can target it.

    6. Update the JSDoc / module comment at the top of the file to note the Phase 44 change: "Default eviction is LRU-cap + clearSessionRoute (fired by session_end hook at adapter.ts:72-73). The wall-clock TTL is disabled by default; pass `routeTtlMs` explicitly to enable (test-only)."

    7. Do NOT remove `pruneExpired`; it remains callable when an explicit TTL is set.

    8. Do NOT change `DEFAULT_MAX_SESSION_ROUTES` or `DEFAULT_MAX_TOOL_CALLS`.

    Run `npx vitest run src/openclaw/__tests__/tool-invocation-context.test.ts`. The Plan 01 Task 3 RED test ("default-constructor store retains a captured tool-call past 24h of simulated clock time") MUST turn GREEN. The existing "expires stale routes and tool calls after the configured TTL" test MUST still pass (it constructs the store with `routeTtlMs: 100` — the override path).

    Run `npm test` to confirm no regressions in any other file that consumes this store.
  </action>
  <verify>
    <automated>npx vitest run src/openclaw/__tests__/tool-invocation-context.test.ts --reporter=verbose 2>&1 | tee /tmp/44-04-task1.log; echo "--- full suite ---"; npm test 2>&1 | tail -30 | tee /tmp/44-04-task1-full.log</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/openclaw/__tests__/tool-invocation-context.test.ts` exits 0 (all tests pass).
    - The Plan 01 Task 3 new test (`default-constructor store retains a captured tool-call past 24h of simulated clock time` or whatever name was used — grep the test file for `25 * 60 * 60 * 1000`) PASSES. Verify via reporter output.
    - The pre-existing test at lines 37-58 (`expires stale routes and tool calls after the configured TTL`) STILL PASSES. Verify by name.
    - `grep -q 'Number.POSITIVE_INFINITY\|readonly.*routeTtlMs.*NO_TTL' src/openclaw/tool-invocation-context.ts` succeeds (proves the TTL-disable approach landed).
    - `grep -q 'DEFAULT_MAX_SESSION_ROUTES = 2048' src/openclaw/tool-invocation-context.ts` succeeds (LRU cap unchanged).
    - `npm run typecheck` exits 0.
    - `npm test` (full unit suite) exits 0.
  </acceptance_criteria>
  <done>
    Default TTL removed. Override path preserved. Plan 01 Task 3 RED turns GREEN. All other unit tests remain green. Typecheck clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin in-memory | Store lives entirely inside the OpenClaw gateway process. No new surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-05 | Denial of Service | Unbounded store growth if `session_end` hook misfires | accept | Existing LRU cap (`DEFAULT_MAX_SESSION_ROUTES = 2048`, `DEFAULT_MAX_TOOL_CALLS = 2048`) bounds memory. Same-uid trust boundary (Phase 43 D-08) means the attacker would need local process access, which is already game-over. 44-RESEARCH.md §Security Domain acknowledges this as the existing bound. |
</threat_model>

<verification>
- `npm run typecheck && npm test` exits 0.
- The one file modified has a single semantic change: default TTL disabled.
- Existing adapter.ts session_end wiring untouched.
- No new external imports added.
</verification>

<success_criteria>
- Plan 01 Task 3 RED -> GREEN.
- Memory bounds preserved (LRU cap intact; session_end hook cleanup intact).
- No regressions in any unit test.
- `git diff --stat` shows exactly one file (`src/openclaw/tool-invocation-context.ts`) changed.
</success_criteria>

<output>
After completion, create `.planning/phases/44-deliver-completion-notification-wake-ups-to-dispatching-agen/44-04-SUMMARY.md` documenting the exact sentinel used (Infinity vs. class-field NO_TTL), any `expiresAt` serialization guards added, and confirmation that adapter.ts session_end wiring was reviewed unchanged.
</output>
