---
phase: 32-agent-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/aof/SKILL.md
  - skills/aof/SKILL-SEED.md
  - src/context/__tests__/context-budget-gate.test.ts
autonomous: true
requirements: [GUID-01]

must_haves:
  truths:
    - "SKILL.md documents the subscribe parameter on aof_dispatch with correct enum values"
    - "SKILL.md documents aof_task_subscribe and aof_task_unsubscribe tools in the Agent Tools table"
    - "SKILL.md explains idempotency expectations for callback handlers (at-least-once delivery)"
    - "SKILL.md documents callback depth limit (3) and timeout (2 minutes)"
    - "Budget gate CI test passes after SKILL.md update"
  artifacts:
    - path: "skills/aof/SKILL.md"
      provides: "Full-tier agent guidance with subscription/callback documentation"
      contains: "Subscriptions & Callbacks"
    - path: "skills/aof/SKILL-SEED.md"
      provides: "Seed-tier agent guidance with subscription tool rows"
      contains: "aof_task_subscribe"
    - path: "src/context/__tests__/context-budget-gate.test.ts"
      provides: "Updated budget gate with raised ceiling for v1.8 content"
      contains: "BUDGET_CEILING_TOKENS"
  key_links:
    - from: "skills/aof/SKILL.md"
      to: "src/context/__tests__/context-budget-gate.test.ts"
      via: "token count measurement"
      pattern: "BUDGET_CEILING_TOKENS"
---

<objective>
Update SKILL.md and SKILL-SEED.md to document subscription tools, callback behavior, and idempotency expectations so agents understand how to use v1.8 Task Notifications. Adjust budget gate test to accommodate the new content.

Purpose: Agents need standing context about subscription/callback tools and handler contracts to use the notification system correctly.
Output: Updated SKILL.md with Subscriptions & Callbacks section, updated SKILL-SEED.md with subscribe tool rows, passing budget gate test.
</objective>

<execution_context>
@/Users/xavier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-agent-guidance/32-CONTEXT.md
@.planning/phases/32-agent-guidance/32-RESEARCH.md

@skills/aof/SKILL.md
@skills/aof/SKILL-SEED.md
@src/context/__tests__/context-budget-gate.test.ts

<interfaces>
<!-- Budget gate test constants and assertions the executor needs to understand -->

From src/context/__tests__/context-budget-gate.test.ts:
```typescript
const BUDGET_CEILING_TOKENS = 2150;  // Needs bump to ~2500
const PRE_V14_SKILL_BASELINE_TOKENS = 3411;

// Tests:
// 1. "full-tier context stays under budget ceiling" — totalTokens <= BUDGET_CEILING_TOKENS
// 2. "gate catches regressions" — 4x SKILL.md tokens > ceiling (keeps ceiling meaningful)
// 3. "achieves 50%+ reduction from pre-v1.4 baseline" — skillTokens < 3411 * 0.5 (NEEDS RELAXATION)
// 4. "SKILL.md contains completion protocol" — content checks (unchanged)
```

From src/context/budget.ts:
```typescript
export function estimateTokens(text: string): number;  // ~4 chars per token heuristic
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md and SKILL-SEED.md with subscription documentation</name>
  <files>skills/aof/SKILL.md, skills/aof/SKILL-SEED.md</files>
  <action>
SKILL.md changes (the working tree already has 6 new tool rows — keep those):

1. **Update aof_dispatch row** in Agent Tools table to mention subscribe param:
   `| aof_dispatch | Create task and assign to agent/team. Accepts workflow (see DAG section) and subscribe params. | { taskId, status, assignedAgent, filePath, sessionId, subscriptionId } |`

2. **Add 2 new tool rows** to Agent Tools table (after aof_task_dep_remove, before aof_project_create):
   `| aof_task_subscribe | Subscribe to task outcome notifications | { subscriptionId, taskId, granularity, status } |`
   `| aof_task_unsubscribe | Cancel a task outcome subscription | { subscriptionId, status } |`

3. **Add new section** after `## Completion Protocol` (at end of file). Target ~22 lines:

```
## Subscriptions & Callbacks

Subscribe to task notifications. Callbacks spawn a new session to the subscriber agent with task results.

- **Subscribe at dispatch:** `subscribe: "completion"` or `subscribe: "all"` param on `aof_dispatch`
- **Subscribe later:** `aof_task_subscribe` tool (subscriberId must be a valid org chart agent ID)
- **Unsubscribe:** `aof_task_unsubscribe` with the subscriptionId

### Granularity

| Granularity | Fires | Use for |
|-------------|-------|---------|
| `completion` | Once on terminal state (done/cancelled/deadletter) | React-on-done workflows |
| `all` | Every status change, batched per poll cycle | Progress monitoring |

`all` is a superset of `completion` -- no need for both on the same task.

### Callback Handler Contract

- You receive a session with task results as context. Process it and exit.
- Delivery is at-least-once. You may receive the same callback more than once. Design handlers to be idempotent.
- Callback chains are depth-limited to 3.
- Callback sessions have a 2-minute timeout -- keep handlers lightweight.
```

IMPORTANT: Use `subscribe: "completion"` and `subscribe: "all"` — NOT `subscribe: true`. The actual Zod schema uses `z.enum(["completion", "all"])`, not boolean.

SKILL-SEED.md changes:
1. Add the same 2 subscribe/unsubscribe tool rows to the Agent Tools table (after dep_remove, before project_create)
2. Update aof_dispatch row to mention subscribe param (same wording as SKILL.md)
3. Do NOT add the full Subscriptions & Callbacks section to SKILL-SEED.md — it is the minimal seed tier. The tool rows are sufficient; agents needing callback details will load full tier.
  </action>
  <verify>
    <automated>grep -c "aof_task_subscribe\|aof_task_unsubscribe\|Subscriptions & Callbacks\|subscribe.*param\|at-least-once\|depth-limited to 3" skills/aof/SKILL.md | xargs test 6 -eq</automated>
  </verify>
  <done>SKILL.md has subscribe/unsubscribe in tool table, updated aof_dispatch row, and ~22-line Subscriptions & Callbacks section with granularity table and callback handler contract. SKILL-SEED.md has subscribe/unsubscribe tool rows and updated aof_dispatch row. No batched payload details documented. Idempotency warning present.</done>
</task>

<task type="auto">
  <name>Task 2: Adjust budget gate test to accommodate v1.8 content growth</name>
  <files>src/context/__tests__/context-budget-gate.test.ts</files>
  <action>
Update the budget gate test in `src/context/__tests__/context-budget-gate.test.ts`:

1. **Raise BUDGET_CEILING_TOKENS** from 2150 to 2500. This provides ~10% headroom over the estimated ~2268 total after v1.8 additions. Update the comment to reflect v1.8 growth:
   ```typescript
   /**
    * Hard ceiling for full-tier context (SKILL.md + tool descriptions).
    * Bumped from 2150 to 2500 for v1.8 subscription/callback documentation.
    * If this test fails, someone inflated the context — investigate before bumping.
    */
   const BUDGET_CEILING_TOKENS = 2500;
   ```

2. **Relax the 50% reduction test to 40% reduction.** The 50% reduction was a one-time v1.4 improvement metric. v1.8 legitimately adds new features. Change:
   - From: `expect(skillTokens).toBeLessThan(PRE_V14_SKILL_BASELINE_TOKENS * 0.5);`
   - To: `expect(skillTokens).toBeLessThan(PRE_V14_SKILL_BASELINE_TOKENS * 0.6);`
   - Update the test name from `"achieves 50%+ reduction from pre-v1.4 baseline"` to `"achieves 40%+ reduction from pre-v1.4 baseline"`

3. **Leave unchanged:** The "gate catches regressions" test (4x inflation check auto-adjusts) and the "completion protocol content" test.

After editing, run the test suite to confirm all pass:
```bash
npx vitest run context-budget-gate
```
  </action>
  <verify>
    <automated>cd /Users/xavier/Projects/aof && npx vitest run context-budget-gate 2>&1 | tail -5</automated>
  </verify>
  <done>Budget gate test passes with BUDGET_CEILING_TOKENS=2500 and 40% reduction threshold. All 4 test assertions green. No regression in other tests.</done>
</task>

</tasks>

<verification>
1. `npx vitest run context-budget-gate` — all 4 tests pass
2. `grep "Subscriptions & Callbacks" skills/aof/SKILL.md` — section exists
3. `grep "aof_task_subscribe" skills/aof/SKILL.md skills/aof/SKILL-SEED.md` — tool rows in both files
4. `grep "at-least-once" skills/aof/SKILL.md` — idempotency warning present
5. `grep "depth-limited to 3" skills/aof/SKILL.md` — depth limit documented
6. `wc -l skills/aof/SKILL.md` — should be ~230 lines (206 + ~24 new lines)
</verification>

<success_criteria>
- SKILL.md documents subscribe param on aof_dispatch with correct enum values ("completion", "all")
- SKILL.md has aof_task_subscribe and aof_task_unsubscribe in tool table
- SKILL.md has Subscriptions & Callbacks section (~22 lines) after Completion Protocol
- SKILL.md explains at-least-once delivery and idempotency expectation
- SKILL.md documents depth limit (3) and timeout (2 minutes)
- SKILL-SEED.md has subscribe/unsubscribe tool rows
- Budget gate CI test passes (all 4 assertions)
</success_criteria>

<output>
After completion, create `.planning/phases/32-agent-guidance/32-01-SUMMARY.md`
</output>
