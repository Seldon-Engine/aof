---
phase: 14-templates-ad-hoc-api-and-artifacts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dispatch/dag-context-builder.ts
  - src/dispatch/dag-transition-handler.ts
  - src/dispatch/dag-context-builder.test.ts
  - src/dispatch/dag-transition-handler.test.ts
autonomous: true
requirements: [ARTF-01, ARTF-02]

must_haves:
  truths:
    - "Each dispatched hop gets a per-hop artifact directory created before agent spawn"
    - "Downstream hops receive artifact paths for all completed predecessor hops in their context"
    - "Artifact directories follow the convention tasks/<id>/work/<hop-id>/"
  artifacts:
    - path: "src/dispatch/dag-transition-handler.ts"
      provides: "mkdir -p for hop artifact directory before spawn"
      contains: "mkdir"
    - path: "src/dispatch/dag-context-builder.ts"
      provides: "artifactPaths field in HopContext"
      contains: "artifactPaths"
  key_links:
    - from: "src/dispatch/dag-transition-handler.ts"
      to: "filesystem"
      via: "mkdir(hopWorkDir, { recursive: true }) before spawnSession"
      pattern: "mkdir.*work.*hopId"
    - from: "src/dispatch/dag-context-builder.ts"
      to: "HopContext"
      via: "artifactPaths record mapping predecessor IDs to directory paths"
      pattern: "artifactPaths"
---

<objective>
Add per-hop artifact directories and artifact path injection into hop context.

Purpose: Enable structured artifact handoff between hops -- each hop writes to its own subdirectory (ARTF-01) and downstream hops discover upstream artifacts via paths in their dispatch context (ARTF-02).
Output: Artifact directories auto-created on dispatch, HopContext extended with artifactPaths.
</objective>

<execution_context>
@/Users/xavier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/dispatch/dag-context-builder.ts
@src/dispatch/dag-transition-handler.ts

<interfaces>
<!-- Key types the executor needs -->

From src/dispatch/dag-context-builder.ts:
```typescript
export interface HopContext {
  hopId: string;
  description?: string;
  role: string;
  upstreamResults: Record<string, Record<string, unknown>>;
  autoAdvance: boolean;
}

export function buildHopContext(task: Task, hopId: string): HopContext;
```

From src/dispatch/dag-transition-handler.ts:
```typescript
export async function dispatchDAGHop(
  task: Task,
  hopId: string,
  taskContext: TaskContext,
  adapter: GatewayAdapter,
  store: ITaskStore,
  eventLogger: EventLogger,
): Promise<void>;
// dispatchDAGHop calls buildHopContext internally, then spawnSession
```

From src/schemas/task.ts (Task type):
```typescript
// task.path is set when loaded from store (absolute filesystem path)
// task.frontmatter.workflow contains definition + state
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend HopContext with artifactPaths and update buildHopContext</name>
  <files>src/dispatch/dag-context-builder.ts, src/dispatch/dag-context-builder.test.ts</files>
  <behavior>
    - Test: HopContext includes artifactPaths as Record of string to string
    - Test: buildHopContext returns artifactPaths with entries for completed predecessor hops only
    - Test: buildHopContext returns empty artifactPaths when hop has no predecessors
    - Test: buildHopContext returns empty artifactPaths for predecessor hops that are not complete (pending, dispatched, skipped, failed)
    - Test: Artifact paths follow convention: join(dirname(task.path!), 'work', predId)
    - Test: buildHopContext throws if task.path is not set (guard)
  </behavior>
  <action>
    In src/dispatch/dag-context-builder.ts:
    - Add `artifactPaths: Record<string, string>` to the HopContext interface with JSDoc: "Absolute paths to completed predecessor hop artifact directories, keyed by hop ID."
    - Import `dirname` and `join` from `node:path`
    - In `buildHopContext()`, after building upstreamResults, build `artifactPaths`:
      - Assert `task.path` is defined: `if (!task.path) throw new Error('task.path required for artifact path resolution')`
      - For each predecessor hop ID in `hop.dependsOn`, check if `state.hops[predId]?.status === 'complete'`
      - If complete, set `artifactPaths[predId] = join(dirname(task.path), 'work', predId)`
    - Include `artifactPaths` in the returned HopContext object

    Update existing tests to account for the new field. Add new tests for the behaviors above. Test fixtures must set `task.path` to a valid absolute path.
  </action>
  <verify>
    <automated>npx vitest run src/dispatch/dag-context-builder.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>buildHopContext returns artifactPaths mapping completed predecessor hop IDs to their work directory paths. Guard throws if task.path is missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create hop artifact directory before agent dispatch</name>
  <files>src/dispatch/dag-transition-handler.ts, src/dispatch/dag-transition-handler.test.ts</files>
  <behavior>
    - Test: dispatchDAGHop creates directory at work/<hopId>/ before spawning session
    - Test: Directory uses recursive creation (mkdir -p equivalent)
    - Test: Directory path is derived from task.path: join(dirname(task.path!), 'work', hopId)
  </behavior>
  <action>
    In src/dispatch/dag-transition-handler.ts:
    - Import `mkdir` from `node:fs/promises` and `dirname`, `join` from `node:path` (if not already imported)
    - In `dispatchDAGHop()`, BEFORE the `buildHopContext()` call and spawnSession:
      - Compute `const hopWorkDir = join(dirname(task.path!), 'work', hopId)`
      - Call `await mkdir(hopWorkDir, { recursive: true })`
    - Add a log line: `console.log(\`[dag] Created artifact dir: \${hopWorkDir}\`)`

    Update existing dispatchDAGHop tests to verify mkdir is called before spawn. Use vi.mock or check filesystem if tests use real dirs.
  </action>
  <verify>
    <automated>npx vitest run src/dispatch/dag-transition-handler.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>dispatchDAGHop creates per-hop artifact directory (work/<hopId>/) before agent spawn using recursive mkdir.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/dispatch/` -- all dispatch tests pass
- `npx tsc --noEmit` -- no type errors
</verification>

<success_criteria>
- HopContext interface includes artifactPaths field
- buildHopContext populates artifactPaths for completed predecessors only
- dispatchDAGHop creates work/<hopId>/ directory before spawning agent
- Guard on task.path prevents undefined paths in artifact resolution
- All existing dispatch tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-templates-ad-hoc-api-and-artifacts/14-02-SUMMARY.md`
</output>
