---
phase: 23-tiered-context-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/aof/SKILL-SEED.md
  - skills/aof/skill.json
  - src/context/skills.ts
  - src/context/resolvers.ts
  - src/context/__tests__/skills.test.ts
  - src/context/__tests__/resolvers.test.ts
autonomous: true
requirements: [SKILL-07]

must_haves:
  truths:
    - "A seed skill file exists with ~55 lines covering tool table, AOF/1 protocol, completion outcomes, and workflow upgrade hint"
    - "skill.json declares both seed and full tiers with entrypoints and token estimates"
    - "SkillManifest interface accepts an optional tiers field"
    - "SkillResolver.resolve() accepts an optional tier parameter and returns the tier-appropriate content"
    - "When a requested tier does not exist in skill.json, SkillResolver falls back to the main entrypoint"
  artifacts:
    - path: "skills/aof/SKILL-SEED.md"
      provides: "Minimal agent skill for simple tasks"
      min_lines: 40
    - path: "skills/aof/skill.json"
      provides: "Skill manifest with tiers field"
      contains: "tiers"
    - path: "src/context/skills.ts"
      provides: "SkillManifest with optional tiers"
      contains: "tiers"
    - path: "src/context/resolvers.ts"
      provides: "SkillResolver with tier parameter"
      contains: "tier"
  key_links:
    - from: "src/context/resolvers.ts"
      to: "src/context/skills.ts"
      via: "loadSkillManifest reads tiers field"
      pattern: "manifest\\.tiers"
    - from: "src/context/resolvers.ts"
      to: "skills/aof/skill.json"
      via: "tiers.seed.entrypoint selects SKILL-SEED.md"
      pattern: "tiers.*entrypoint"
---

<objective>
Create the seed skill file and extend the skill manifest and resolver to support tiered context delivery.

Purpose: Agents working on simple tasks will receive ~500 tokens instead of ~1665 tokens of skill context, reducing context usage by ~70% for the common case.
Output: SKILL-SEED.md, updated skill.json with tiers, extended SkillManifest interface, tier-aware SkillResolver, and tests.
</objective>

<execution_context>
@/Users/xavier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-tiered-context-delivery/23-CONTEXT.md

@skills/aof/SKILL.md
@skills/aof/skill.json
@src/context/skills.ts
@src/context/resolvers.ts
@src/context/__tests__/skills.test.ts
@src/context/__tests__/resolvers.test.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/context/skills.ts:
```typescript
export interface SkillManifest {
  version: 'v1';
  name: string;
  description: string;
  tags: string[];
  entrypoint: string;
  references?: string[];
  estimatedTokens?: number;
}

export async function loadSkillManifest(skillPath: string): Promise<SkillManifest>;
export async function listSkills(skillsDir: string): Promise<SkillManifest[]>;
```

From src/context/resolvers.ts:
```typescript
export class SkillResolver implements ContextResolver {
  readonly type = "skill";
  constructor(skillsDir: string);
  canResolve(ref: string): boolean;
  resolve(ref: string): Promise<string>;
}
```

From src/context/budget.ts:
```typescript
export function estimateTokens(text: string): number;  // Math.ceil(text.length / 4)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SKILL-SEED.md and update skill.json with tiers</name>
  <files>skills/aof/SKILL-SEED.md, skills/aof/skill.json</files>
  <behavior>
    - SKILL-SEED.md exists and is under 60 lines
    - SKILL-SEED.md contains a tool table listing all 8 tools (aof_dispatch, aof_task_update, aof_task_complete, aof_status_report, aof_board, aof_project_create, aof_project_list, aof_project_add_participant)
    - SKILL-SEED.md contains AOF/1 envelope format
    - SKILL-SEED.md contains completion outcomes (done, blocked, needs_review, partial)
    - SKILL-SEED.md contains a workflow upgrade hint mentioning contextTier: 'full' and aof_context_load
    - SKILL-SEED.md does NOT contain DAG workflow examples, org chart guidance, or projects section
    - skill.json has a `tiers` field with seed and full entries, each having entrypoint and estimatedTokens
    - skill.json tiers.seed.entrypoint is "SKILL-SEED.md"
    - skill.json tiers.full.entrypoint is "SKILL.md"
    - skill.json tiers.seed.estimatedTokens is approximately ceil(SKILL-SEED.md chars / 4)
    - skill.json tiers.full.estimatedTokens matches existing estimatedTokens (1665)
  </behavior>
  <action>
    Create `skills/aof/SKILL-SEED.md` with this structure (~55 lines, ~500 tokens target):
    - YAML frontmatter: name, description (brief), version: 3.0.0
    - Title: "# AOF -- Agentic Ops Fabric" with one-liner description
    - Agent Tools table: same 8-row table as SKILL.md (Tool | Purpose | Returns)
    - "No parameter tables here -- tool JSON schemas provide full parameter docs at call time."
    - Inter-Agent Protocols section: AOF/1 envelope format line, protocol types table (5 types), completion outcomes table (4 outcomes)
    - Workflow hint section: short note: "For DAG workflow composition, org chart setup, or project management, dispatch with `contextTier: 'full'` or call `aof_context_load` mid-session."
    - Do NOT include: DAG Workflows section, Workflow Patterns, Org Chart section, Projects section

    Update `skills/aof/skill.json`:
    - Keep all existing fields (version, name, description, tags, entrypoint, estimatedTokens)
    - Add `tiers` field:
      ```json
      "tiers": {
        "seed": {
          "entrypoint": "SKILL-SEED.md",
          "estimatedTokens": <computed via ceil(chars/4)>
        },
        "full": {
          "entrypoint": "SKILL.md",
          "estimatedTokens": 1665
        }
      }
      ```
    - Compute seed estimatedTokens after writing SKILL-SEED.md using ceil(charCount / 4)
  </action>
  <verify>
    <automated>wc -l skills/aof/SKILL-SEED.md && node -e "const fs=require('fs'); const s=fs.readFileSync('skills/aof/SKILL-SEED.md','utf8'); const j=JSON.parse(fs.readFileSync('skills/aof/skill.json','utf8')); console.log('seed_lines:', s.split('\\n').length); console.log('has_tiers:', !!j.tiers); console.log('seed_entry:', j.tiers?.seed?.entrypoint); console.log('full_entry:', j.tiers?.full?.entrypoint); console.log('has_8_tools:', (s.match(/aof_/g)||[]).length >= 8)"</automated>
  </verify>
  <done>SKILL-SEED.md exists at ~55 lines with tool table, AOF/1 protocol, completion outcomes, and workflow upgrade hint. skill.json has tiers field with seed and full entries with accurate token estimates. Seed content does NOT include DAG examples, org chart, or projects section.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend SkillManifest with tiers and add tier parameter to SkillResolver</name>
  <files>src/context/skills.ts, src/context/resolvers.ts, src/context/__tests__/skills.test.ts, src/context/__tests__/resolvers.test.ts</files>
  <behavior>
    - SkillManifest has optional `tiers` field typed as Record of tier name to { entrypoint: string, estimatedTokens?: number }
    - loadSkillManifest validates tiers field if present (each entry must have string entrypoint)
    - loadSkillManifest does NOT reject manifests without tiers (backward compatible)
    - SkillResolver.resolve() accepts optional second parameter `tier?: string`
    - resolve("skill:aof", "seed") reads tiers.seed.entrypoint from manifest and returns that file's content
    - resolve("skill:aof", "full") reads tiers.full.entrypoint from manifest
    - resolve("skill:aof") with no tier reads the main entrypoint (existing behavior, backward compatible)
    - resolve("skill:aof", "nonexistent") falls back to main entrypoint gracefully (no error)
    - resolve("skill:old-skill-no-tiers", "seed") falls back to main entrypoint since old skill has no tiers field
  </behavior>
  <action>
    1. In `src/context/skills.ts`:
       - Add optional `tiers` field to `SkillManifest` interface:
         ```typescript
         tiers?: Record<string, { entrypoint: string; estimatedTokens?: number }>;
         ```
       - In `loadSkillManifest`, add validation for `tiers` if present:
         - Must be an object
         - Each value must have a string `entrypoint`
         - Each value's `estimatedTokens` must be a number if present
         - Do NOT error if `tiers` is absent (backward compat)

    2. In `src/context/resolvers.ts`:
       - Update `SkillResolver.resolve()` signature to accept optional tier:
         ```typescript
         async resolve(ref: string, tier?: string): Promise<string>
         ```
       - Update the `ContextResolver` interface's `resolve` method to accept optional second arg (or just update SkillResolver since it already implements the interface and TypeScript allows extra optional params)
       - Implementation:
         - Load manifest as before
         - If `tier` is provided AND manifest has `tiers` AND `tiers[tier]` exists: read `tiers[tier].entrypoint` instead of `manifest.entrypoint`
         - Otherwise: read `manifest.entrypoint` (existing behavior)
       - This ensures graceful fallback: unknown tier or no tiers field = main entrypoint

    3. In `src/context/__tests__/skills.test.ts`:
       - Add test: "loads manifest with tiers field"
       - Add test: "validates tiers entries have string entrypoint"
       - Add test: "accepts manifest without tiers (backward compat)"

    4. In `src/context/__tests__/resolvers.test.ts`:
       - Add test in SkillResolver section: "resolves seed tier from tiers field"
       - Add test: "resolves full tier from tiers field"
       - Add test: "falls back to main entrypoint when tier not found in tiers"
       - Add test: "falls back to main entrypoint when manifest has no tiers"
       - Add test: "resolves without tier parameter (backward compat)"
  </action>
  <verify>
    <automated>cd /Users/xavier/Projects/aof && npx vitest run src/context/__tests__/skills.test.ts src/context/__tests__/resolvers.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>SkillManifest has optional tiers field. SkillResolver.resolve() accepts optional tier parameter. Fallback to main entrypoint when tier is missing or unknown. All existing tests pass, new tier tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/context/__tests__/skills.test.ts src/context/__tests__/resolvers.test.ts` -- all pass including new tier tests
- `skills/aof/SKILL-SEED.md` exists, is under 60 lines, contains 8 tool names and AOF/1 protocol
- `skills/aof/skill.json` contains `tiers` with seed and full entries
- `src/context/skills.ts` exports SkillManifest with optional `tiers` field
- `src/context/resolvers.ts` SkillResolver.resolve() accepts optional tier parameter
</verification>

<success_criteria>
- SKILL-SEED.md is a self-contained minimal skill (~55 lines, ~500 tokens) that enables agents to complete simple tasks (dispatch, update, complete) without full DAG/org-chart context
- skill.json tiers field enables programmatic tier selection
- SkillResolver supports tier-aware resolution with graceful fallback
- All existing tests continue to pass (backward compat)
- New tests verify tier selection and fallback behavior
</success_criteria>

<output>
After completion, create `.planning/phases/23-tiered-context-delivery/23-01-SUMMARY.md`
</output>
