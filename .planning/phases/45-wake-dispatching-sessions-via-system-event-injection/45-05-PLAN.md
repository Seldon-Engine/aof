---
phase: 45
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - .planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-UAT.md
autonomous: false
tags: [phase-45, wake-up, system-event, uat, human-verify, checkpoint]
requirements:
  - D-45-GOAL
  - D-45-FALLBACK-WARNING
  - D-45-FEATURE-DETECT

must_haves:
  truths:
    - "After deploy + dual launchctl kickstart, the live `main` agent in the user's Telegram dispatcher topic reacts in chat to a dispatched task completion WITHOUT human prompting (D-45-GOAL — the unmockable acceptance test)"
    - "Daemon log contains wake-up.system-event-enqueued AND wake-up.heartbeat-requested AND wake-up.delivered with channel='both' for the dispatched task"
    - "Chat audit message is one line, contains `agent: main` (NOT `agent: unknown`), uses ✓ for done / ⚠ for failure-class"
    - "Phase 44 UAT regression: the chat audit message itself still appears (Phase 44 behavior preserved)"
    - "Capability-absent fallback (manual via monkey-patch): chat message contains the inline FALLBACK_WARNING_LINE; daemon log contains wake-up.system-event-unavailable; no exceptions thrown"
  artifacts:
    - path: .planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-UAT.md
      provides: "Human-verify matrix with deploy/restart/zombie-cleanup checklist + the live Telegram probe + capability-absent monkey-patch probe + log-grep assertions + pass/fail criteria"
      contains: "launchctl kickstart"
  key_links:
    - from: "user's live OpenClaw gateway (post-deploy, post-kickstart)"
      to: "AOF daemon /v1/deliveries/wait long-poll IPC chain → openclaw runtime.system.enqueueSystemEvent + requestHeartbeatNow → user's `main` agent next-turn context"
      via: "dispatched task transitions to done → notifier packages systemEvent + heartbeat → plugin enqueues + heartbeats → agent's heartbeat-driven turn includes the completion text"
      pattern: "wake-up.system-event-enqueued.*wake-up.heartbeat-requested.*wake-up.delivered"
---

<objective>
Phase-gate Plan 45. The 20 validation lanes (L1-L20) cover the mechanical chain end-to-end via automated tests. This plan adds the unmockable acceptance test: a real OpenClaw gateway, real Telegram, real `main` agent, real LLM-driven turn — does the agent actually wake up and react, or does it just sit there waiting for human input?

Per RESEARCH.md §"Testing Strategy Layer 3" the manual UAT IS the load-bearing acceptance test for D-45-GOAL. No code is changed in this plan; one UAT document is created and the user runs through it interactively.

Per CLAUDE.md §Build & Release: dual launchctl kickstart (gateway AND daemon — restarting only the gateway leaves the daemon on old code). `op run` wrapper presence check before kickstart (historical breakage point). Reboot-or-kill-zombies for zombie `openclaw-agent` (Flavor 1) AND stale `openclaw` workers (Flavor 2 — manifests as missing-chunk errors).

Purpose: D-45-GOAL — the dispatcher's next turn must REACT to the completion as turn-context, not just have a chat post a human can point at. D-45-FALLBACK-WARNING — when capability is absent, the human sees the warning where they will see it (in chat). D-45-FEATURE-DETECT — graceful degrade.
Output: 45-UAT.md document. One checkpoint that pauses for user confirmation before Phase 45 is considered complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-CONTEXT.md
@.planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-RESEARCH.md
@.planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-VALIDATION.md
@.planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-01-SUMMARY.md
@.planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-02-SUMMARY.md
@.planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-03-SUMMARY.md
@.planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated sweep one more time; confirm Plan 04 verification still GREEN before deploy</name>
  <files>— (no files modified — verification gate)</files>
  <read_first>
    - CLAUDE.md §Build & Release + §Orphan vitest workers
    - .planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-04-SUMMARY.md (confirmation that Plan 04 Task 3 was GREEN)
  </read_first>
  <action>
    Re-run the Plan 04 Task 3 sweep to confirm nothing has drifted between Plan 04 completion and now (typically within minutes — but be defensive in case auto-mode is interrupted).

    0. Pre-run orphan kill:
       ```
       ps -eo pid,command | grep -E "node \(vitest" | grep -v grep | awk '{print $1}' | xargs -r kill -9
       ```

    1. `npm run typecheck`
    2. `npm test`
    3. `AOF_INTEGRATION=1 npx vitest run tests/integration/wake-up-dispatcher.test.ts --config tests/integration/vitest.config.ts`
    4. `AOF_INTEGRATION=1 npm run test:integration:plugin`
    5. `npm run test:e2e`
    6. `npm run build`
    7. Post-run orphan kill:
       ```
       ps -eo pid,command | grep -E "node \(vitest" | grep -v grep | awk '{print $1}' | xargs -r kill -9
       ```

    Each step exits 0. If anything fails, halt and return PLANNING INCONCLUSIVE with the failing step. Do not proceed to UAT until typecheck + unit + integration + E2E + build are all GREEN.
  </action>
  <behavior>
    - Full automated sweep GREEN
    - npm run build produces a fresh dist/ ready for `npm run deploy` in Task 3
  </behavior>
  <verify>
    <automated>npm run typecheck && npm test && AOF_INTEGRATION=1 npx vitest run tests/integration/wake-up-dispatcher.test.ts --config tests/integration/vitest.config.ts && npm run test:e2e && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All 5 sweep commands exit 0.
    - `ls dist/` (or wherever the build output lands) shows fresh files with mtime within the last 5 minutes.
    - Post-sweep `ps -eo pid,pcpu,command | grep vitest | grep -v grep` is empty.
  </acceptance_criteria>
  <done>Full automated sweep re-confirmed GREEN; build artifact fresh; safe to deploy.</done>
</task>

<task type="auto">
  <name>Task 2: Write 45-UAT.md — the human-verify runbook</name>
  <files>
    - .planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-UAT.md (CREATE)
  </files>
  <read_first>
    - CLAUDE.md §Build & Release (full — `op run` plist verify, dual launchctl kickstart, both zombie flavors)
    - .planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-RESEARCH.md §"Testing Strategy" §Layer 3 (the canonical UAT step list — copy verbatim, expand with grep-able log assertions)
    - .planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-CONTEXT.md §specifics (the sessionKey topic the user runs the probe in: `agent:main:telegram:group:-1003844680528:topic:1`)
    - .planning/phases/44-deliver-completion-notification-wake-ups-to-dispatching-agen/44-UAT.md (Phase 44's UAT doc as a structural template — same daemon log location, same probe command shape)
    - .planning/phases/44-deliver-completion-notification-wake-ups-to-dispatching-agen/44-BLOCKERS.md (the failure mode that motivated Phase 45 — the orchestrator NOT reacting; the UAT must explicitly assert "agent reacts" not just "message lands")
  </read_first>
  <action>
    Create `45-UAT.md` with the structure below. Every step MUST be a literal command the user can paste; every assertion MUST be a literal grep / string the user can check.

    ```markdown
    # Phase 45 UAT — Wake dispatching sessions via system-event injection

    **Goal of this UAT:** prove D-45-GOAL — the dispatching agent's next turn includes the completion as injected turn-context AND the agent reacts (posts in chat about the completion) WITHOUT a human poke.

    **Pre-flight (do this BEFORE deploying):**

    1. Confirm Plan 04 Task 3 sweep is GREEN (paste link to `/tmp/45-04-verify.txt` from Plan 04).
    2. Confirm `op run` wrapper present in BOTH plist files (CLAUDE.md §Build & Release historical-breakage check):
       ```bash
       rg -A 1 "ProgramArguments" ~/Library/LaunchAgents/ai.openclaw.gateway.plist ~/Library/LaunchAgents/ai.openclaw.aof.plist | rg "oprun|op run|\.openclaw/bin/"
       ```
       Each plist must show an `op run …` invocation. If missing, restore from the most recent `.bak-pre-oprun-restore-*` BEFORE proceeding.

    **Deploy:**

    3. From the repo root:
       ```bash
       npm run deploy
       ```
       Wait for completion. Confirm `~/.aof/dist/daemon/index.js` mtime is within the last 30 seconds:
       ```bash
       stat -f %m ~/.aof/dist/daemon/index.js
       ```

    **Restart:**

    4. Dual kickstart (CLAUDE.md mandate — restarting only one is a trap):
       ```bash
       launchctl kickstart -k "gui/$(id -u)/ai.openclaw.aof"
       launchctl kickstart -k "gui/$(id -u)/ai.openclaw.gateway"
       ```

    5. Confirm both jobs back online:
       ```bash
       launchctl print "gui/$(id -u)/ai.openclaw.aof"     | rg "state ="
       launchctl print "gui/$(id -u)/ai.openclaw.gateway" | rg "state ="
       ```

    **Zombie cleanup (CLAUDE.md §Build & Release Flavor 1 + Flavor 2):**

    6. Kill zombie openclaw-agent processes (Flavor 1 — stale AOF plugin code):
       ```bash
       AOF_DEPLOY_MTIME=$(stat -f %m ~/.aof/dist/daemon/index.js)
       ps -eo pid,lstart,command | rg openclaw-agent | rg -v grep
       ```
       For each agent process whose lstart predates the deploy mtime, `kill -9 <pid>`. Re-run the ps until empty.

    7. Kill stale openclaw worker processes (Flavor 2 — stale OpenClaw chunks):
       ```bash
       INSTALL_MTIME=$(stat -f %m /opt/homebrew/lib/node_modules/openclaw/)
       ps -eo pid,lstart,command | rg "^[[:space:]]*[0-9]* .* openclaw[[:space:]]*$"
       ```
       Kill any worker whose lstart predates `$INSTALL_MTIME` (or the deploy mtime, whichever is later).

       (Or reboot — clears both flavors atomically.)

    **Probe (D-45-GOAL — Scenario A: capability present, agent reacts):**

    8. From Telegram, in the topic `agent:main:telegram:group:-1003844680528:topic:1`, send to the `main` agent:
       > "Dispatch a probe task that asks researcher to write 'hello world' and exits."

    9. Walk away. Do NOT send any further messages in this topic. Wait for the child task to reach `done`.

    10. Watch the daemon log live in another terminal:
        ```bash
        tail -F ~/.aof/data/daemon.log | rg --line-buffered "wake-up\.|TASK-"
        ```

    11. **PASS criterion (D-45-GOAL):** within ~15 seconds of the child task reaching `done`, the `main` agent posts a message in the SAME Telegram topic that acknowledges the completion. The post is from `main`, NOT the AOF audit message. Examples of acceptable wording (it's an LLM, exact text varies):
        - "The probe task completed; researcher wrote 'hello world'."
        - "✓ Probe done — researcher returned 'hello world'."
        - Any natural acknowledgement that demonstrates `main` saw the completion as turn context.

        **FAIL criterion:** chat shows ONLY the AOF audit message (`✓ TASK-NNN (done) — …`). `main` stays silent until you message it again.

    12. Verify the daemon log shows the full Phase 45 chain for that task:
        ```bash
        TASK_ID=<the task id from step 11>
        rg "$TASK_ID" ~/.aof/data/daemon.log | rg "wake-up\."
        ```
        Expected lines:
        - `wake-up.attempted` with `channel="both"`
        - `wake-up.system-event-enqueued`
        - `wake-up.heartbeat-requested`
        - `wake-up.delivered` with `channel="both"`

    13. Verify the chat audit message is one line and shows `agent: main`:
        - The audit message MUST start with `✓ ` (for done) or `⚠ ` (for failure)
        - MUST contain `[agent: main]`, NOT `[agent: ?]` or `Agent: unknown`
        - MUST NOT have a multi-line "Task complete: / Agent: / Reason:" block (Phase 44's old format)

    **Probe (D-45-FALLBACK-WARNING — Scenario B: capability absent, fallback warning visible):**

    14. From the repo root, monkey-patch the runtime (only the gateway side; do not commit):
        ```bash
        # Temporary — comment out the runtime.system field in the deployed plugin
        # OR run a one-off node script that imports the plugin and constructs the API
        # without runtime.system. RESEARCH.md §"Manual-Only Verifications" describes
        # the technique. Easiest reproducible method: run `npm install -g openclaw@<old-version>`
        # for an OpenClaw build that predates runtime.system.
        ```
        (Document the exact monkey-patch the user applied.)

    15. Repeat steps 4-7 (kickstart both jobs, kill zombies).

    16. Repeat steps 8-9 (dispatch probe, walk away).

    17. **PASS criterion (D-45-FALLBACK-WARNING + D-45-FEATURE-DETECT):**
        - Chat audit message contains the FALLBACK_WARNING_LINE substring: `Session-context wake-up not delivered`
        - Daemon log contains: `wake-up.system-event-unavailable` (boot-time AND/OR per-attempt)
        - Daemon log shows `wake-up.attempted` with `channel="chat"` (NOT `"both"`)
        - `main` agent does NOT react (this is the expected degraded behavior — confirms fallback is honest)
        - No exceptions or stack traces in the log

    18. Restore by reinstalling the current OpenClaw and repeating step 4-7.

    **Phase 44 regression check:**

    19. After Scenario A passes, the Phase 44 chat audit message MUST still appear in chat. Phase 45 only adds behavior; it does not remove the chat path. Confirm the chat post lands within ~2 seconds of the child transition (Phase 44 timing).

    **Sign-off:**

    | Check | Expected | Actual | Result |
    |-------|----------|--------|--------|
    | D-45-GOAL: agent reacts in chat without poke (Scenario A) | main posts about the completion within 15s | (fill in) | PASS / FAIL |
    | wake-up.system-event-enqueued log fires | line present | (fill in) | PASS / FAIL |
    | wake-up.heartbeat-requested log fires | line present | (fill in) | PASS / FAIL |
    | wake-up.delivered carries channel="both" | line present | (fill in) | PASS / FAIL |
    | Chat audit one-line + agent: main | format match | (fill in) | PASS / FAIL |
    | D-45-FALLBACK-WARNING visible (Scenario B) | warning text in chat | (fill in) | PASS / FAIL |
    | wake-up.system-event-unavailable fires (Scenario B) | line present | (fill in) | PASS / FAIL |
    | Phase 44 audit message still appears (Scenario A) | message lands | (fill in) | PASS / FAIL |

    All rows must be PASS to consider Phase 45 shipped.

    **Debug aids if Scenario A fails despite logs showing successful enqueue+heartbeat:** the issue is OpenClaw-side (heartbeat handler, prompt context injection). Inspect the agent's transcript per CLAUDE.md §End-to-end debugging: `~/.openclaw/agents/main/sessions/<sid>.jsonl`. Filter for the heartbeat-driven turn around the time of the wake-up; look for whether `enqueueSystemEvent` text actually landed in the prompt context.
    ```

    Save the file. Do NOT commit yet — Plan 05 Task 3 is the checkpoint where the user runs through it.
  </action>
  <behavior>
    - 45-UAT.md exists at the canonical path
    - Each step is a literal command the user can paste
    - Each pass criterion is a grep / string the user can check
    - Both Scenario A (capability present) and Scenario B (capability absent) are documented
    - Phase 44 regression check included
  </behavior>
  <verify>
    <automated>test -f .planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-UAT.md && grep -c "launchctl kickstart" .planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-UAT.md` exists.
    - `grep -c "launchctl kickstart" .planning/phases/45-.../45-UAT.md` returns at least 2 (gateway + aof in dual-kickstart block).
    - `grep -c "op run" .planning/phases/45-.../45-UAT.md` returns at least 1.
    - `grep -c "wake-up.system-event-enqueued" .planning/phases/45-.../45-UAT.md` returns at least 1.
    - `grep -c "wake-up.heartbeat-requested" .planning/phases/45-.../45-UAT.md` returns at least 1.
    - `grep -c "wake-up.system-event-unavailable" .planning/phases/45-.../45-UAT.md` returns at least 1.
    - `grep -c "channel=\"both\"" .planning/phases/45-.../45-UAT.md` OR `grep -c 'channel="both"' .planning/phases/45-.../45-UAT.md` returns at least 1.
    - `grep -c "agent: main" .planning/phases/45-.../45-UAT.md` returns at least 1.
    - `grep -c "Session-context wake-up not delivered" .planning/phases/45-.../45-UAT.md` returns at least 1.
    - `grep -c "agent:main:telegram:group:-1003844680528:topic:1" .planning/phases/45-.../45-UAT.md` returns at least 1.
    - `grep -c "openclaw-agent" .planning/phases/45-.../45-UAT.md` returns at least 1 (zombie flavor 1 cleanup).
    - `grep -c "stale openclaw worker" .planning/phases/45-.../45-UAT.md` OR `grep -ci "flavor 2" .planning/phases/45-.../45-UAT.md` returns at least 1 (zombie flavor 2 cleanup).
    - The sign-off table contains 8 rows (one per check listed above).
  </acceptance_criteria>
  <done>UAT runbook created. Ready for human-verify checkpoint in Task 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CHECKPOINT — User runs the UAT and confirms PASS or reports gaps</name>
  <what-built>
    Phase 45 deliverables:
    - 7 RED test files now GREEN (all 20 validation lanes from 45-VALIDATION.md)
    - OpenClawApi.runtime.system optional surface
    - IPC envelope extended with optional systemEvent + heartbeat fields + per-channel ACK
    - Plugin invokes runtime.system.enqueueSystemEvent + requestHeartbeatNow when capability present
    - Daemon notifier packages the envelope, gates via systemEventCapable getter, emits four new wake-up.* telemetry events + channel dimension on existing two
    - One-line chat message format with correct dispatcherAgentId (Agent: unknown bug fixed)
    - Inline FALLBACK_WARNING_LINE when capability absent
    - POST /v1/plugin/capability route + daemon-side capability cache
    - Recovery path inherits Phase 45 behavior automatically via deliverOne
    - Manual UAT runbook at `.planning/phases/45-.../45-UAT.md`
  </what-built>
  <how-to-verify>
    Open `.planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-UAT.md` and follow EVERY step in order. Do not skip the pre-flight `op run` plist check or the zombie cleanup — those are the most common UAT-failure causes per CLAUDE.md.

    Steps:
    1. Pre-flight (steps 1-2 in the UAT doc): confirm plan 04 sweep GREEN; confirm op run wrapper present in plists
    2. Deploy (step 3): `npm run deploy`; verify daemon mtime
    3. Restart (steps 4-5): dual launchctl kickstart; both jobs back online
    4. Zombie cleanup (steps 6-7): kill stale openclaw-agent (Flavor 1) AND stale openclaw worker (Flavor 2)
    5. Scenario A — agent reacts (steps 8-13): dispatch probe from Telegram → walk away → confirm `main` posts a reaction in chat AND daemon log shows the full wake-up.* chain with channel="both" AND audit message is one-line + correct agent
    6. Scenario B — fallback warning (steps 14-18): monkey-patch OR install older OpenClaw → repeat probe → confirm warning text in chat + wake-up.system-event-unavailable in log + agent does NOT react (expected degraded behavior)
    7. Phase 44 regression check (step 19): audit message still appears in Scenario A
    8. Fill in the sign-off table; ALL rows must be PASS

    If any row is FAIL, paste the failing line / log snippet and describe the actual behavior. Resume signal will route to gap-closure planning.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "approved — all UAT checks PASS" (Phase 45 ships)
    - "gap — Scenario A fails: <description>" (return to /gsd-plan-phase 45 --gaps)
    - "gap — Scenario B fails: <description>" (return to /gsd-plan-phase 45 --gaps)
    - "gap — log assertion fails: <description>" (return to /gsd-plan-phase 45 --gaps)
    - "blocked — environment issue (op run / zombie / deploy): <description>" (resolve out-of-band, then resume)
  </resume-signal>
  <files>(none — this is a human-verify checkpoint; no files are modified by this task)</files>
  <action>See &lt;how-to-verify&gt; above. Executor: do NOT attempt automation here — this is checkpoint:human-verify. Pause and wait for the user to execute the UAT matrix in 45-UAT.md and reply with the resume-signal. The user fills in the sign-off table inside 45-UAT.md as they go.</action>
  <verify>
    <automated>MISSING — this is a checkpoint:human-verify task. Verification is performed by the human operator against .planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-UAT.md Scenarios A + B + Phase 44 regression check. Executor waits for the resume-signal before proceeding.</automated>
  </verify>
  <done>User replies with one of the resume-signal values. All 8 sign-off rows in 45-UAT.md ticked PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → live OpenClaw + AOF deployed system | UAT exercises the real code path against the user's real Telegram chat. The user authenticates via the existing `op run` 1Password pipe; Phase 45 introduces no new credential surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-05-01 | I (Information disclosure) | UAT runbook references the user's Telegram chat ID `-1003844680528` and topic `1` | accept | Already in CONTEXT.md; user-owned chat ID; not a secret. Documented in canonical references. |
| T-45-05-02 | T (Tampering) | Stale openclaw worker (Flavor 2) could route the probe through old code, masking a Phase 45 bug as "works for me" | mitigate | UAT step 7 explicitly kills Flavor-2 stale workers and recommends reboot as the atomic remedy. Sign-off table requires all PASS — a flaky reproduction would surface as inconsistent results. |
| T-45-05-03 | D (Denial of service) | UAT probe dispatches a real task in the user's project; failures could leave dangling tasks | accept | The probe asks for a trivial 'hello world'; researcher exits cleanly. If the task fails to complete, the user sees it in `aof tasks list` and can clean up manually. Same risk profile as Phase 44 UAT. |
</threat_model>

<verification>
Plan 05 verification IS the checkpoint in Task 3. The user fills in the UAT sign-off table and reports back via the resume-signal.

If all 8 sign-off rows are PASS → Phase 45 ships → close out per execute-phase workflow.

If any row is FAIL → orchestrator routes to `/gsd-plan-phase 45 --gaps` with the failure description.
</verification>

<success_criteria>
- 45-UAT.md exists with deploy + restart + zombie + Scenario A + Scenario B + sign-off matrix
- User runs the UAT and reports PASS for all 8 sign-off rows
- Live `main` agent reacts in chat to a real task completion without human poke (D-45-GOAL — the unmockable acceptance test)
- Capability-absent fallback proves the warning appears in chat (D-45-FALLBACK-WARNING)
- Phase 44 chat audit behavior preserved
</success_criteria>

<output>
After Task 3 resume-signal "approved":
1. Update the sign-off table in 45-UAT.md with the actual user report
2. Create `.planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-05-SUMMARY.md` documenting:
   - Sign-off matrix snapshot (8 rows, all PASS)
   - Any UAT findings worth capturing as project memory or lessons.md entries
   - Confirmation that Phase 45 closes the `aof_dispatch → walk-away → task completes → orchestrator resumes` loop
   - Pointer to the deferred refactor (vestigial chat-delivery code audit) — track in backlog
   - Recommendation: ship as v1.11.0 minor (per project memory: GSD milestone versions pair with release versions; bump from latest tag)
</output>
