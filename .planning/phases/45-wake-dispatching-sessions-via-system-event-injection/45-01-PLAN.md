---
phase: 45
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/ipc/__tests__/schemas.test.ts
  - src/ipc/__tests__/chat-delivery-queue.test.ts
  - src/openclaw/__tests__/openclaw-chat-delivery.test.ts
  - src/openclaw/__tests__/chat-message-sender.test.ts
  - src/openclaw/__tests__/bug-045-agent-unknown.test.ts
  - src/daemon/__tests__/notifier-recovery-on-restart.test.ts
  - src/daemon/__tests__/plugin-capability.test.ts
  - tests/integration/wake-up-dispatcher.test.ts
autonomous: true
tags: [phase-45, wake-up, system-event, tdd, red-tests, wave-0]
requirements:
  - D-45-PRIMITIVE
  - D-45-CHANNEL-ORTHOGONALITY
  - D-45-MESSAGE-BREVITY
  - D-45-BUG-AGENT-UNKNOWN
  - D-45-HEARTBEAT-POLICY
  - D-45-HEARTBEAT-TARGET
  - D-45-FEATURE-DETECT
  - D-45-FALLBACK-WARNING
  - D-45-DEDUP-KEY
  - D-45-DEDUP-INTERACTION-WITH-RECOVERY
  - D-45-TELEMETRY
  - D-45-TELEMETRY-DIMENSION

must_haves:
  truths:
    - "All 8 RED test files exist on disk with new Phase 45 cases that fail today (no production code yet)"
    - "Running each test file individually produces test failures whose error messages reference the missing Phase 45 fields/exports/behavior (e.g. 'systemEvent is not a function', 'expected channel: \"both\"', missing schema field)"
    - "TypeScript compiles (`npm run typecheck` exits 0) — RED tests use type assertions / placeholder types that compile but the test bodies fail at runtime"
    - "All 20 validation lanes (L1-L20) from 45-VALIDATION.md have at least one assertion landed in this plan"
    - "Round-trip ACK round-trip (Blocker #1): ChatDeliveryQueue end-to-end test asserts enqueueAndAwait().done resolves to the FULL parsed POST body when deliverResult is called with per-channel blocks"
    - "Agent-callback-fallback regression (Blocker #2): a Wave 0 test asserts the audit branch in openclaw-chat-delivery.ts:184-227 STILL fires when chat-send rejects — guards the contract that chat failures throw, never set result.chat.success: false"
  artifacts:
    - path: src/ipc/__tests__/schemas.test.ts
      provides: "Zod schema tests for ChatDeliveryRequest.systemEvent + ChatDeliveryRequest.heartbeat + ChatDeliveryResultPost.systemEvent/.heartbeat + new PluginCapabilityPost (L2, L3)"
      contains: "PluginCapabilityPost"
    - path: src/ipc/__tests__/chat-delivery-queue.test.ts
      provides: "Round-trip ACK test for Blocker #1 — enqueueAndAwait().done resolves with full ChatDeliveryResultPost (chat + systemEvent + heartbeat blocks); reject path on result.success=false preserved (NoPlatformError → kind='no-platform' → agent-callback-fallback contract)"
      contains: "enqueueAndAwait"
    - path: src/openclaw/__tests__/openclaw-chat-delivery.test.ts
      provides: "Notifier-side cases for envelope packaging, notifyOn gating, capability-absent fallback, telemetry events, channel dimension (L6, L7, L9, L13, L14, L15, L16); Blocker #2 agent-callback-fallback regression"
      contains: "Phase 45 system-event injection"
    - path: src/openclaw/__tests__/chat-message-sender.test.ts
      provides: "Plugin-side cases for runtime.system.enqueueSystemEvent + requestHeartbeatNow calls, contextKey, coalesceMs, target=last, fallback warning rendering, message format (L4, L5, L8, L10, L11)"
      contains: "Phase 45 system-event injection"
    - path: src/openclaw/__tests__/bug-045-agent-unknown.test.ts
      provides: "Regression test that locks D-45-BUG-AGENT-UNKNOWN — renderMessage uses delivery.dispatcherAgentId, not event.actor (L12)"
      contains: "bug-045"
    - path: src/daemon/__tests__/notifier-recovery-on-restart.test.ts
      provides: "Recovery-path cases that confirm replayUnnotifiedTerminals inherits Phase 45 system-event behavior with same contextKey (L17, L18)"
      contains: "Phase 45 system-event on recovery"
    - path: src/daemon/__tests__/plugin-capability.test.ts
      provides: "Capability transport (POST /v1/plugin/capability) tests + IpcDeps.setPluginCapability wiring (L19)"
      contains: "PluginCapabilityPost"
    - path: tests/integration/wake-up-dispatcher.test.ts
      provides: "E2E case that dispatches a task through the daemon→plugin chain and asserts both channels in the IPC envelope (L20); ACK forwarding assertion (Warning #7 — extends L20 to assert the ACK posted by the plugin contains result.systemEvent and result.heartbeat blocks)"
      contains: "Phase 45"
  key_links:
    - from: "RED tests in src/openclaw/__tests__/*.test.ts and src/daemon/__tests__/*.test.ts and src/ipc/__tests__/chat-delivery-queue.test.ts"
      to: "Future Wave 1-3 production code in src/ipc/schemas.ts, src/ipc/chat-delivery-queue.ts, src/openclaw/matrix-notifier.ts, src/daemon/daemon.ts, src/openclaw/types.ts, src/openclaw/openclaw-chat-delivery.ts, src/openclaw/chat-message-sender.ts, src/openclaw/adapter.ts"
      via: "test failures pinpoint exactly what each subsequent wave must implement"
      pattern: "Phase 45"
---

<objective>
Wave 0: install RED tests covering all 20 validation lanes (L1-L20) from 45-VALIDATION.md PLUS two new round-trip / contract-guard tests for the Blocker #1 (per-channel ACK plumbing) and Blocker #2 (chat-send-throws contract) checker findings. Per CLAUDE.md §Engineering Standards "TDD: failing test first." No production code is written in this plan — only tests that describe the behavior subsequent waves will implement.

The validation map says `src/openclaw/__tests__/notifier-recovery-on-restart.test.ts`; reality is that file lives at `src/daemon/__tests__/notifier-recovery-on-restart.test.ts` (verified). The map says `tests/e2e/wake-up-dispatcher.test.ts`; reality is `tests/integration/wake-up-dispatcher.test.ts` (verified). The map says `src/ipc/__tests__/schemas.test.ts` is "extended"; reality is the file does not exist and must be CREATED. This plan honors the verified paths.

Purpose: Make Wave 1-3 GREEN-by-construction. Every Wave 1+ task asserts "test from this Wave 0 file goes from RED to GREEN."
Output: 7 extended/created vitest unit test files + 1 extended integration test file, all failing at runtime, all type-checking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-CONTEXT.md
@.planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-RESEARCH.md
@.planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-PATTERNS.md
@.planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED tests for IPC schemas + plugin-capability route + recovery + bug regression + round-trip ACK (L2, L3, L12, L17, L18, L19 + Blocker #1 round-trip)</name>
  <files>
    - src/ipc/__tests__/schemas.test.ts (CREATE)
    - src/ipc/__tests__/chat-delivery-queue.test.ts (CREATE — Blocker #1 round-trip)
    - src/openclaw/__tests__/bug-045-agent-unknown.test.ts (CREATE)
    - src/daemon/__tests__/notifier-recovery-on-restart.test.ts (EXTEND)
    - src/daemon/__tests__/plugin-capability.test.ts (CREATE)
  </files>
  <read_first>
    - src/ipc/schemas.ts (full — current ChatDeliveryRequest at lines 152-169 + ChatDeliveryResultPost at lines 172-181; will need to import and reference these in tests)
    - src/ipc/chat-delivery-queue.ts (full — current Waiter typedef at 35-38, enqueueAndAwait at 59-104, deliverResult at 138-154; the round-trip RED test in 1E asserts the Phase 45 / Blocker-#1 expected shape that Plan 02 Task 3 will deliver)
    - src/ipc/__tests__/envelope.test.ts (analog test pattern for ipc schemas — same vitest + Zod parse style; this directory has NO existing schemas.test.ts file, so create a sibling following envelope.test.ts conventions)
    - src/ipc/types.ts (full — IpcDeps shape; new optional `setPluginCapability?: (post: PluginCapabilityPost) => void` field will need to be tested without writing it yet)
    - src/ipc/routes/delivery-result.ts (full — analog for the new POST /v1/plugin/capability route; same Zod parse + 405 + log pattern; tests in this plan describe the route handler that Wave 2 will create)
    - src/daemon/__tests__/notifier-recovery-on-restart.test.ts (full — existing Phase 44 fixture pattern at lines 50-73 to extend)
    - src/openclaw/openclaw-chat-delivery.ts (lines 138-200 — current `deliverOne` shape and current `renderMessage` rendering at lines 333-360; the bug-045 regression test asserts the FIXED behavior; the agent-callback-fallback audit branch at lines 184-227 is what Blocker #2 regression test in Task 2A guards)
    - src/openclaw/subscription-delivery.ts (full — `OpenClawChatDelivery` Zod schema with `dispatcherAgentId` field that the bug fix must read from)
    - .planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-PATTERNS.md §"src/ipc/schemas.ts" (Phase 45 extension sketch with exact Zod field shapes — copy verbatim into the test file as the expected schema-parse outputs)
    - .planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-RESEARCH.md §"Hook Point" + §"Telemetry Implementation Plan" (locked field names + values for assertion arguments)
  </read_first>
  <action>
    Create or extend each test file with the cases below. Use the existing project conventions:
    - vitest (`describe`, `it`, `expect`, `vi.fn()`, `vi.mock`)
    - `vi.mock("../../logging/index.js", () => ({ createLogger: () => ({ trace: vi.fn(), debug: vi.fn(), info: vi.fn(), warn: vi.fn(), error: vi.fn(), fatal: vi.fn(), child: vi.fn() }) }))` mock-logger pattern from `src/openclaw/__tests__/openclaw-chat-delivery.test.ts:13-15`
    - `.js` import path suffix per CLAUDE.md §Conventions
    - Tests MUST FAIL today (production code does not exist yet); they MUST type-check (use `// @ts-expect-error` only where the missing export breaks compilation, OR cast through `as unknown as ChatDeliveryRequest` to keep the test compilable while asserting on the runtime field)

    --- 1A. `src/ipc/__tests__/schemas.test.ts` (CREATE) — covers L2, L3, L19 schema portion ---

    Imports: `import { describe, it, expect } from "vitest"; import { ChatDeliveryRequest, ChatDeliveryResultPost, PluginCapabilityPost } from "../schemas.js";`

    Test cases (every one MUST fail today):

    1. `it("ChatDeliveryRequest accepts optional systemEvent block (L2)")` — feed a request with `systemEvent: { sessionKey: "agent:main:telegram:group:42", contextKey: "task:TASK-001:done", text: "Task TASK-001 reached status \"done\" — probe", deliveryContext: { channel: "telegram", to: "-42", threadId: "1" } }`. Assert `ChatDeliveryRequest.parse(payload).systemEvent?.contextKey === "task:TASK-001:done"`.

    2. `it("ChatDeliveryRequest accepts optional heartbeat block with target=last (L2)")` — feed `heartbeat: { sessionKey: "agent:main:...", agentId: "main", coalesceMs: 750, reason: "aof:wake-up", heartbeat: { target: "last" } }`. Assert `parse(payload).heartbeat?.coalesceMs === 750` AND `parse(payload).heartbeat?.heartbeat.target === "last"`.

    3. `it("ChatDeliveryRequest.heartbeat rejects non-'last' target (D-45-HEARTBEAT-TARGET)")` — feed `heartbeat.heartbeat: { target: "current" }`. Assert `.safeParse(payload).success === false`.

    4. `it("ChatDeliveryRequest tolerates absent systemEvent + heartbeat (backward compat)")` — feed a request shaped exactly like Phase 44 (no new fields). Assert parse succeeds AND `.systemEvent === undefined` AND `.heartbeat === undefined`.

    5. `it("ChatDeliveryResultPost accepts optional per-channel result blocks (L3)")` — feed `{ success: true, chat: { success: true }, systemEvent: { success: true }, heartbeat: { success: false, error: { kind: "heartbeat-request-failed", message: "..." } } }`. Assert parsed result preserves all three per-channel fields.

    6. `it("ChatDeliveryResultPost.chat is z.literal(true) — Blocker #2 contract")` — feed `{ success: true, chat: { success: false } }`; assert `.safeParse(payload).success === false` (chat success literal-false rejected). Then feed `{ success: true, chat: { success: true, error: { kind: "x", message: "y" } } }`; assert `.safeParse(payload).success === false` (chat carries no error field per Blocker #2).

    7. `it("PluginCapabilityPost parses { pluginId, systemEventCapable } (L19)")` — feed `{ pluginId: "openclaw", systemEventCapable: true }`; assert parse succeeds. Then feed `{ systemEventCapable: false }` (no pluginId); assert parse succeeds and `pluginId` defaults to `"openclaw"`.

    8. `it("PluginCapabilityPost rejects non-boolean systemEventCapable")` — feed `{ pluginId: "openclaw", systemEventCapable: "yes" }`; assert `.safeParse(payload).success === false`.

    --- 1B. `src/openclaw/__tests__/bug-045-agent-unknown.test.ts` (CREATE) — covers L12 ---

    Per CLAUDE.md the bug-NNN convention is `bug-NNN-description.test.ts`. This file is the regression lock for the rendered chat message containing `[agent: main]` (NOT `Agent: unknown` — the Phase 44 bug). Once this passes the bug cannot regress.

    Use `OpenClawChatDeliveryNotifier` from `../openclaw-chat-delivery.js`. Reuse the `makeFixture` + `makeEvent` style from the existing `openclaw-chat-delivery.test.ts:17-49`. Mock `messageTool.send = vi.fn(async () => ({ success: true, chat: { success: true } }))`.

    Test cases:

    1. `it("renderMessage uses delivery.dispatcherAgentId, not event.actor (D-45-BUG-AGENT-UNKNOWN)")` — create a subscription whose `delivery.dispatcherAgentId === "main"` and an event whose `actor === "system:scheduler"`. Trigger the notifier on a `done` transition. Assert the message argument passed to `messageTool.send` contains the substring `agent: main` AND does NOT contain the substring `unknown` AND does NOT contain `system:scheduler`.

    2. `it("renderMessage falls back to '?' when delivery.dispatcherAgentId is missing")` — same fixture but `delivery.dispatcherAgentId` is undefined. Assert message contains `agent: ?` (NOT `agent: undefined`, NOT `Agent: unknown`).

    --- 1C. `src/daemon/__tests__/notifier-recovery-on-restart.test.ts` (EXTEND) — covers L17, L18 ---

    Append a new `describe("replayUnnotifiedTerminals — Phase 45 system-event on recovery", () => { ... })` block. Use the existing `beforeEach` fixture (lines 50-73). Construct the notifier with `systemEventCapable: true`. Mock `messageTool.send = vi.fn(async () => ({ success: true, chat: { success: true }, systemEvent: { success: true }, heartbeat: { success: true } }))`.

    Test cases:

    1. `it("recovery-path call to deliverOne packages systemEvent + heartbeat with same shape as live path (L17)")` — seed a terminal-status task + an unnotified active subscription with `delivery.sessionKey: "agent:main:telegram:group:42"`. Call `chatNotifier.replayUnnotifiedTerminals(store)`. Assert `messageTool.send` was called with a 3rd arg containing `systemEvent: expect.objectContaining({ contextKey: "task:${taskId}:${toStatus}", sessionKey: "agent:main:telegram:group:42" })` AND `heartbeat: expect.objectContaining({ coalesceMs: 750, heartbeat: { target: "last" }, reason: "aof:wake-up" })`.

    2. `it("recovery contextKey matches live contextKey shape: task:{taskId}:{toStatus} (L18)")` — assert the contextKey value passed during recovery has format `^task:[A-Z0-9-]+:(done|review|failed|cancelled|deadletter)$`.

    --- 1D. `src/daemon/__tests__/plugin-capability.test.ts` (CREATE) — covers L19 ---

    Test the new POST /v1/plugin/capability route handler. Analog: `src/ipc/routes/delivery-result.ts` (POST + Zod parse + 405 on non-POST + 400 on bad body). Test the route handler in isolation by invoking `handlePluginCapability(req, res, deps)` directly with mocked req/res, like `src/daemon/__tests__/server.test.ts` patterns.

    Test cases (every one MUST fail today — handler does not exist):

    1. `it("POST /v1/plugin/capability with valid body invokes deps.setPluginCapability and returns 200 (L19)")` — mock `deps.setPluginCapability = vi.fn()`. Send POST with body `{ pluginId: "openclaw", systemEventCapable: true }`. Assert `deps.setPluginCapability` called once with `{ pluginId: "openclaw", systemEventCapable: true }` AND response status === 200.

    2. `it("POST with invalid body returns 400")` — body `{ systemEventCapable: "yes" }`. Assert response status === 400.

    3. `it("non-POST returns 405")` — GET request. Assert response status === 405.

    4. `it("missing deps.setPluginCapability returns 500 with internal error")` — `deps.setPluginCapability` undefined. Assert response status === 500.

    --- 1E. `src/ipc/__tests__/chat-delivery-queue.test.ts` (CREATE — Blocker #1 round-trip) ---

    This test guards the load-bearing fix from Plan 02 Task 3. Today the queue's `Waiter.resolve` typedef is `() => void`; per-channel ACK data parsed from the POST body is dropped on the floor. Plan 02 Task 3 widens it to `(result: ChatDeliveryResultPost) => void`. This test fails today (because the production code drops the data) and turns GREEN after Plan 02 Task 3 ships.

    Imports:
    ```typescript
    import { describe, it, expect, vi } from "vitest";
    import { ChatDeliveryQueue } from "../chat-delivery-queue.js";
    import type { ChatDeliveryRequest, ChatDeliveryResultPost } from "../schemas.js";
    ```

    Mock-logger pattern (same as openclaw-chat-delivery.test.ts:13-15):
    ```typescript
    vi.mock("../../logging/index.js", () => ({
      createLogger: () => ({
        trace: vi.fn(), debug: vi.fn(), info: vi.fn(), warn: vi.fn(), error: vi.fn(), fatal: vi.fn(), child: vi.fn(),
      }),
    }));
    ```

    Test cases (each MUST fail today — production code drops the per-channel data):

    1. `it("enqueueAndAwait().done resolves with the FULL parsed POST body when deliverResult is called with per-channel blocks (Blocker #1)")`:
       - Construct `const queue = new ChatDeliveryQueue();`
       - Call `const { id, done } = queue.enqueueAndAwait({ subscriptionId: "sub-1", taskId: "TASK-001", toStatus: "done", message: "✓ done", delivery: { kind: "openclaw-chat" } });`
       - Set up the awaited promise: `const acked = done;` (do not await yet)
       - Call `queue.deliverResult(id, { success: true, chat: { success: true }, systemEvent: { success: true }, heartbeat: { success: true } } as ChatDeliveryResultPost);`
       - Assert `await expect(acked).resolves.toEqual({ success: true, chat: { success: true }, systemEvent: { success: true }, heartbeat: { success: true } });`
       - Today this fails because `done` resolves to `undefined` (or `void`) — the `waiter.resolve()` call has no arg and the parsed POST body is dropped.

    2. `it("enqueueAndAwait().done resolves with chat-only ACK when deliverResult body has no per-channel blocks (backward compat)")`:
       - Same setup. Call `queue.deliverResult(id, { success: true } as ChatDeliveryResultPost);`
       - Assert `await expect(acked).resolves.toEqual({ success: true });` (no chat / systemEvent / heartbeat fields)
       - Confirms backward-compat with Phase 44 plugin ACKs.

    3. `it("enqueueAndAwait().done resolves with systemEvent-error block when plugin reports system-event-failed")`:
       - Setup as case 1. Call `queue.deliverResult(id, { success: true, chat: { success: true }, systemEvent: { success: false, error: { kind: "system-event-failed", message: "session events require a sessionKey" } } });`
       - Assert `await expect(acked).resolves.toMatchObject({ systemEvent: { success: false, error: { kind: "system-event-failed" } } });`

    4. `it("enqueueAndAwait().done REJECTS with kind preserved when result.success=false (Blocker #2 reject-path contract)")`:
       - Setup as case 1. Call `queue.deliverResult(id, { success: false, error: { kind: "no-platform", message: "cannot resolve platform for delivery" } });`
       - Assert `await expect(acked).rejects.toThrowError(/cannot resolve platform/);`
       - Assert the rejected error carries `kind === "no-platform"` (use a try/catch + cast to `Error & { kind?: string }`)
       - This guards the agent-callback-fallback contract: chat-send failures throw, never set chat.success: false.

    5. `it("backward compat: a Phase 44-shaped POST body still parses and resolves (no chat/systemEvent/heartbeat fields)")` — same as case 2 but stress-test that older plugins continue to work.

    --- 1F. update tests/integration/wake-up-dispatcher.test.ts ACK extension (Warning #7 lives in Task 2C below — call out from here) ---

    The Warning-#7 ACK forwarding assertion lives in Task 2C (`tests/integration/wake-up-dispatcher.test.ts`) — see Task 2C. This Task 1 covers the unit-level round-trip; Task 2C covers the integration-level forwarding.
  </action>
  <behavior>
    - Each new test file imports the schema/handler symbols that Wave 1+ will create. Before those waves land, the imports fail at runtime (ERR_MODULE_NOT_FOUND for handler) OR the parse/expect assertions fail (for schemas without the new fields). Either failure mode counts as RED.
    - `npm run typecheck` exits 0 — tests are written so they compile against the EXPECTED API. Where a symbol doesn't exist yet (`PluginCapabilityPost`, `handlePluginCapability`), import the path and let `tsc` flag the missing export with `// @ts-expect-error Wave 1 will export this` OR write the import as `import { ChatDeliveryRequest, ChatDeliveryResultPost } from "../schemas.js"; const PluginCapabilityPost = (await import("../schemas.js" as string)) as never;` (dynamic import only at runtime, lets typecheck pass).
    - Each test contains an explicit `expect(...).toBe(...)` that will fail at runtime today.
    - The chat-delivery-queue.test.ts round-trip cases fail today because the production `Waiter.resolve` typedef is `() => void` and resolves with no arg (Plan 02 Task 3 fixes this).
  </behavior>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ipc/__tests__/schemas.test.ts` exists.
    - File `src/ipc/__tests__/chat-delivery-queue.test.ts` exists.
    - File `src/openclaw/__tests__/bug-045-agent-unknown.test.ts` exists.
    - File `src/daemon/__tests__/plugin-capability.test.ts` exists.
    - `grep -c "Phase 45 system-event on recovery" src/daemon/__tests__/notifier-recovery-on-restart.test.ts` returns at least 1.
    - `grep -c "PluginCapabilityPost" src/ipc/__tests__/schemas.test.ts` returns at least 2 (import + at least one parse).
    - `grep -c "task:\\${" src/ipc/__tests__/schemas.test.ts` OR `grep -c "task:TASK-001:done" src/ipc/__tests__/schemas.test.ts` returns at least 1 (D-45-DEDUP-KEY shape asserted).
    - `grep -c "target.*last" src/ipc/__tests__/schemas.test.ts` returns at least 1 (D-45-HEARTBEAT-TARGET asserted).
    - `grep -c "z.literal(true)" src/ipc/__tests__/schemas.test.ts` OR `grep -c "Blocker #2 contract" src/ipc/__tests__/schemas.test.ts` returns at least 1 (chat literal-true contract asserted).
    - `grep -c "agent: main" src/openclaw/__tests__/bug-045-agent-unknown.test.ts` returns at least 1.
    - `grep -c "coalesceMs.*750" src/daemon/__tests__/notifier-recovery-on-restart.test.ts` returns at least 1.
    - `grep -c "Blocker #1" src/ipc/__tests__/chat-delivery-queue.test.ts` returns at least 1 (round-trip test marker).
    - `grep -c "enqueueAndAwait" src/ipc/__tests__/chat-delivery-queue.test.ts` returns at least 3 (multiple test cases).
    - `grep -c 'kind: "no-platform"' src/ipc/__tests__/chat-delivery-queue.test.ts` returns at least 1 (Blocker #2 reject-path contract).
    - `npm run typecheck` exits 0.
    - `npm test -- src/ipc/__tests__/schemas` exits NON-ZERO (RED — Phase 45 schema fields missing).
    - `npm test -- src/ipc/__tests__/chat-delivery-queue` exits NON-ZERO (RED — Waiter.resolve drops the per-channel data).
    - `npm test -- src/openclaw/__tests__/bug-045-agent-unknown` exits NON-ZERO (RED — fix not landed).
    - `npm test -- src/daemon/__tests__/plugin-capability` exits NON-ZERO (RED — handler missing).
  </acceptance_criteria>
  <done>5 test files created/extended, all type-checking, all failing at runtime with errors that point to specific Phase 45 production code Wave 1+ must add. Orphan vitest workers killed per CLAUDE.md mandate after the verify run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RED tests for plugin-side sender + daemon-side notifier + integration + agent-callback-fallback regression (L1, L4-L11, L13-L16, L20 + Blocker #2 regression + Warning #7 ACK forwarding)</name>
  <files>
    - src/openclaw/__tests__/openclaw-chat-delivery.test.ts (EXTEND)
    - src/openclaw/__tests__/chat-message-sender.test.ts (EXTEND if exists, else CREATE)
    - tests/integration/wake-up-dispatcher.test.ts (EXTEND)
  </files>
  <read_first>
    - src/openclaw/__tests__/openclaw-chat-delivery.test.ts (full — existing makeFixture + MockMatrixMessageTool patterns; new describe block goes at end of file)
    - src/openclaw/__tests__/dispatch-notification.test.ts (full — analog mock-pattern for chat-message-sender.test.ts: pure unit test, vi.fn for api.runtime.system, type-cast api shape via `as unknown as OpenClawApi`)
    - src/openclaw/__tests__/adapter.test.ts (full — `makeMockClient` + mock api shape that includes `runtime.channel.outbound.loadAdapter`)
    - src/openclaw/openclaw-chat-delivery.ts (full — current deliverOne flow + renderMessage at 333-360 + replayUnnotifiedTerminals at 250-321; tests assert the Phase 45 EXTENDED shape; the agent-callback-fallback audit branch lives at lines 184-227 — Blocker #2 regression test below pins this)
    - src/openclaw/chat-message-sender.ts (full — current sendChatDelivery returns Promise<void>; Phase 45 changes return to Promise<SendChatDeliveryResult>; tests assert the new return shape AND the new runtime.system calls; chat send body lines 110-183 STILL throws on failure — Blocker #2 contract)
    - src/openclaw/types.ts (full — to know that `OpenClawApi` will be extended with optional `runtime?.system` in Wave 1; tests reference `api.runtime.system.enqueueSystemEvent` and accept `// @ts-expect-error` until Wave 1 lands the type)
    - src/openclaw/subscription-delivery.ts (full — OpenClawChatDelivery schema with dispatcherAgentId, capturedAt, pluginId, sessionKey, channel, target, threadId fields used in test fixtures)
    - tests/integration/wake-up-dispatcher.test.ts (full — Phase 44 anchor; Phase 45 extension adds new it() blocks at the end)
    - tests/integration/vitest.config.ts OR existing AOF_INTEGRATION=1 gate pattern in tests/integration/* (for env-gate)
    - .planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-PATTERNS.md §"src/openclaw/__tests__/openclaw-chat-delivery.test.ts" + §"src/openclaw/__tests__/chat-message-sender.test.ts" + §"tests/integration/wake-up-dispatcher.test.ts" (verbatim test sketches to copy)
    - .planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-RESEARCH.md §"Telemetry Implementation Plan" + §"Coalesce Window Recommendation" (locked event names + 750ms value)
    - .planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-CONTEXT.md §decisions (D-45-PRIMITIVE through D-45-TELEMETRY-DIMENSION — locked behaviors)
  </read_first>
  <action>
    Extend or create each test file with the cases below. Use the same vitest + mock-logger conventions from Task 1. ALL tests MUST fail today; ALL must type-check.

    --- 2A. `src/openclaw/__tests__/openclaw-chat-delivery.test.ts` (EXTEND) — covers L6, L7, L9, L13, L14, L15, L16 + Blocker #2 regression ---

    Append a new describe block: `describe("OpenClawChatDeliveryNotifier — Phase 45 system-event injection", () => { ... })`.

    Mock messageTool: `const send = vi.fn(async () => ({ success: true, chat: { success: true }, systemEvent: { success: true }, heartbeat: { success: true } }));`

    Construct the notifier with the new `systemEventCapable` option (Wave 3 will add this option to the constructor): `new OpenClawChatDeliveryNotifier({ resolveStoreForTask, messageTool: { send }, systemEventCapable: true })`.

    For telemetry assertions, spy on the wakeLog.info/warn calls. Use the existing `vi.mock("../../logging/index.js", ...)` pattern from line 13-15; `vi.mocked(createLogger).mock.results[0].value.info` gives access to the spy.

    Test cases (each MUST fail today):

    1. `it("packages systemEvent + heartbeat envelope when systemEventCapable=true (L6)")` — seed task + sub with `delivery.sessionKey: "agent:main:telegram:group:42", channel: "telegram", target: "-42", threadId: "1", dispatcherAgentId: "main"`. Trigger `done` transition. Assert `send` called with 3rd arg `expect.objectContaining({ systemEvent: expect.objectContaining({ sessionKey: "agent:main:telegram:group:42", contextKey: "task:TASK-XXX:done", text: expect.any(String), deliveryContext: { channel: "telegram", to: "-42", threadId: "1" } }), heartbeat: expect.objectContaining({ sessionKey: "agent:main:telegram:group:42", agentId: "main", coalesceMs: 750, reason: "aof:wake-up", heartbeat: { target: "last" } }) })`.

    2. `it("notifyOn gating filters BOTH channels uniformly (L7)")` — sub with `notifyOn: ["done"]`. Trigger `review` transition. Assert `send` NOT called (chat AND system-event both gated).

    3. `it("omits systemEvent + heartbeat when systemEventCapable=false (L9 + L16)")` — construct with `systemEventCapable: false`. Trigger transition. Assert `send` 3rd arg has NO `systemEvent` key AND no `heartbeat` key. Assert wakeLog.info called with `"wake-up.attempted"` event AND `channel: "chat"` field.

    4. `it("emits wake-up.system-event-enqueued with required fields (L13)")` — capable=true; ack returns `{ success: true, chat: { success: true }, systemEvent: { success: true } }`. Assert wakeLog.info called with `"wake-up.system-event-enqueued"` event whose payload contains `subscriptionId, taskId, toStatus, sessionKey, dispatcherAgentId: "main", contextKey: "task:TASK-XXX:done"`.

    5. `it("emits wake-up.heartbeat-requested after enqueue (L14)")` — same setup; ack returns `heartbeat: { success: true }`. Assert wakeLog.info called with `"wake-up.heartbeat-requested"` event.

    6. `it("emits wake-up.system-event-failed with kind on systemEvent error (L15)")` — ack returns `systemEvent: { success: false, error: { kind: "system-event-failed", message: "system events require a sessionKey" } }`. Assert wakeLog.warn called with `"wake-up.system-event-failed"` AND payload `kind: "system-event-failed"` AND `message` substring matches.

    7. `it("emits wake-up.system-event-failed with kind on heartbeat error (L15)")` — ack returns `heartbeat: { success: false, error: { kind: "heartbeat-request-failed", message: "..." } }`. Assert wakeLog.warn called with `"wake-up.system-event-failed"` event AND payload `kind: "heartbeat-request-failed"`.

    8. `it("non-fatal: chat delivery proceeds when system-event fails (L15)")` — ack with `chat.success: true, systemEvent.success: false`. Assert subscription marked notified for the toStatus AND wakeLog.info called with `"wake-up.delivered"` (chat path still records).

    9. `it("wake-up.attempted carries channel='both' when capable (L16)")` — capable=true. Assert wakeLog.info called with `"wake-up.attempted"` AND payload `channel: "both"`.

    10. `it("wake-up.delivered carries channel reflecting actual outcomes (L16)")` — ack with both successes → assert payload `channel: "both"`. Then a separate test where ack returns chat.success=true, systemEvent.success=false → assert `channel: "chat"`.

    11. `it("Blocker #2 regression: agent-callback-fallback audit branch STILL fires when chat-send rejects")` — guards the contract that chat failures throw, never set result.chat.success: false. Mock `messageTool.send = vi.fn(async () => { const err = new Error("cannot resolve platform for delivery"); (err as Error & { kind?: string }).kind = "no-platform"; throw err; });`. Trigger a transition. Assert: (a) the existing agent-callback-fallback audit code path at `openclaw-chat-delivery.ts:184-227` runs (assert by spying on the audit-record logger call OR by asserting subscription.delivery is updated with `agent-callback-fallback` indicator — pick whichever is observable in the existing test fixture); (b) wakeLog.warn called with the existing `"wake-up.failed"` or analog event; (c) NO `wake-up.system-event-enqueued` log fires (capability of the chat path is irrelevant — chat threw first). This test fails today because the production code does not currently emit Phase 45 telemetry, so the assertion that `wake-up.system-event-enqueued` did NOT fire is vacuously true; however, after Plan 04 lands, this test pins the contract that a future executor wrapping chat-send in try/catch (turning throws into result.chat.success: false) would break.

    --- 2B. `src/openclaw/__tests__/chat-message-sender.test.ts` (EXTEND if exists, else CREATE) — covers L4, L5, L8, L10, L11 ---

    Check if file exists. If not, CREATE it (analog: `src/openclaw/__tests__/dispatch-notification.test.ts` — same imports + mock pattern). Either way, add a `describe("sendChatDelivery — Phase 45 system-event injection", () => { ... })` block with these cases.

    Imports: `import { describe, it, expect, vi } from "vitest"; import { sendChatDelivery } from "../chat-message-sender.js"; import type { OpenClawApi } from "../types.js"; import type { ChatDeliveryRequest } from "../../ipc/schemas.js";`

    Helper:
    ```
    function makeMockApi(overrides: Partial<OpenClawApi> = {}): OpenClawApi {
      const sendText = vi.fn(async () => undefined);
      return {
        registerService: vi.fn(),
        registerTool: vi.fn(),
        on: vi.fn(),
        runtime: {
          channel: { outbound: { loadAdapter: vi.fn(async () => ({ sendText })) } },
        } as unknown as OpenClawApi["runtime"],
        ...overrides,
      } as unknown as OpenClawApi;
    }
    ```

    Test cases:

    1. `it("calls runtime.system.enqueueSystemEvent with sessionKey + contextKey + deliveryContext (L4 + L8)")` — fake `enqueueSystemEvent = vi.fn(() => true)`. Build req with `systemEvent: { sessionKey: "agent:main:telegram:group:42", contextKey: "task:TASK-001:done", text: "Task TASK-001 reached status \"done\" — probe", deliveryContext: { channel: "telegram", to: "-42" } }`. Call `sendChatDelivery(api, req)`. Assert `enqueueSystemEvent` called with first arg = the text AND second arg containing `sessionKey, contextKey: "task:TASK-001:done", deliveryContext: { channel: "telegram", to: "-42" }`.

    2. `it("calls runtime.system.requestHeartbeatNow with coalesceMs=750 + target='last' (L5)")` — fake `requestHeartbeatNow = vi.fn()`. Build req with `heartbeat: { sessionKey: "agent:main:...", agentId: "main", coalesceMs: 750, reason: "aof:wake-up", heartbeat: { target: "last" } }`. Assert `requestHeartbeatNow` called with `expect.objectContaining({ sessionKey: "agent:main:...", agentId: "main", coalesceMs: 750, reason: "aof:wake-up", heartbeat: { target: "last" } })`.

    3. `it("returns SendChatDeliveryResult with per-channel success bits when both succeed")` — both runtime fakes succeed. Assert returned `result.chat === { success: true }` (Blocker #2 — chat is success-only, no error field), `result.systemEvent === { success: true }`, `result.heartbeat === { success: true }`.

    4. `it("tags synchronous throw from enqueueSystemEvent with kind='system-event-failed'")` — `enqueueSystemEvent.mockImplementation(() => { throw new Error("system events require a sessionKey"); })`. Assert `result.systemEvent === { success: false, error: { kind: "system-event-failed", message: "system events require a sessionKey" } }`.

    5. `it("tags synchronous throw from requestHeartbeatNow with kind='heartbeat-request-failed'")` — same shape, different mock.

    6. `it("skips system-event/heartbeat calls when api.runtime.system absent (graceful degrade)")` — `api.runtime` has no `system` key. Assert no throw, `result.systemEvent === undefined`, `result.heartbeat === undefined`.

    7. `it("preserves NoPlatformError throw path — chat send fails BEFORE system-event runs (Blocker #2 contract)")` — req without channel info; chat path throws NoPlatformError. Assert system-event mocks NOT called (chat path threw first; preserves Phase 44 fallback to `agent-callback-fallback` audit). Assert sendChatDelivery itself REJECTS with the NoPlatformError (does NOT return a `{ chat: { success: false } }` shape — chat failure is exception-only per Blocker #2).

    8. `it("plugin-side does NOT add fallback warning to message — daemon renders the warning (L10)")` — assert that `sendChatDelivery` does NOT mutate `req.message`. The fallback warning is daemon-rendered; the plugin sends it verbatim.

    --- 2C. `tests/integration/wake-up-dispatcher.test.ts` (EXTEND) — covers L20 + Warning #7 ACK forwarding ---

    Append new it() blocks under the existing AOF_INTEGRATION-gated describe.

    Test cases:

    1. `it("[Phase 45] full daemon → plugin → mock-runtime round-trip enqueues system event + requests heartbeat (L20)")` — extend the existing harness. Add a mock `runtime.system = { enqueueSystemEvent: vi.fn(() => true), requestHeartbeatNow: vi.fn() }` to the fakeApi. Construct notifier with `systemEventCapable: true` (or post capability via the new POST /v1/plugin/capability). Trigger a real transition to `done` via `store.transition(...)`. Wait for delivery. Assert `enqueueSystemEvent` called once with `contextKey: "task:${taskId}:done"` AND `requestHeartbeatNow` called once with `heartbeat: { target: "last" }`.

    2. `it("[Phase 45] capability absent: chat delivery succeeds, fallback warning in message, no system-event call (L20 fallback)")` — drop `system` from fakeApi.runtime. Notifier `systemEventCapable: false`. Trigger transition. Assert chat sendText called with message containing `"Session-context wake-up not delivered"` AND `enqueueSystemEvent` NOT called AND no exception thrown.

    3. `it("[Phase 45 — Warning #7] plugin's POST /v1/deliveries/{id}/result body carries result.systemEvent and result.heartbeat blocks when those fields are populated on the request envelope")`:
       - Same harness as case 1 (capable=true, mock runtime.system).
       - Spy on `client.postChatDeliveryResult` (or whatever the plugin's IPC client calls — extend the test harness's mock IPC layer to capture POST bodies).
       - Trigger a transition. Wait for delivery + ACK round-trip.
       - Assert the captured POST body to /v1/deliveries/{id}/result contains: `success: true`, `chat: { success: true }`, `systemEvent: { success: true }`, `heartbeat: { success: true }`.
       - This guards the load-bearing forwarding chain: sendChatDelivery → SendChatDeliveryResult → dispatchAndAck → postChatDeliveryResult.
  </action>
  <behavior>
    - All new test bodies fail at runtime today (production code missing).
    - `npm run typecheck` exits 0 — type-cast `as unknown as ChatDeliveryRequest` etc. where the shape is wider than current Zod schema.
    - Existing Phase 44 test cases continue to pass (Phase 45 tests are additive only — no existing test modified).
    - Blocker #2 regression test (case 11 in 2A) pins the contract that chat failures THROW; if a future executor turns the throw into `result.chat.success: false`, this test catches the regression.
    - Warning #7 ACK forwarding test (case 3 in 2C) asserts the structured ACK reaches the daemon (extension of L20).
  </behavior>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 45 system-event injection" src/openclaw/__tests__/openclaw-chat-delivery.test.ts` returns at least 1.
    - `grep -c "Phase 45 system-event injection" src/openclaw/__tests__/chat-message-sender.test.ts` returns at least 1.
    - `grep -c "Phase 45" tests/integration/wake-up-dispatcher.test.ts` returns at least 1.
    - `grep -c "Blocker #2" src/openclaw/__tests__/openclaw-chat-delivery.test.ts` returns at least 1 (agent-callback-fallback regression marker).
    - `grep -c "Warning #7" tests/integration/wake-up-dispatcher.test.ts` returns at least 1 (ACK forwarding test marker).
    - `grep -c "wake-up.system-event-enqueued" src/openclaw/__tests__/openclaw-chat-delivery.test.ts` returns at least 1.
    - `grep -c "wake-up.heartbeat-requested" src/openclaw/__tests__/openclaw-chat-delivery.test.ts` returns at least 1.
    - `grep -c "wake-up.system-event-failed" src/openclaw/__tests__/openclaw-chat-delivery.test.ts` returns at least 2 (kind=system-event-failed AND kind=heartbeat-request-failed).
    - `grep -c "channel.*both" src/openclaw/__tests__/openclaw-chat-delivery.test.ts` returns at least 1.
    - `grep -c "coalesceMs.*750" src/openclaw/__tests__/chat-message-sender.test.ts` returns at least 1.
    - `grep -c "task:.*:done" src/openclaw/__tests__/chat-message-sender.test.ts` returns at least 1 (D-45-DEDUP-KEY shape).
    - `grep -c "Session-context wake-up not delivered" tests/integration/wake-up-dispatcher.test.ts` returns at least 1 (D-45-FALLBACK-WARNING).
    - `grep -c "result.systemEvent" tests/integration/wake-up-dispatcher.test.ts` returns at least 1 (Warning #7 — ACK forwarding assertion).
    - `grep -c "result.heartbeat" tests/integration/wake-up-dispatcher.test.ts` returns at least 1.
    - `npm run typecheck` exits 0.
    - `npm test -- src/openclaw/__tests__/openclaw-chat-delivery` exits NON-ZERO (RED — Phase 45 cases fail; existing Phase 44 cases still pass).
    - `npm test -- src/openclaw/__tests__/chat-message-sender` exits NON-ZERO (RED).
    - Phase 44 unit tests in non-Phase-45 describe blocks continue to PASS — verified by `npm test -- src/openclaw` exiting non-zero ONLY due to Phase 45 cases (no regression in Phase 44 cases).
  </acceptance_criteria>
  <done>3 test files extended/created with all Phase 45 lanes covered + Blocker #2 regression + Warning #7 ACK forwarding. Total of 8 RED test files across Tasks 1+2 (path corrections noted). Orphan vitest workers killed after verify run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-author → test-suite | Test fixtures construct mock OpenClaw runtime; no untrusted input crosses the daemon/plugin trust boundary at this stage (no production code yet). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01-01 | I (Information disclosure) | Test fixtures use placeholder sessionKey `"agent:main:telegram:group:42"` | accept | Placeholder Telegram chat ID `42` is not real. Existing Phase 44 tests use the same convention. No real session keys committed. |
| T-45-01-02 | T (Tampering) | RED tests describe future production behavior | accept | Tests have no execution authority over production data. They run only in vitest sandbox under `npm test`. |
</threat_model>

<verification>
After both tasks complete:

```bash
npm run typecheck
npm test -- src/ipc/__tests__/schemas src/ipc/__tests__/chat-delivery-queue src/openclaw/__tests__/openclaw-chat-delivery src/openclaw/__tests__/chat-message-sender src/openclaw/__tests__/bug-045-agent-unknown src/daemon/__tests__/notifier-recovery-on-restart src/daemon/__tests__/plugin-capability 2>&1 | tail -50
```

Expected:
- typecheck exits 0
- The above test sweep exits NON-ZERO with failure messages mentioning the missing Phase 45 production symbols / wrong call shapes / missing log events. Each Phase 45 describe block fails; Phase 44 describe blocks pass.

After verify, kill orphan vitest workers per CLAUDE.md §Orphan vitest workers:
```bash
ps -eo pid,command | grep -E "node \(vitest" | grep -v grep | awk '{print $1}' | xargs -r kill -9
```
</verification>

<success_criteria>
- 8 RED test files exist with all 20 validation lanes (L1-L20) covered by at least one assertion + Blocker #1 round-trip + Blocker #2 contract regression + Warning #7 ACK forwarding
- TypeScript compiles cleanly
- Each Phase 45 test failure points to specific Wave 1+ work (missing schema field, missing handler, missing notifier option, missing log event, missing per-channel ACK plumbing)
- Phase 44 tests untouched and still GREEN
- Orphan vitest workers killed after verify run
</success_criteria>

<output>
After completion, create `.planning/phases/45-wake-dispatching-sessions-via-system-event-injection/45-01-SUMMARY.md` describing:
- Each test file created/extended with line-count delta
- Mapping table: test name → validation lane (L1-L20) → D-45-* requirement → expected Wave 1+ implementation site
- Confirmation that all 8 Wave 0 anchor files now exist with RED Phase 45 cases
- Note about path corrections from 45-VALIDATION.md (recovery test in src/daemon/, integration test in tests/integration/, schemas.test.ts CREATE not extend, chat-delivery-queue.test.ts NEW per Blocker #1)
- Explicit pointer to the Blocker #1 round-trip test (Task 1E) and the Blocker #2 regression test (Task 2A case 11) — these are the load-bearing contract guards
</output>
</content>
