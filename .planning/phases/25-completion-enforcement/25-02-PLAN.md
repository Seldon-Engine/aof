---
phase: 25-completion-enforcement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/aof/SKILL.md
  - src/openclaw/openclaw-executor.ts
  - src/openclaw/__tests__/executor.test.ts
autonomous: true
requirements:
  - GUID-01
  - GUID-02

must_haves:
  truths:
    - "SKILL.md instructs agents to always call aof_task_complete with a brief summary"
    - "SKILL.md warns agents that exiting without aof_task_complete fails the task"
    - "formatTaskInstruction() includes completion consequences (task marked failed, retried by another agent)"
    - "SKILL.md stays within budget gate ceiling (under 2150 tokens)"
  artifacts:
    - path: "skills/aof/SKILL.md"
      provides: "Completion protocol instructions for agents"
      contains: "aof_task_complete"
    - path: "src/openclaw/openclaw-executor.ts"
      provides: "Enhanced formatTaskInstruction with enforcement consequences"
      contains: "FAILED and retried"
  key_links:
    - from: "skills/aof/SKILL.md"
      to: "src/context/__tests__/context-budget-gate.test.ts"
      via: "Budget gate test validates SKILL.md token count"
      pattern: "context-budget-gate"
    - from: "src/openclaw/openclaw-executor.ts"
      to: "src/openclaw/__tests__/executor.test.ts"
      via: "Test validates formatTaskInstruction output"
      pattern: "formatTaskInstruction"
---

<objective>
Update agent-facing instructions to tell agents about the completion requirement. Both SKILL.md (loaded at skill discovery time) and formatTaskInstruction() (injected at dispatch time) must inform agents that exiting without calling `aof_task_complete` fails the task and triggers retry.

Purpose: Belt and suspenders — agents learn about the completion requirement through two channels. SKILL.md provides standing context, formatTaskInstruction() provides per-dispatch reinforcement with consequences.

Output: Updated SKILL.md with ~40 token completion protocol section, enhanced formatTaskInstruction() with enforcement consequences, extended executor tests.
</objective>

<execution_context>
@/Users/xavier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@skills/aof/SKILL.md
@src/openclaw/openclaw-executor.ts
@src/context/__tests__/context-budget-gate.test.ts
@src/openclaw/__tests__/executor.test.ts

<interfaces>
<!-- Key contracts the executor needs -->

From src/openclaw/openclaw-executor.ts (line 301-317):
```typescript
private formatTaskInstruction(context: TaskContext): string {
  // Builds instruction string with taskId, taskPath, projectId, projectRoot, taskRelpath
  // Already has: "CRITICAL: verify aof_task_complete tool is available"
  // Already has: "IMPORTANT: call aof_task_complete with taskId when done"
  // ENHANCE: Add enforcement consequences (task marked FAILED, retried by another agent)
}
```

From src/context/__tests__/context-budget-gate.test.ts:
- Budget ceiling: 2150 tokens for SKILL.md
- Current usage: ~1665 tokens
- Headroom: ~485 tokens
- Test command: `npx vitest run src/context/__tests__/context-budget-gate.test.ts`

SKILL.md location: skills/aof/SKILL.md (194 lines currently)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update SKILL.md with completion protocol section</name>
  <files>skills/aof/SKILL.md</files>
  <behavior>
    - Test 1: Budget gate test passes (SKILL.md under 2150 tokens) — run existing test
    - Test 2: SKILL.md contains "aof_task_complete" instruction text
  </behavior>
  <action>
    1. Add a "Completion Protocol" section to `skills/aof/SKILL.md`. Keep it under ~40 tokens to stay within the 485-token headroom. Add it near the end of the file, before any closing sections.

    Exact text to add (per research recommendation, ~35 tokens):
    ```markdown
    ## Completion Protocol

    Always call `aof_task_complete` with a brief summary when done. Exiting without this call fails the task and triggers retry. Include what you did and any artifacts produced.
    ```

    2. Verify the budget gate passes: `npx vitest run src/context/__tests__/context-budget-gate.test.ts`

    IMPORTANT: The addition MUST be terse. Do not add examples, do not add detailed explanations. The budget gate ceiling is 2150 tokens and current usage is ~1665 — exceeding this breaks CI.
  </action>
  <verify>
    <automated>npx vitest run src/context/__tests__/context-budget-gate.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - SKILL.md contains "Completion Protocol" section
    - Section instructs agents to call aof_task_complete with summary
    - Section warns that exiting without the call fails the task
    - Budget gate test passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enhance formatTaskInstruction with enforcement consequences</name>
  <files>src/openclaw/openclaw-executor.ts, src/openclaw/__tests__/executor.test.ts</files>
  <behavior>
    - Test 1: formatTaskInstruction output contains "FAILED" consequence warning
    - Test 2: formatTaskInstruction output contains "retried by another agent" language
    - Test 3: formatTaskInstruction output includes instruction to provide brief summary of actions
  </behavior>
  <action>
    1. Modify `formatTaskInstruction()` in `src/openclaw/openclaw-executor.ts` (line 314). The existing text already has a CRITICAL section about verifying the tool and an IMPORTANT section about calling it. Enhance the IMPORTANT section to include consequences and summary instruction.

    Replace the existing IMPORTANT line (end of line 314) with:
    ```typescript
    `**COMPLETION REQUIREMENT:** You MUST call \`aof_task_complete\` with taskId="${context.taskId}" when finished. ` +
    `If you exit without calling this tool, the task will be marked as FAILED and retried by another agent. ` +
    `Include a brief summary of actions taken and artifacts produced.`
    ```

    This replaces the softer "IMPORTANT: When you have completed this task..." wording with explicit enforcement language. Keep the CRITICAL section about verifying tool availability — it's still needed.

    2. Extend the existing executor test file `src/openclaw/__tests__/executor.test.ts` to verify the enhanced instruction text. Add test cases that check the formatTaskInstruction output contains:
       - "FAILED" (consequence)
       - "retried by another agent" (consequence)
       - "summary of actions" (summary requirement)

    If formatTaskInstruction is private, test it indirectly through the public spawnSession method's arguments, or check if the test file already has a pattern for testing private methods.
  </action>
  <verify>
    <automated>npx vitest run src/openclaw/__tests__/executor.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - formatTaskInstruction includes enforcement consequence language
    - Agents are told task will be FAILED if they exit without calling aof_task_complete
    - Agents are told to include a summary of actions taken
    - All executor tests pass
  </done>
</task>

</tasks>

<verification>
1. Budget gate passes: `npx vitest run src/context/__tests__/context-budget-gate.test.ts --reporter=verbose`
2. Executor tests pass: `npx vitest run src/openclaw/__tests__/executor.test.ts --reporter=verbose`
3. Full test suite green: `npm test`
</verification>

<success_criteria>
- SKILL.md has completion protocol section within budget ceiling
- formatTaskInstruction includes enforcement consequences
- Both instruction channels (SKILL.md and dispatch-time) tell agents about aof_task_complete requirement
- All tests pass including budget gate
</success_criteria>

<output>
After completion, create `.planning/phases/25-completion-enforcement/25-02-SUMMARY.md`
</output>
