---
phase: 28-schema-and-storage
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/schemas/subscription.ts
  - src/store/subscription-store.ts
  - src/store/__tests__/subscription-store.test.ts
autonomous: true
requirements: [SUB-04]

must_haves:
  truths:
    - "TaskSubscription Zod schema validates correct subscription data and rejects invalid data"
    - "Subscriptions persist as co-located subscriptions.json in task directories"
    - "Subscription writes use write-file-atomic for crash safety"
    - "CRUD operations (create, read, list, cancel) work correctly"
    - "Missing subscriptions.json returns empty subscriptions (no error)"
    - "Task directory is created automatically when first subscription is added"
  artifacts:
    - path: "src/schemas/subscription.ts"
      provides: "Zod schemas for SubscriptionGranularity, SubscriptionStatus, TaskSubscription, SubscriptionsFile"
      exports: ["SubscriptionGranularity", "SubscriptionStatus", "TaskSubscription", "SubscriptionsFile"]
    - path: "src/store/subscription-store.ts"
      provides: "SubscriptionStore class with CRUD operations"
      exports: ["SubscriptionStore"]
    - path: "src/store/__tests__/subscription-store.test.ts"
      provides: "Unit tests for schema validation and all CRUD operations"
      min_lines: 100
  key_links:
    - from: "src/store/subscription-store.ts"
      to: "src/schemas/subscription.ts"
      via: "import schemas for parse/validate"
      pattern: "import.*from.*schemas/subscription"
    - from: "src/store/subscription-store.ts"
      to: "write-file-atomic"
      via: "atomic writes for crash safety"
      pattern: "writeFileAtomic"
    - from: "src/store/subscription-store.ts"
      to: "task directory"
      via: "taskDirResolver constructor injection"
      pattern: "taskDirResolver"
---

<objective>
Create the subscription data model with Zod schema validation and a SubscriptionStore class providing CRUD operations with crash-safe filesystem persistence.

Purpose: Foundation for the entire v1.8 Task Notifications feature -- all subsequent phases (API, delivery, safety) build on this schema and store.
Output: Validated schema, working store with tests, co-located subscriptions.json persistence.
</objective>

<execution_context>
@/Users/xavier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-schema-and-storage/28-CONTEXT.md
@.planning/phases/28-schema-and-storage/28-RESEARCH.md
@.planning/phases/28-schema-and-storage/28-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/schemas/task.ts:
```typescript
export const TaskStatus = z.enum([
  "backlog", "ready", "in-progress", "blocked", "review", "done", "cancelled", "deadletter",
]);
export type TaskStatus = z.infer<typeof TaskStatus>;
```

From src/store/interfaces.ts:
```typescript
export interface ITaskStore {
  readonly tasksDir: string;
  get(id: string): Promise<Task | undefined>;
  // taskDir() is private -- reconstruct path as: join(tasksDir, status, taskId)
}
```

From src/store/task-store.ts:
```typescript
private taskDir(id: string, status: TaskStatus): string {
  return join(this.statusDir(status), id);
}
// statusDir returns: join(this.tasksDir, status)
```

Test pattern from src/store/__tests__/task-store-directory.test.ts:
```typescript
import { describe, it, expect, beforeEach, afterEach } from "vitest";
import { mkdtemp, rm, mkdir, writeFile, readdir, readFile } from "node:fs/promises";
import { join } from "node:path";
import { tmpdir } from "node:os";

let tmpDir: string;
beforeEach(async () => {
  tmpDir = await mkdtemp(join(tmpdir(), "aof-test-prefix-"));
});
afterEach(async () => {
  await rm(tmpDir, { recursive: true, force: true });
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema and test scaffold</name>
  <files>src/schemas/subscription.ts, src/store/__tests__/subscription-store.test.ts</files>
  <behavior>
    - Schema validates: valid subscription with all required fields (id uuid, subscriberId non-empty, granularity "completion"|"all", status "active"|"delivered"|"failed"|"cancelled", createdAt/updatedAt ISO datetime)
    - Schema validates: optional fields (deliveredAt, failureReason) accepted when present
    - Schema rejects: missing required fields (subscriberId, granularity, createdAt, updatedAt)
    - Schema rejects: invalid granularity value (e.g. "immediate")
    - Schema rejects: invalid status value (e.g. "pending")
    - Schema rejects: non-UUID id field
    - Schema defaults: status defaults to "active" when omitted
    - SubscriptionsFile validates: version must be literal 1, subscriptions array
    - SubscriptionsFile defaults: empty subscriptions array when omitted
  </behavior>
  <action>
RED phase:
1. Create `src/schemas/subscription.ts` with the Zod schemas:
   - `SubscriptionGranularity` = z.enum(["completion", "all"])
   - `SubscriptionStatus` = z.enum(["active", "delivered", "failed", "cancelled"])
   - `TaskSubscription` = z.object with fields: id (z.string().uuid()), subscriberId (z.string().min(1)), granularity, status (default "active"), createdAt (z.string().datetime()), updatedAt (z.string().datetime()), deliveredAt (z.string().datetime().optional()), failureReason (z.string().optional())
   - `SubscriptionsFile` = z.object with version (z.literal(1)), subscriptions (z.array(TaskSubscription).default([]))
   - Export all schemas and their `z.infer<>` types using the dual-export pattern (same name for schema const and type, matching src/schemas/task.ts pattern)
   - Add `.describe()` on each field following existing conventions

2. Create `src/store/__tests__/subscription-store.test.ts` with test stubs covering all behaviors listed above. Write tests FIRST, run them to confirm RED (schema imports work, tests fail on validation expectations).

GREEN phase:
3. Tests should pass immediately since schema is created in step 1 -- this is schema-only, no store logic yet. If any test fails, fix the schema to match the test expectations.

Commit: `test(28-01): add subscription schema with validation tests`
  </action>
  <verify>
    <automated>npx vitest run src/store/__tests__/subscription-store.test.ts -t "schema" 2>&1 | tail -20</automated>
  </verify>
  <done>TaskSubscription and SubscriptionsFile Zod schemas exist with type exports. Schema tests pass: valid data accepted, invalid data rejected, defaults applied correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SubscriptionStore CRUD with persistence</name>
  <files>src/store/subscription-store.ts, src/store/__tests__/subscription-store.test.ts</files>
  <behavior>
    - create(): creates subscription with UUID id, stores in subscriptions.json, returns TaskSubscription
    - create(): creates task directory (mkdir recursive) if it does not exist
    - create(): uses write-file-atomic for crash-safe writes
    - get(): returns subscription by id, undefined if not found
    - list(): returns all subscriptions for a task, empty array if no file
    - list(): accepts optional status filter (e.g. "active" only)
    - cancel(): sets subscription status to "cancelled", updates updatedAt timestamp
    - cancel(): throws if subscription not found
    - Missing file: readFile ENOENT returns { version: 1, subscriptions: [] }
    - Persisted data: survives read-after-write cycle (write, read back, data matches)
  </behavior>
  <action>
RED phase:
1. Add CRUD test cases to the existing test file in a new describe block (e.g. "SubscriptionStore"). Tests use a tmpDir with beforeEach/afterEach cleanup. The taskDirResolver in tests should simply return `join(tmpDir, "tasks", "ready", taskId)`. Run tests -- all new tests should fail (SubscriptionStore does not exist yet).

GREEN phase:
2. Create `src/store/subscription-store.ts` with:
   - `SubscriptionStore` class accepting `taskDirResolver: (taskId: string) => Promise<string>` in constructor
   - Private `readSubscriptionsFile(filePath)` method: reads JSON, parses with SubscriptionsFile.parse(), catches ENOENT and returns `{ version: 1, subscriptions: [] }`
   - `create(taskId, subscriberId, granularity)`: resolves dir, mkdir(dir, { recursive: true }), reads file, pushes new subscription (crypto.randomUUID(), ISO timestamps), writes with writeFileAtomic, returns subscription
   - `get(taskId, subscriptionId)`: reads file, finds by id, returns subscription or undefined
   - `list(taskId, opts?: { status?: SubscriptionStatus })`: reads file, filters by status if provided, returns array
   - `cancel(taskId, subscriptionId)`: reads file, finds subscription, sets status="cancelled" and updatedAt=now, writes atomically, returns updated subscription. Throws Error if not found.
   - All writes use `writeFileAtomic(filePath, JSON.stringify(data, null, 2))`

3. Run tests -- all should pass.

REFACTOR (if needed):
4. Clean up any duplication between read/write helpers.

Commit: `feat(28-01): implement SubscriptionStore with CRUD and atomic persistence`
  </action>
  <verify>
    <automated>npx vitest run src/store/__tests__/subscription-store.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>SubscriptionStore class exists with create/get/list/cancel methods. All tests pass: CRUD operations work correctly, writes are atomic via write-file-atomic, missing files handled gracefully, task directories auto-created.</done>
</task>

</tasks>

<verification>
1. All subscription store tests pass: `npx vitest run src/store/__tests__/subscription-store.test.ts`
2. Full test suite still passes: `npx vitest run`
3. Schema exports are importable: `import { TaskSubscription, SubscriptionsFile } from "../schemas/subscription.js"`
4. Store exports are importable: `import { SubscriptionStore } from "../store/subscription-store.js"`
</verification>

<success_criteria>
- TaskSubscription Zod schema validates/rejects data correctly with all required fields
- SubscriptionsFile schema wraps subscriptions array with version field
- SubscriptionStore.create() persists subscription to subscriptions.json using write-file-atomic
- SubscriptionStore.get() retrieves a subscription by ID
- SubscriptionStore.list() returns all subscriptions (with optional status filter)
- SubscriptionStore.cancel() soft-deletes by setting status to "cancelled"
- Missing subscriptions.json returns empty array (no error)
- Task directory auto-created on first subscription
- All tests pass, full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/28-schema-and-storage/28-01-SUMMARY.md`
</output>
