---
phase: 34-dead-code-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/task.ts
  - src/schemas/project.ts
  - src/schemas/gate.ts
  - src/schemas/workflow.ts
  - src/schemas/index.ts
  - src/dispatch/gate-evaluator.ts
  - src/dispatch/gate-conditional.ts
  - src/dispatch/gate-context-builder.ts
  - src/dispatch/index.ts
  - src/dispatch/executor.ts
  - src/dispatch/assign-executor.ts
  - src/dispatch/scheduler.ts
  - src/dispatch/escalation.ts
  - src/dispatch/__tests__/gate-conditional.test.ts
  - src/dispatch/__tests__/gate-context-builder.test.ts
  - src/dispatch/__tests__/gate-enforcement.test.ts
  - src/dispatch/__tests__/gate-evaluator.test.ts
  - src/dispatch/__tests__/gate-timeout.test.ts
  - src/schemas/__tests__/gate.test.ts
  - src/schemas/__tests__/workflow.test.ts
  - src/schemas/__tests__/task-gate-extensions.test.ts
  - src/store/task-store.ts
  - src/migration/gate-to-dag.ts
  - src/migration/__tests__/gate-to-dag.test.ts
  - src/packaging/migrations/002-gate-to-dag-batch.ts
  - src/packaging/__tests__/upgrade-scenarios.test.ts
  - src/cli/commands/setup.ts
autonomous: true
requirements: [DEAD-01, DEAD-02, DEAD-03, DEAD-04, DEAD-05]

must_haves:
  truths:
    - "No gate source files exist in src/schemas/ or src/dispatch/"
    - "No gate test files exist in src/dispatch/__tests__/ or src/schemas/__tests__/"
    - "No gate barrel re-exports exist in schemas/index.ts or dispatch/index.ts"
    - "No lazy migration code exists in task-store.ts"
    - "No unused imports remain in scheduler.ts"
    - "tsc --noEmit passes with zero errors"
    - "Full vitest suite passes with no regressions"
  artifacts:
    - path: "src/schemas/task.ts"
      provides: "TaskFrontmatter with inlined GateHistoryEntry, ReviewContext, TestSpec (no import from gate.js)"
      contains: "GateHistoryEntry"
    - path: "src/schemas/project.ts"
      provides: "ProjectManifest with inlined WorkflowConfig, RejectionStrategy, Gate (no import from workflow.js)"
      contains: "WorkflowConfig"
  key_links:
    - from: "src/schemas/task.ts"
      to: "no dependency on gate.ts"
      via: "inlined schemas replace import"
      pattern: "GateHistoryEntry.*=.*z\\.object"
    - from: "src/schemas/project.ts"
      to: "no dependency on workflow.ts"
      via: "inlined schemas replace import"
      pattern: "WorkflowConfig.*=.*z\\.object"
---

<objective>
Remove the entire legacy gate workflow system: source files, test files, barrel re-exports, lazy migration code, and unused scheduler imports. This is approximately 2,900 lines of dead code removal.

Purpose: Eliminate deprecated gate system code that has been superseded by DAG workflows since v1.2. The gate-to-DAG migration shipped in v1.3 months ago.
Output: Codebase compiles and all tests pass with ~2,900 fewer lines of legacy code.
</objective>

<execution_context>
@/Users/xavier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-dead-code-removal/34-CONTEXT.md
@.planning/phases/34-dead-code-removal/34-RESEARCH.md

@src/schemas/task.ts
@src/schemas/project.ts
@src/schemas/gate.ts
@src/schemas/workflow.ts
@src/schemas/index.ts
@src/dispatch/index.ts
@src/dispatch/executor.ts
@src/dispatch/assign-executor.ts
@src/dispatch/scheduler.ts
@src/dispatch/escalation.ts
@src/store/task-store.ts
@src/migration/gate-to-dag.ts
@src/packaging/migrations/002-gate-to-dag-batch.ts
@src/cli/commands/setup.ts

<interfaces>
<!-- Key types being inlined before gate.ts deletion -->

From src/schemas/gate.ts (to inline into task.ts):
- GateHistoryEntry: z.object with gate, role, agent?, entered, exited?, outcome?, summary?, blockers, rejectionNotes?, duration?
- ReviewContext: z.object with fromGate, fromAgent?, fromRole, timestamp, blockers, notes?
- TestSpec: z.object with given, when, then (status?, body_contains?)

From src/schemas/gate.ts (to inline into project.ts):
- Gate: z.object with id, role, canReject, when?, description?, requireHuman?, timeout?, escalateTo?

From src/schemas/workflow.ts (to inline into project.ts):
- RejectionStrategy: z.enum(["origin"])
- WorkflowConfig: z.object with name, rejectionStrategy, gates, outcomes?
- validateWorkflow function (move to project.ts or delete if unused elsewhere)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline gate schemas and delete gate source files with cascade cleanup</name>
  <files>
    src/schemas/task.ts, src/schemas/project.ts, src/schemas/gate.ts, src/schemas/workflow.ts,
    src/schemas/index.ts, src/dispatch/gate-evaluator.ts, src/dispatch/gate-conditional.ts,
    src/dispatch/gate-context-builder.ts, src/dispatch/index.ts, src/dispatch/executor.ts,
    src/dispatch/assign-executor.ts, src/dispatch/scheduler.ts, src/dispatch/escalation.ts
  </files>
  <action>
    This task follows the research-recommended commit ordering. Make incremental commits, each leaving tsc clean and vitest green.

    **Commit 1: Inline gate schemas into consuming files**
    1. In `src/schemas/task.ts`: Copy the `GateHistoryEntry`, `ReviewContext`, and `TestSpec` Zod schema definitions (with their type exports) directly into the file. Remove `import { GateHistoryEntry, ReviewContext, TestSpec } from "./gate.js"`. The schemas are pure Zod with no external imports beyond `z`. Keep `GateOutcome` only if referenced -- check first. Keep the `gate` optional field and `GateState` type and `superRefine` block in task.ts (they are already defined there, not imported from gate.ts).
    2. In `src/schemas/project.ts`: Copy `Gate`, `RejectionStrategy`, and `WorkflowConfig` Zod schema definitions (with type exports) directly into the file. Remove `import { WorkflowConfig } from "./workflow.js"`. The `Gate` schema is needed by `WorkflowConfig`. Also check if `validateWorkflow` is imported anywhere -- if not, do not bring it over; if yes, inline it too. Add a comment `/** @deprecated Legacy gate workflow types, kept for backward compat of persisted data */` above the inlined block.
    3. Run `npx tsc --noEmit` to verify. This commit changes no behavior.

    **Commit 2: Delete gate source files and clean all import cascades**
    1. Delete these 5 files:
       - `src/schemas/gate.ts`
       - `src/schemas/workflow.ts`
       - `src/dispatch/gate-evaluator.ts` (364 lines)
       - `src/dispatch/gate-conditional.ts` (164 lines)
       - `src/dispatch/gate-context-builder.ts` (238 lines)
    2. Clean import cascades in consuming files:
       - `src/dispatch/executor.ts`: Remove `import type { GateContext }` (line 9), remove `gateContext?` field (line 32) from `TaskContext` interface.
       - `src/dispatch/assign-executor.ts`: Remove `import { buildGateContext }` (line 16), remove the gate context injection block (lines ~150-169 that build and inject gate context into task context).
       - `src/dispatch/scheduler.ts`: Remove ALL gate-specific imports (lines 22, 23, 25, 27 per research). Also remove `checkGateTimeouts` call (line ~248). Additionally remove all other unused imports from prior extractions (lines 8-27 have 18+ symbols) -- remove any import that `tsc --noEmit` flags as unused.
       - `src/dispatch/escalation.ts`: Remove `escalateGateTimeout` function (~lines 51-167) and `checkGateTimeouts` function (~lines 169-229). Remove `WorkflowConfig` import. KEEP `checkHopTimeouts` and all DAG-related code. This removes ~180 lines from the 493-line file.
    3. Remove barrel re-exports:
       - `src/schemas/index.ts`: Remove lines ~94-107 that re-export from `"./gate.js"` and `"./workflow.js"`.
       - `src/dispatch/index.ts`: Remove lines ~11-18 that re-export from `"./gate-conditional.js"` and `"./gate-evaluator.js"`.
    4. Run `npx tsc --noEmit` then `./scripts/test-lock.sh run` to verify.
  </action>
  <verify>
    <automated>npx tsc --noEmit && ./scripts/test-lock.sh run && echo "PASS: Gate source removal complete"</automated>
  </verify>
  <done>
    - gate.ts, workflow.ts, gate-evaluator.ts, gate-conditional.ts, gate-context-builder.ts deleted
    - task.ts has inlined GateHistoryEntry, ReviewContext, TestSpec with no import from gate.js
    - project.ts has inlined Gate, RejectionStrategy, WorkflowConfig with no import from workflow.js
    - No gate imports remain in executor.ts, assign-executor.ts, scheduler.ts, escalation.ts
    - No gate barrel re-exports in schemas/index.ts or dispatch/index.ts
    - No unused imports in scheduler.ts
    - tsc --noEmit clean, vitest green
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete gate test files and remove lazy migration code</name>
  <files>
    src/dispatch/__tests__/gate-conditional.test.ts, src/dispatch/__tests__/gate-context-builder.test.ts,
    src/dispatch/__tests__/gate-enforcement.test.ts, src/dispatch/__tests__/gate-evaluator.test.ts,
    src/dispatch/__tests__/gate-timeout.test.ts, src/schemas/__tests__/gate.test.ts,
    src/schemas/__tests__/workflow.test.ts, src/schemas/__tests__/task-gate-extensions.test.ts,
    src/migration/__tests__/gate-to-dag.test.ts,
    src/store/task-store.ts, src/migration/gate-to-dag.ts,
    src/packaging/migrations/002-gate-to-dag-batch.ts,
    src/packaging/__tests__/upgrade-scenarios.test.ts, src/cli/commands/setup.ts
  </files>
  <action>
    **Commit 3: Delete gate test files**
    Delete these 8 test files:
    - `src/dispatch/__tests__/gate-conditional.test.ts` (298 lines)
    - `src/dispatch/__tests__/gate-context-builder.test.ts` (262 lines)
    - `src/dispatch/__tests__/gate-enforcement.test.ts` (542 lines)
    - `src/dispatch/__tests__/gate-evaluator.test.ts` (776 lines)
    - `src/dispatch/__tests__/gate-timeout.test.ts` (289 lines)
    - `src/schemas/__tests__/gate.test.ts` (364 lines)
    - `src/schemas/__tests__/workflow.test.ts` (334 lines)
    - `src/schemas/__tests__/task-gate-extensions.test.ts`
    Run `./scripts/test-lock.sh run` to verify remaining tests still pass.

    **Commit 4: Remove lazy migration and batch migration**
    1. In `src/store/task-store.ts`:
       - Remove `import { migrateGateToDAG }` (line ~26)
       - Remove lazy migration block in `get()` method (lines ~251-258)
       - Remove lazy migration block in `getByPrefix()` method (lines ~292-298)
       - Remove lazy migration block in `list()` method (lines ~343-352)
       - These blocks check for gate format and call migrateGateToDAG -- just remove them, the methods should pass through without migration.
    2. Delete `src/migration/gate-to-dag.ts` (240 lines)
    3. Delete `src/migration/__tests__/gate-to-dag.test.ts` (257 lines)
    4. Delete `src/packaging/migrations/002-gate-to-dag-batch.ts` (138 lines)
    5. In `src/cli/commands/setup.ts`: Remove the import of `migration002` and remove it from the migrations array (line ~65). Also update `src/packaging/__tests__/upgrade-scenarios.test.ts` to remove its import of `migration002` and any test cases that reference it (this file is confirmed to import `migration002`).
    6. Run `npx tsc --noEmit && ./scripts/test-lock.sh run` to verify.
  </action>
  <verify>
    <automated>npx tsc --noEmit && ./scripts/test-lock.sh run && ls src/dispatch/__tests__/gate-*.test.ts src/schemas/__tests__/task-gate-extensions.test.ts 2>/dev/null | wc -l | grep -q '^0$' && echo "PASS: Gate tests and migration removed"</automated>
  </verify>
  <done>
    - All 8 gate test files deleted (7 dispatch/schema gate tests + task-gate-extensions.test.ts)
    - gate-to-dag.test.ts deleted (257 lines)
    - Lazy migration blocks removed from task-store.ts get/getByPrefix/list
    - migration/gate-to-dag.ts deleted (240 lines)
    - 002-gate-to-dag-batch.ts deleted (138 lines)
    - setup.ts migration chain updated
    - upgrade-scenarios.test.ts updated to remove migration002 references
    - tsc --noEmit clean, vitest green
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` -- zero TypeScript errors
2. `./scripts/test-lock.sh run` -- full vitest suite green
3. `grep -r "gate-evaluator\|gate-conditional\|gate-context-builder\|GateSchema\|WorkflowGateSchema" src/ tests/` -- zero hits (excluding migration stubs if any remain)
4. `grep -r "from.*gate\.js\|from.*workflow\.js" src/ --include="*.ts"` -- zero hits (no remaining imports from deleted files)
</verification>

<success_criteria>
- 5 gate source files deleted (~1,036 lines)
- 8 gate test files deleted (including task-gate-extensions.test.ts)
- 2 migration test files deleted (~257 lines)
- Migration source + batch deleted (~378 lines)
- Barrel re-exports cleaned from schemas/index.ts and dispatch/index.ts
- Unused imports cleaned from scheduler.ts
- Lazy migration blocks removed from task-store.ts
- Gate context injection removed from assign-executor.ts
- Gate timeout functions removed from escalation.ts
- upgrade-scenarios.test.ts updated to remove migration002 references
- Total: approximately 2,500-3,000 lines removed
- Zero TypeScript compilation errors
- Zero test failures
</success_criteria>

<output>
After completion, create `.planning/phases/34-dead-code-removal/34-01-SUMMARY.md`
</output>
