---
phase: 40-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - src/memory/__tests__/memory-update.test.ts
  - src/memory/__tests__/memory-search.test.ts
  - src/memory/__tests__/memory-delete.test.ts
  - src/memory/__tests__/memory-list.test.ts
  - src/memory/__tests__/store-schema.test.ts
  - src/memory/__tests__/hash.test.ts
  - src/memory/__tests__/memory-store.test.ts
  - src/memory/__tests__/hnsw-resilience.test.ts
  - src/memory/__tests__/hnsw-index.test.ts
  - src/memory/__tests__/memory-get.test.ts
  - src/memory/__tests__/pipeline-integration.test.ts
  - src/cli/__tests__/memory-health.test.ts
  - src/drift/__tests__/adapters.test.ts
  - src/commands/__tests__/org-drift-cli.test.ts
  - src/commands/__tests__/memory-cli.test.ts
  - src/cli/commands/__tests__/trace.test.ts
  - src/trace/__tests__/trace-writer.test.ts
  - src/protocol/__tests__/dag-router-integration.test.ts
  - src/dispatch/__tests__/callback-delivery.test.ts
  - src/dispatch/__tests__/recovery-handlers.test.ts
  - src/dispatch/__tests__/lifecycle-handlers.test.ts
  - src/dispatch/__tests__/alert-handlers.test.ts
  - src/dispatch/__tests__/dag-scheduler-integration.test.ts
  - src/dispatch/__tests__/dag-timeout.test.ts
  - src/dispatch/__tests__/dag-transition-handler.test.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-04
  - TEST-05

must_haves:
  truths:
    - "At least 10 test files use createTestHarness or withTestProject for setup/teardown"
    - "Test files using store/logger mocks use createMockStore/createMockLogger instead of as-any casts"
    - "All 15 test files with missing temp dir cleanup now have proper cleanup (via harness or manual afterEach)"
    - "All existing tests still pass after migration"
  artifacts:
    - path: "src/memory/__tests__/memory-update.test.ts"
      provides: "Migrated test with temp cleanup"
    - path: "src/dispatch/__tests__/callback-delivery.test.ts"
      provides: "Migrated test using createMockStore"
    - path: "src/cli/commands/__tests__/trace.test.ts"
      provides: "Migrated test replacing 8 as-any casts"
  key_links:
    - from: "src/dispatch/__tests__/callback-delivery.test.ts"
      to: "src/testing/mock-store.ts"
      via: "import createMockStore"
      pattern: "import.*createMockStore.*from.*testing"
    - from: "src/memory/__tests__/memory-update.test.ts"
      to: "src/testing/index.ts"
      via: "cleanup in afterEach/afterAll"
      pattern: "afterEach|afterAll.*cleanup|rm.*recursive"
---

<objective>
Migrate test files to use the shared harness and mock factories created in Plan 01.

Purpose: Eliminate duplicated setup/teardown across 60+ test files, replace as-any mock casts with typed factories, and fix 15 files leaking temp directories. This is the adoption step that realizes the value of the infrastructure built in Plan 01.
Output: All identified test files migrated, zero as-any store/logger casts in migrated files, all temp dirs cleaned up.
</objective>

<execution_context>
@/Users/xavier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-test-infrastructure/40-CONTEXT.md
@.planning/phases/40-test-infrastructure/40-RESEARCH.md
@.planning/phases/40-test-infrastructure/40-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor should import these -->

From src/testing/index.ts (after Plan 01):
```typescript
// Harness
export { createTestHarness, withTestProject, type TestHarness } from "./harness.js";
// Mock factories
export { createMockStore, type MockTaskStore } from "./mock-store.js";
export { createMockLogger, type MockEventLogger } from "./mock-logger.js";
// Existing utilities
export { readEventLogEntries, findEvents, expectEvent } from "./event-log-reader.js";
export { getMetricValue } from "./metrics-reader.js";
export { readTasksInDir } from "./task-reader.js";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix temp dir cleanup in 15 files and migrate harness-compatible files</name>
  <files>
    src/memory/__tests__/memory-update.test.ts,
    src/memory/__tests__/memory-search.test.ts,
    src/memory/__tests__/memory-delete.test.ts,
    src/memory/__tests__/memory-list.test.ts,
    src/memory/__tests__/store-schema.test.ts,
    src/memory/__tests__/hash.test.ts,
    src/memory/__tests__/memory-store.test.ts,
    src/memory/__tests__/hnsw-resilience.test.ts,
    src/memory/__tests__/hnsw-index.test.ts,
    src/memory/__tests__/memory-get.test.ts,
    src/memory/__tests__/pipeline-integration.test.ts,
    src/cli/__tests__/memory-health.test.ts,
    src/drift/__tests__/adapters.test.ts,
    src/commands/__tests__/org-drift-cli.test.ts,
    src/commands/__tests__/memory-cli.test.ts
  </files>
  <action>
For each of the 15 files with missing temp dir cleanup:

1. Read the file to understand its setup pattern (mkdtemp usage, what tmpDir variable is called)
2. Determine if the file uses the standard AOF pattern (mkdtemp + FilesystemTaskStore + EventLogger) — if so, migrate to `createTestHarness()` or `withTestProject()`
3. If the file uses a different pattern (e.g., memory tests using mkdtemp for non-AOF purposes), add proper cleanup via `afterEach` or `afterAll` with `rm(tmpDir, { recursive: true, force: true })` — do NOT force the AOF harness on non-AOF test patterns

**For harness-compatible files** (those using mkdtemp + FilesystemTaskStore + EventLogger):
- Replace manual setup with `import { createTestHarness, type TestHarness } from "../../testing/index.js"`
- Replace `beforeEach` body with `harness = await createTestHarness()`
- Replace `afterEach` body with `await harness.cleanup()`
- Replace `tmpDir`, `store`, `logger` references with `harness.tmpDir`, `harness.store`, `harness.logger`
- Remove imports for mkdtemp, rm, tmpdir, FilesystemTaskStore, EventLogger that are no longer needed

**For non-harness files** (memory tests etc. that just need cleanup):
- Add `import { rm } from "node:fs/promises"` if not already present
- Add `afterEach(async () => { await rm(tmpDir, { recursive: true, force: true }); })` or `afterAll` depending on whether tmpDir is created per-test or once

Run tests after EACH file migration to catch regressions immediately (per research pitfall 4).
  </action>
  <verify>
    <automated>npx vitest run src/memory/__tests__/ src/cli/__tests__/ src/drift/__tests__/ src/commands/__tests__/ -x</automated>
  </verify>
  <done>All 15 files with missing cleanup now have proper afterEach/afterAll cleanup. Harness-compatible files migrated to createTestHarness. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate mock store/logger files to typed factories</name>
  <files>
    src/cli/commands/__tests__/trace.test.ts,
    src/trace/__tests__/trace-writer.test.ts,
    src/protocol/__tests__/dag-router-integration.test.ts,
    src/dispatch/__tests__/callback-delivery.test.ts,
    src/dispatch/__tests__/recovery-handlers.test.ts,
    src/dispatch/__tests__/lifecycle-handlers.test.ts,
    src/dispatch/__tests__/alert-handlers.test.ts,
    src/dispatch/__tests__/dag-scheduler-integration.test.ts,
    src/dispatch/__tests__/dag-timeout.test.ts,
    src/dispatch/__tests__/dag-transition-handler.test.ts
  </files>
  <action>
For each file using `as any` casts for store or logger mocks:

1. Read the file to understand its mock pattern
2. Replace inline mock objects with `createMockStore()` / `createMockLogger()` from `../../testing/index.js`
3. Remove `as any` or `as unknown as` casts for store/logger construction
4. Keep test-specific method overrides: `mockStore.get.mockResolvedValue(task)` stays as-is
5. If file has a local `createMockStore`/`createMockLogger` (e.g., trace-writer.test.ts, callback-delivery.test.ts), replace with the shared factory

**Specific files:**
- `trace.test.ts`: Replace 8 `store: mockStore as any` casts — use createMockStore() with pre-seeded tasks where applicable
- `trace-writer.test.ts`: Replace local createMockStore/createMockLogger with shared factories
- `dag-router-integration.test.ts`: Replace `logger: logger as any` with createMockLogger()
- `callback-delivery.test.ts`: Replace local createMockTaskStore with shared createMockStore()
- `recovery-handlers.test.ts`, `lifecycle-handlers.test.ts`, `alert-handlers.test.ts`, `dag-scheduler-integration.test.ts`, `dag-timeout.test.ts`, `dag-transition-handler.test.ts`: Replace inline logger mocks with createMockLogger()

Also scan for any additional test files in dispatch/__tests__/ that use the same patterns and migrate those too.

If any files from this list ALSO have missing temp dir cleanup, fix that too (may overlap with Task 1's scope if they use both patterns).

Run tests after each file migration.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/__tests__/trace.test.ts src/trace/__tests__/ src/protocol/__tests__/ src/dispatch/__tests__/ -x</automated>
  </verify>
  <done>All identified files use createMockStore/createMockLogger from shared testing utilities. Zero as-any casts for store/logger construction in migrated files. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — full test suite passes with zero regressions
- `grep -r "as any" src/**/__tests__/*.test.ts | grep -i "store\|logger" | wc -l` — count reduced significantly (target: zero for migrated files)
- `grep -rn "mkdtemp" src/**/__tests__/*.test.ts` cross-referenced with `grep -rn "rm(" src/**/__tests__/*.test.ts` — all mkdtemp files have corresponding cleanup
- At least 10 test files import from `src/testing/index.js`
</verification>

<success_criteria>
- 15 files with missing temp cleanup all have proper afterEach/afterAll cleanup
- At least 10 test files use createTestHarness or withTestProject
- All identified as-any mock files use createMockStore/createMockLogger
- Full test suite green — zero regressions from migration
</success_criteria>

<output>
After completion, create `.planning/phases/40-test-infrastructure/40-02-SUMMARY.md`
</output>
