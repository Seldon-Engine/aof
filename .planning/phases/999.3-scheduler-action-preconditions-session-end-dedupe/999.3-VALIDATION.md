---
phase: 999.3
slug: scheduler-action-preconditions-session-end-dedupe
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-25
---

# Phase 999.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Lanes derived from `999.3-RESEARCH.md` §Validation Architecture (L1–L13, R1–R8).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 3.x |
| **Config file** | `vitest.config.ts` (root) + `vitest.e2e.config.ts` (E2E) |
| **Quick run command** | `npx vitest run src/dispatch/__tests__/precondition.test.ts src/dispatch/__tests__/recovery-handlers.test.ts` |
| **Full suite command** | `npm run typecheck && npm test` |
| **E2E command** | `npm run test:e2e` |
| **Estimated runtime** | quick: <2s · full: ~10s · E2E: ~60s |

---

## Sampling Rate

- **After every task commit:** Run quick command (sub-2s)
- **After every plan wave:** `npx vitest run src/dispatch src/store/__tests__/task-store-concurrent-transition.test.ts` (~5–10s)
- **Before `/gsd-verify-work`:** Full suite must be green (`npm run typecheck && npm test`)
- **Pre-release gate:** `npm run test:e2e` green (60s, single fork)
- **Max feedback latency:** 10s for unit, 60s for E2E

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Lane | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------|-----------|-------------------|-------------|--------|
| 999.3-01-* | 01 | 0 | D-999.3-OBSERVABILITY (schema) | L9 | unit | `npx vitest run src/schemas/__tests__/event.test.ts` | ❌ W0 | ⬜ pending |
| 999.3-02-* | 02 | 1 | D-999.3-ENVELOPE, D-999.3-MATCH-FN | L1, L2 | unit | `npx vitest run src/dispatch/__tests__/precondition.test.ts` | ❌ W0 | ⬜ pending |
| 999.3-03-* | 03 | 2 | D-999.3-LOCK | L3, L8 | unit | `npx vitest run src/dispatch/__tests__/recovery-handlers.test.ts src/dispatch/__tests__/bug-2026-04-15-stale-heartbeat-precondition.test.ts` | ❌ W0 (regression test) | ⬜ pending |
| 999.3-04-* | 04 | 2 | D-999.3-LOCK, D-999.3-MISMATCH-RETURN | L4, L7 | unit | `npx vitest run src/dispatch/__tests__/lifecycle-handlers.test.ts` | ✅ | ⬜ pending |
| 999.3-05-* | 05 | 2 | D-999.3-LOCK | L5 | unit | `npx vitest run src/dispatch/__tests__/assign-executor.test.ts src/dispatch/__tests__/lock-reentrancy.test.ts` | ❌ W0 (lock-reentrancy) | ⬜ pending |
| 999.3-06-* | 06 | 2 | D-999.3-LOCK | L6 | unit | `npx vitest run src/dispatch/__tests__/alert-handlers.test.ts` | ✅ | ⬜ pending |
| 999.3-07-* | 07 | 3 | D-999.3-SCHEDULER-POPULATION | L10 | unit | `npx vitest run src/dispatch/__tests__/scheduler-helpers.test.ts` | ✅ | ⬜ pending |
| 999.3-08-* | 08 | 3 | D-999.3-BACKWARDS-COMPAT, D-999.3-TEST-MATRIX | L11, L12, L13 | integration | `npm run typecheck && npm test` + `npm run test:e2e` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Plan IDs are placeholders; final IDs assigned by gsd-planner.*

---

## Validation Lanes

| ID | Lane | Trigger (RED) | Success (GREEN) | File |
|---|---|---|---|---|
| **L1** | SchedulerAction type extension | Compile-time check: `expected?` field reachable; old action shapes still type-check | `npm run typecheck` passes; existing tests still compile | `src/dispatch/types.ts` (verified by every test that constructs a `SchedulerAction`) |
| **L2** | matchesExpected helper | Pure-function tests: each precondition dimension passes/fails alone, short-circuit order verified, undefined `expected` returns `{ok: true}` | All unit tests green | `src/dispatch/__tests__/precondition.test.ts` (NEW) |
| **L3** | handleStaleHeartbeat incident regression | Task at `in-progress`, transition to `blocked`, fire stale_heartbeat action with `expected.status: "in-progress"`. Pre-fix: handler proceeds. | Post-fix: `actionsFailed === 0`, `actionsExecuted === 0`, `scheduler_action_skipped` event in events.jsonl with `reason: "status_mismatch"` | `src/dispatch/__tests__/bug-2026-04-15-stale-heartbeat-precondition.test.ts` (NEW; uses `createTestHarness`) |
| **L3.1** | handleStaleHeartbeat per-precondition | Per-dimension tests for `status`, `leaseAgent`, `lastTransitionAtBefore` mismatches | Each emits skip event with correct `reason` | `src/dispatch/__tests__/recovery-handlers.test.ts` (extended) |
| **L4** | handleExpireLease precondition | Per-dimension tests; in-progress + blocked status pairs | Each emits skip event with correct `reason`; existing tests still pass | `src/dispatch/__tests__/lifecycle-handlers.test.ts` (extended) |
| **L5** | handleAssign precondition (inside executeAssignAction) | RED: action with `expected.status: "ready"` but task is now `in-progress` → skip; per-dimension tests. **Critical: precondition check goes INSIDE `executeAssignAction`, not at handler boundary, to avoid lockManager double-wrap deadlock (R1).** | Skip event emitted, no `acquireLease` call, no spawn | `src/dispatch/__tests__/assign-executor.test.ts` (extended) |
| **L6** | handleSlaViolation precondition | Per-dimension tests; status & lastTransitionAtBefore | Skip event emitted on mismatch | `src/dispatch/__tests__/alert-handlers.test.ts` (extended) |
| **L7** | handlePromote precondition | Per-dimension tests; status (backlog) and lastTransitionAtBefore | Skip event emitted on mismatch | `src/dispatch/__tests__/lifecycle-handlers.test.ts` (extended) |
| **L8** | lockManager re-entrancy proof | Outer `lockManager.withLock(taskId, body)` + inner `store.transition(taskId, …)` does not deadlock | Test completes within timeout (5s); no hang | `src/dispatch/__tests__/lock-reentrancy.test.ts` (NEW; uses real `InMemoryTaskLockManager` + real `FilesystemTaskStore`) |
| **L9** | scheduler_action_skipped event schema | Schema accepts new event type; payload validates; event appears in events.jsonl. **Wave 0 task: must land BEFORE any handler emits the event** (R2). | `EventType` enum includes `"scheduler_action_skipped"`; payload shape stable | `src/schemas/event.ts` + `src/schemas/__tests__/event.test.ts` (extended or new) |
| **L10** | Scheduler-side population | Each action-builder site populates `expected` correctly given a fixture task | Every action emitted by `checkExpiredLeases`, `checkBacklogPromotion`, `runPoll` (stale_heartbeat + sla_violation sites), `buildDispatchActions` carries the correct `expected` | `src/dispatch/__tests__/scheduler-helpers.test.ts` (extended) + poll-level test in `src/dispatch/__tests__/scheduler.test.ts` |
| **L11** | Backwards-compat (no `expected` field) | `SchedulerAction` without `expected` → handler proceeds normally, no skip event | Existing test fixtures (which don't set `expected`) keep passing untouched | All existing handler tests |
| **L12** | Fix A regression | Duplicate-file race test must keep passing without modification | `task-store-concurrent-transition.test.ts` GREEN | `src/store/__tests__/task-store-concurrent-transition.test.ts` (UNCHANGED) |
| **L13** | Integration sweep | Aggregated check: count of unresolved `scheduler_action_failed` of type `stale_heartbeat` across integration tests' events.jsonl is zero | No `scheduler_action_failed` of `type: "stale_heartbeat"` in any test's `events/*.jsonl` | Meta-assertion in `tests/integration/` (new tiny scan helper or extend an existing integration test) |

---

## Wave 0 Requirements

- [ ] `src/schemas/event.ts` — extend `EventType` enum with `"scheduler_action_skipped"` (covers L9). **MUST land first** before any handler emits the event (R2 — closed Zod enum will reject unknown values at runtime).
- [ ] `src/dispatch/precondition.ts` — new module hosting `matchesExpected` helper (covers L2 implementation surface).
- [ ] `src/dispatch/__tests__/precondition.test.ts` — covers L2.
- [ ] `src/dispatch/__tests__/lock-reentrancy.test.ts` — covers L8 (real `InMemoryTaskLockManager` + real `FilesystemTaskStore`, NOT mocks).
- [ ] `src/dispatch/__tests__/bug-2026-04-15-stale-heartbeat-precondition.test.ts` — covers L3 (incident regression; uses `createTestHarness`).
- [ ] `src/schemas/__tests__/event.test.ts` — extend if exists, otherwise create (covers L9 schema validation).

Existing test infrastructure (`createTestHarness`, `createMockStore`, `createMockLogger`) covers all other lanes; no framework install required.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Production race reproduction | D-999.3-TEST-MATRIX (L3) | The original 2026-04-15 incident only manifests under live `assign-helpers` + `handleStaleHeartbeat` collision in a deployed daemon — synthetic regression test approximates the shape but real production confirmation requires the Telegram-bound dispatch-and-die-mid-task scenario. | Post-deploy of v1.18.0: dispatch a task with a tight `timeoutMs` to a Telegram-bound agent that will exit without `aof_task_complete`. Watch `~/.aof/data/events/<date>.jsonl` for `scheduler_action_skipped` event with `reason: "status_mismatch"` and absence of duplicate-file errors. Document in `999.3-UAT.md`. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (schema, precondition module, tests for new files)
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s for quick gate
- [ ] `nyquist_compliant: true` set in frontmatter (set by gsd-planner after plans land)

**Approval:** pending
