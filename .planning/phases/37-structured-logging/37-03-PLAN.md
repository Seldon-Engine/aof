---
phase: 37-structured-logging
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - src/daemon/daemon.ts
  - src/daemon/index.ts
  - src/daemon/standalone-adapter.ts
  - src/service/aof-service.ts
  - src/protocol/router.ts
  - src/protocol/task-lock.ts
  - src/openclaw/openclaw-executor.ts
  - src/openclaw/adapter.ts
  - src/openclaw/matrix-notifier.ts
  - src/murmur/cleanup.ts
  - src/plugins/watchdog/index.ts
  - src/store/task-store.ts
  - src/mcp/server.ts
  - src/memory/index.ts
  - src/memory/project-memory.ts
  - src/metrics/exporter.ts
autonomous: true
requirements: [LOG-04, LOG-06]

must_haves:
  truths:
    - "Zero console.* calls remain in core module source files (daemon, service, protocol, openclaw, murmur, plugins, store, mcp, memory, metrics) excluding tests"
    - "CLI commands (src/cli/, src/commands/) still use console.* for user-facing output"
    - "EventLogger (src/events/) has zero changes"
    - "config/registry.ts console.warn is kept (runs before logger init)"
    - "All daemon startup/shutdown messages are structured logs, not console.error"
    - "All existing tests pass"
  artifacts:
    - path: "src/service/aof-service.ts"
      provides: "Migrated service with structured logging"
      contains: "createLogger"
    - path: "src/daemon/standalone-adapter.ts"
      provides: "Migrated daemon adapter with structured logging"
      contains: "createLogger"
    - path: "src/protocol/router.ts"
      provides: "Migrated protocol router with structured logging"
      contains: "createLogger"
  key_links:
    - from: "src/daemon/*.ts"
      to: "src/logging/index.ts"
      via: "import createLogger"
      pattern: "import.*createLogger.*from.*logging"
    - from: "src/service/aof-service.ts"
      to: "src/logging/index.ts"
      via: "import createLogger"
      pattern: "import.*createLogger.*from.*logging"
---

<objective>
Migrate all console.* calls in remaining core modules (daemon, service, protocol, openclaw, murmur, plugins, store, mcp, memory, metrics) to structured Pino logger. Verify CLI and EventLogger are untouched.

Purpose: Completes the console.* migration across all core modules and verifies the boundary between operational logging (structured) and user-facing output (console).
Output: Zero console.* in core modules, CLI and EventLogger confirmed unchanged.
</objective>

<execution_context>
@/Users/xavier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-structured-logging/37-RESEARCH.md
@.planning/phases/37-structured-logging/37-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate daemon, service, and protocol modules</name>
  <files>src/daemon/daemon.ts, src/daemon/index.ts, src/daemon/standalone-adapter.ts, src/service/aof-service.ts, src/protocol/router.ts, src/protocol/task-lock.ts</files>
  <action>
Migrate 6 files (~27 console.* calls, ~17 catch blocks):

**daemon/ (3 files, 8 console.*, 6 catch blocks):**
- `daemon.ts`: 1 console.* -> `const log = createLogger("daemon");`
- `index.ts`: 3 console.* -> `const log = createLogger("daemon");` -- All startup/shutdown messages become structured logs per user decision (no console.error exceptions)
- `standalone-adapter.ts`: 4 console.*, 5 catch blocks -> `const log = createLogger("daemon");` -- Silent catches get warn-level logging

**service/ (1 file, 15 console.*, 4 catch blocks):**
- `aof-service.ts`: `const log = createLogger("service");` -- This is the main service orchestrator. All console.log/error/warn become structured. Catch blocks get `log.warn({ err, op, ...ids }, "msg")`.

**protocol/ (2 files, 4 console.*, 6 catch blocks):**
- `router.ts`: 4 console.*, 4 catch blocks -> `const log = createLogger("protocol");`
- `task-lock.ts`: 0 console.*, 2 silent catch blocks -> `const log = createLogger("task-lock");` -- Remediate the 2 silent catches with warn-level logs

For each file:
1. `import { createLogger } from "../logging/index.js";` (adjust relative path as needed)
2. `const log = createLogger("component");` at module level
3. Replace all console.* with structured log calls
4. Remediate silent catches with `log.warn({ err, op }, "description")`
5. Use `err` field name for errors (not `error`) to trigger Pino's serializer

Add vi.mock for logging in any affected test files. Do not change business logic or control flow.
  </action>
  <verify>
    <automated>cd /Users/xavier/Projects/aof && grep -c "console\." src/daemon/daemon.ts src/daemon/index.ts src/daemon/standalone-adapter.ts src/service/aof-service.ts src/protocol/router.ts src/protocol/task-lock.ts 2>/dev/null; npx vitest run src/daemon/__tests__/ src/service/__tests__/ src/protocol/__tests__/ -x 2>&1 | tail -15</automated>
  </verify>
  <done>Zero console.* in daemon/, service/, protocol/ source files. All catch blocks emit structured logs. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate openclaw, murmur, plugins, store, mcp, memory, metrics and verify boundaries</name>
  <files>src/openclaw/openclaw-executor.ts, src/openclaw/adapter.ts, src/openclaw/matrix-notifier.ts, src/murmur/cleanup.ts, src/plugins/watchdog/index.ts, src/store/task-store.ts, src/mcp/server.ts, src/memory/index.ts, src/memory/project-memory.ts, src/metrics/exporter.ts</files>
  <action>
Migrate 10 files (~33 console.* calls, ~9 catch blocks):

**openclaw/ (3 files, 15 console.*, 6 catch blocks):**
- `openclaw-executor.ts`: 12 console.*, 4 catch blocks -> `const log = createLogger("openclaw");`
- `adapter.ts`: 2 console.*, 1 catch block -> `const log = createLogger("openclaw");`
- `matrix-notifier.ts`: 1 console.*, 1 catch block -> `const log = createLogger("matrix-notifier");`

**murmur/ (1 file, 3 console.*):**
- `cleanup.ts`: `const log = createLogger("murmur");`

**plugins/ (1 file, 5 console.*, 2 catch blocks):**
- `watchdog/index.ts`: `const log = createLogger("watchdog");`

**store/ (1 file, 2 console.*):**
- `task-store.ts`: `const log = createLogger("store");`

**mcp/ (1 file, 1 console.*, 1 catch block):**
- `server.ts`: `const log = createLogger("mcp");` -- MCP is core infrastructure per user decision

**memory/ (2 files, 6 console.*):**
- `index.ts`: `const log = createLogger("memory");` -- Treat as core (runs in daemon context)
- `project-memory.ts`: `const log = createLogger("memory");`

**metrics/ (1 file, 1 console.*):**
- `exporter.ts`: `const log = createLogger("metrics");`

Same pattern for each: import createLogger, create module-level logger, replace all console.* calls, remediate catch blocks.

**Boundary verification (LOG-06, LOG-07):**
After all migrations, verify:
1. `grep -rc "console\." src/cli/ src/commands/ --include="*.ts" | grep -v ":0" | head -5` -- still has console.* (CLI unchanged)
2. `git diff --name-only src/events/` -- shows NO changed files (EventLogger unchanged)
3. `grep "console\." src/config/registry.ts` -- still has the single console.warn (acceptable exception, runs before logger init)

**Accepted exceptions:**
- `src/config/registry.ts` line ~194: console.warn for unknown env vars (circular dep -- logger depends on config)
- `src/cli/**` and `src/commands/**`: All console.* stays (user-facing output)
- `src/events/**`: Zero changes (EventLogger is a separate audit system)
- `src/packaging/**`: console.* stays (installer/updater is CLI-facing)
- `__tests__/**`: console.* stays (developer-facing)

Add vi.mock for logging in any affected test files.
  </action>
  <verify>
    <automated>cd /Users/xavier/Projects/aof && echo "=== Core module console.* remaining ===" && grep -r "console\." src/daemon/ src/service/ src/protocol/ src/dispatch/ src/openclaw/ src/murmur/ src/plugins/ src/store/ src/mcp/ src/memory/ src/metrics/ --include="*.ts" | grep -v __tests__ | grep -v "\.d\.ts" | grep -v "config/registry" | head -20; echo "=== CLI still has console.* ===" && grep -c "console\." src/cli/index.ts 2>/dev/null; echo "=== Events unchanged ===" && git diff --name-only src/events/ 2>/dev/null; echo "=== Tests ===" && npm test 2>&1 | tail -15</automated>
  </verify>
  <done>Zero console.* in all core module source files (except config/registry.ts accepted exception). CLI and EventLogger confirmed untouched. All tests pass.</done>
</task>

</tasks>

<verification>
- `grep -r "console\." src/daemon/ src/service/ src/protocol/ src/openclaw/ src/murmur/ src/plugins/ src/store/ src/mcp/ src/memory/ src/metrics/ --include="*.ts" | grep -v __tests__ | grep -v "\.d\.ts"` returns zero results
- `grep -r "console\." src/cli/ src/commands/ --include="*.ts" | head -5` confirms CLI still uses console.*
- `git diff --name-only src/events/` shows no changes
- `npm test` passes (full suite green)
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Zero console.* calls in daemon/, service/, protocol/, openclaw/, murmur/, plugins/, store/, mcp/, memory/, metrics/ source files
- CLI (src/cli/, src/commands/) still uses console.* for user-facing output (LOG-06)
- EventLogger (src/events/) has zero changes (LOG-07)
- config/registry.ts keeps its single console.warn (documented exception)
- All existing tests pass
- Full TypeScript compilation clean
</success_criteria>

<output>
After completion, create `.planning/phases/37-structured-logging/37-03-SUMMARY.md`
</output>
