---
phase: 37-structured-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logging/index.ts
  - src/logging/__tests__/logger.test.ts
  - package.json
  - package-lock.json
autonomous: true
requirements: [LOG-01, LOG-02, LOG-03, LOG-07]

must_haves:
  truths:
    - "createLogger(component) returns a Pino child logger that writes JSON to stderr"
    - "Log level is controlled by AOF_LOG_LEVEL via getConfig().core.logLevel"
    - "Child loggers include component field in every log line"
    - "resetLogger() clears the singleton for test isolation"
    - "EventLogger in src/events/ is completely untouched"
  artifacts:
    - path: "src/logging/index.ts"
      provides: "Logger factory module"
      exports: ["createLogger", "resetLogger", "Logger"]
    - path: "src/logging/__tests__/logger.test.ts"
      provides: "Unit tests for logger factory"
      min_lines: 40
  key_links:
    - from: "src/logging/index.ts"
      to: "src/config/registry.ts"
      via: "getConfig().core.logLevel"
      pattern: "getConfig\\(\\)"
---

<objective>
Create the Pino-based structured logging module that all core modules will consume.

Purpose: Establishes the logger factory (createLogger/resetLogger) and validates it works correctly before any migration begins.
Output: src/logging/index.ts with tests, pino installed as dependency.
</objective>

<execution_context>
@/Users/xavier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-structured-logging/37-RESEARCH.md

@src/config/registry.ts
</context>

<interfaces>
<!-- Config registry interface the logger depends on -->

From src/config/registry.ts:
```typescript
export function getConfig(): Readonly<AofConfig>;
export function resetConfig(overrides?: Partial<{ [K in keyof AofConfig]: Partial<AofConfig[K]> }>): void;

// AofConfig.core.logLevel is: "debug" | "info" | "warn" | "error" | "silent"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install Pino and create logger factory module</name>
  <files>package.json, package-lock.json, src/logging/index.ts</files>
  <action>
1. Install pino v9: `npm install pino@^9.6.0`

2. Create `src/logging/index.ts` with:
   - Lazy singleton root logger (matches getConfig() pattern in config/registry.ts)
   - `getRootLogger()` internal function: reads `getConfig().core.logLevel`, creates Pino instance with:
     - `level` from config
     - `timestamp: pino.stdTimeFunctions.isoTime` (ISO 8601)
     - Destination: `pino.destination({ fd: 2, sync: false })` (stderr, async)
     - Default serializers (Pino's built-in err serializer handles stack traces)
   - `createLogger(component: string): Logger` -- returns `getRootLogger().child({ component })`
   - `resetLogger(): void` -- flushes buffered logs via `flushSync()` on the destination stream, then sets root to null. Mirrors resetConfig() pattern.
   - Re-export `type Logger from "pino"` for consumer convenience

3. The module must NOT import from dispatch/, service/, store/, protocol/, or any module above it in the dependency hierarchy. Only dependency is config/registry.ts.

Important: Do NOT install @types/pino -- Pino v9 ships built-in TypeScript types.
  </action>
  <verify>
    <automated>cd /Users/xavier/Projects/aof && npx tsc --noEmit src/logging/index.ts 2>&1 | head -20</automated>
  </verify>
  <done>src/logging/index.ts exists, compiles cleanly, pino is in package.json dependencies</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write logger unit tests</name>
  <files>src/logging/__tests__/logger.test.ts</files>
  <behavior>
    - Test 1: createLogger returns a logger with expected methods (info, warn, error, debug)
    - Test 2: Logger writes JSON to stderr containing level, time, component, and msg fields
    - Test 3: Log level filtering works -- setting level to "error" suppresses info/warn output
    - Test 4: Child logger includes component field in output
    - Test 5: resetLogger() allows re-initialization with different config
    - Test 6: Error objects logged via { err } field include stack trace in output
  </behavior>
  <action>
Create `src/logging/__tests__/logger.test.ts`:

1. Mock config registry to control logLevel:
   ```typescript
   import { resetConfig } from "../../config/registry.js";
   import { createLogger, resetLogger } from "../index.js";
   ```

2. Use Pino's ability to write to a custom writable stream for test capture. Instead of capturing stderr, create the logger module's tests by:
   - Calling `resetConfig({ core: { logLevel: "debug" } })` before each test
   - Calling `resetLogger()` in afterEach to clear singleton state
   - For output verification tests: use a PassThrough stream or capture pino.destination output

3. If capturing Pino output is complex, an alternative approach: verify the logger API shape (methods exist, child returns logger), and verify config integration (resetConfig + resetLogger cycle produces logger with correct level). The JSON format and stderr destination are Pino defaults, not custom code.

4. Ensure tests call resetLogger() in afterEach to avoid singleton leakage between tests.

5. Verify EventLogger is NOT touched: assert `src/events/` has no imports of `src/logging/`.
  </action>
  <verify>
    <automated>cd /Users/xavier/Projects/aof && npx vitest run src/logging/__tests__/logger.test.ts -x 2>&1 | tail -20</automated>
  </verify>
  <done>All logger tests pass, covering createLogger API, level filtering, component field, resetLogger isolation, and error serialization</done>
</task>

</tasks>

<verification>
- `npx vitest run src/logging/ -x` -- all logger tests pass
- `npx tsc --noEmit` -- no type errors across project
- `git diff src/events/` shows no changes (LOG-07: EventLogger unchanged)
- `grep -r "pino" package.json` shows pino in dependencies
</verification>

<success_criteria>
- createLogger("dispatch") returns a Pino child logger that writes JSON to stderr
- AOF_LOG_LEVEL controls which messages appear (debug shows all, error suppresses info/warn)
- resetLogger() enables test isolation
- EventLogger in src/events/ is completely untouched
- All existing tests still pass (npm test)
</success_criteria>

<output>
After completion, create `.planning/phases/37-structured-logging/37-01-SUMMARY.md`
</output>
