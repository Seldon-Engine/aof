---
phase: 21-tool-workflow-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/aof/SKILL.md
  - src/skills/projects/SKILL.md
  - src/mcp/tools.ts
autonomous: true
requirements: [TOOL-01, TOOL-02]

must_haves:
  truths:
    - "Tool descriptions in registerTool calls are each one sentence or less with no inline examples"
    - "Projects skill content is available in skills/aof/SKILL.md"
    - "src/skills/projects/SKILL.md no longer exists"
  artifacts:
    - path: "skills/aof/SKILL.md"
      provides: "Merged skill with projects content compressed into 2-3 bullet points for isolation rules"
      contains: "aof_project_create"
    - path: "src/mcp/tools.ts"
      provides: "Tool registrations with trimmed one-liner descriptions"
  key_links:
    - from: "skills/aof/SKILL.md"
      to: "projects skill content"
      via: "merged section"
      pattern: "aof_project_create|aof_project_list|aof_project_add_participant"
---

<objective>
Trim tool descriptions to one-liners and merge the projects skill into the main SKILL.md.

Purpose: Reduce context injection by eliminating redundant tool documentation and consolidating two separate skill files into one.
Output: Leaner tool descriptions in tools.ts, single merged SKILL.md, deleted projects skill file.
</objective>

<execution_context>
@/Users/xavier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-tool-workflow-api/21-CONTEXT.md

@skills/aof/SKILL.md
@src/skills/projects/SKILL.md
@src/mcp/tools.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim tool descriptions and merge projects skill into main SKILL.md</name>
  <files>src/mcp/tools.ts, skills/aof/SKILL.md, src/skills/projects/SKILL.md</files>
  <action>
**In src/mcp/tools.ts** -- review each `registerTool()` call's `description` field. The current descriptions are already fairly short one-liners. Ensure none exceed one sentence. Do NOT add inline examples or parameter docs to descriptions. The new workflow parameter description (added in plan 02) will also follow this pattern. Current descriptions to verify/trim:
- `aof_dispatch`: "Create a new AOF task and assign to an agent or team" -- fine as-is
- `aof_task_update`: "Update task metadata, status, or work log" -- fine as-is
- `aof_task_complete`: "Mark task as complete" -- fine as-is
- `aof_status_report`: "Report task status counts" -- fine as-is
- `aof_board`: "Get kanban board view for a team" -- fine as-is

If all are already one-liners, no changes needed in tools.ts for descriptions.

**In skills/aof/SKILL.md** -- merge content from `src/skills/projects/SKILL.md`:
1. Add a new section "## Projects" BEFORE the "## Human Operator CLI Reference" section
2. List the 3 project tools with one-liner descriptions (no parameter tables, no JSON examples):
   - `aof_project_create` -- Create an isolated project with its own task store and memory pool
   - `aof_project_list` -- List all projects on the instance
   - `aof_project_add_participant` -- Add an agent to a project's participant list
3. Add project isolation rules as 2-3 compressed bullet points:
   - Tasks with a `project` field are stored in that project's directory; without it, global store
   - Memory is scoped per-project via the `project` parameter on memory tools
   - If participants are listed, only those agents receive tasks from that project
4. Do NOT add verbose examples or parameter tables -- tool JSON schemas already provide parameter documentation

**Delete `src/skills/projects/SKILL.md`** after the merge is complete.
  </action>
  <verify>
    <automated>test ! -f src/skills/projects/SKILL.md && grep -q "aof_project_create" skills/aof/SKILL.md && grep -q "aof_project_list" skills/aof/SKILL.md && grep -q "aof_project_add_participant" skills/aof/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- src/skills/projects/SKILL.md is deleted
- skills/aof/SKILL.md contains all 3 project tools with one-liner descriptions
- skills/aof/SKILL.md contains compressed isolation rules (2-3 bullets)
- No parameter tables or verbose JSON examples added
- All registerTool descriptions in tools.ts are one sentence or less
  </done>
</task>

</tasks>

<verification>
- `grep -c "registerTool" src/mcp/tools.ts` returns the expected tool count
- `test ! -f src/skills/projects/SKILL.md` confirms deletion
- `grep -c "aof_project" skills/aof/SKILL.md` shows at least 3 matches
- All existing tests still pass: `npx vitest run --reporter=verbose 2>&1 | tail -20`
</verification>

<success_criteria>
- Tool descriptions are each one sentence with no inline examples
- Projects skill merged into main SKILL.md with compressed content
- Original projects skill file deleted
- No functionality lost
</success_criteria>

<output>
After completion, create `.planning/phases/21-tool-workflow-api/21-01-SUMMARY.md`
</output>
