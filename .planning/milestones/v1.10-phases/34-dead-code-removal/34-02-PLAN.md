---
phase: 34-dead-code-removal
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - src/mcp/tools.ts
  - src/dispatch/executor.ts
  - src/dispatch/index.ts
  - src/dispatch/event.ts
  - src/dispatch/promotion.ts
  - src/dispatch/dag-transition-handler.ts
  - src/service/aof-service.ts
autonomous: true
requirements: [DEAD-06, DEAD-07, DEAD-08, DEAD-09]

must_haves:
  truths:
    - "No unused MCP output schemas remain in mcp/tools.ts"
    - "No deprecated type aliases (DispatchResult/Executor/MockExecutor) exist in executor.ts or dispatch/index.ts"
    - "No commented-out code blocks remain in event.ts, promotion.ts, or dag-transition-handler.ts"
    - "No deprecated notifier param exists in AOFService constructor"
    - "tsc --noEmit passes with zero errors"
    - "Full vitest suite passes with no regressions"
  artifacts:
    - path: "src/mcp/tools.ts"
      provides: "MCP tool definitions without unused output schemas"
    - path: "src/dispatch/executor.ts"
      provides: "Executor types without deprecated aliases"
    - path: "src/service/aof-service.ts"
      provides: "AOFService without deprecated notifier param"
  key_links:
    - from: "src/dispatch/index.ts"
      to: "src/dispatch/executor.ts"
      via: "barrel re-exports only current types"
      pattern: "export.*from.*executor"
---

<objective>
Remove remaining dead code: 13 unused MCP output schemas, deprecated type aliases, commented-out code blocks, and the deprecated notifier parameter from AOFService.

Purpose: Complete the dead code removal by cleaning up smaller scattered deprecations that accumulated over 8 milestones.
Output: ~150-200 additional lines removed, codebase free of all identified dead code.
</objective>

<execution_context>
@/Users/xavier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-dead-code-removal/34-CONTEXT.md
@.planning/phases/34-dead-code-removal/34-RESEARCH.md
@.planning/phases/34-dead-code-removal/34-01-SUMMARY.md

@src/mcp/tools.ts
@src/dispatch/executor.ts
@src/dispatch/index.ts
@src/dispatch/event.ts
@src/dispatch/promotion.ts
@src/dispatch/dag-transition-handler.ts
@src/service/aof-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused MCP output schemas</name>
  <files>src/mcp/tools.ts</files>
  <action>
    **Commit 5: Remove 13 unused MCP output schemas from mcp/tools.ts**

    The MCP SDK does not require output schemas. These 13 output schema definitions are defined but never referenced. Remove each one:

    Per research, the unused output schemas are at approximately these lines (verify exact positions as Plan 01 did not touch this file):
    - Line ~31: output schema for a tool
    - Line ~50: output schema
    - Line ~65: output schema
    - Line ~80: output schema
    - Line ~99: output schema
    - Line ~334: output schema
    - Line ~368: output schema
    - Line ~401: output schema
    - Line ~433: output schema
    - Line ~463: output schema
    - Line ~496: output schema
    - Line ~529: output schema
    - Line ~592: output schema

    For each schema: find the `const XxxOutputSchema = z.object({...})` definition and its associated type export. Search for any usage of each schema name in the file before deleting -- if truly unused, remove the definition and type. Do NOT remove any input schemas or tool handler functions.

    Run `npx tsc --noEmit` after removal.
  </action>
  <verify>
    <automated>npx tsc --noEmit && echo "PASS: MCP output schemas removed"</automated>
  </verify>
  <done>
    - 13 unused output schema definitions removed from mcp/tools.ts (~100 lines)
    - No input schemas or tool handlers affected
    - tsc --noEmit clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove deprecated aliases, commented-out code, and notifier param</name>
  <files>
    src/dispatch/executor.ts, src/dispatch/index.ts, src/dispatch/event.ts,
    src/dispatch/promotion.ts, src/dispatch/dag-transition-handler.ts, src/service/aof-service.ts
  </files>
  <action>
    **Commit 6: Remove deprecated type aliases**
    In `src/dispatch/executor.ts`:
    - Remove deprecated alias `ExecutorResult` (line ~49-50, alias for the current type name)
    - Remove deprecated alias `DispatchExecutor` (line ~115-116, alias for the current type name)
    - Remove deprecated alias `MockExecutor` (line ~284-285, alias for the current type name)
    - Each has a `@deprecated` JSDoc -- remove the entire block (JSDoc + export).

    In `src/dispatch/index.ts`:
    - Remove re-exports of the deprecated aliases. After Plan 01, gate re-exports are already gone. Now remove the lines re-exporting `DispatchResult`, `Executor`, `MockExecutor` (or their current names `ExecutorResult`, `DispatchExecutor`, `MockExecutor`). Check lines ~3, 5-8 per research.
    - Before removing re-exports, grep the full codebase for usages of the old alias names (`ExecutorResult`, `DispatchExecutor`, `MockExecutor`) to ensure no consumers depend on them. If any consumers found, update their imports to use the current names.

    **Commit 7: Remove commented-out code and stale references + deprecated notifier**
    1. `src/dispatch/event.ts` lines ~13-14: Remove the commented-out import line.
    2. `src/dispatch/promotion.ts` lines ~72-76: Remove the commented-out "Phase 2" block.
    3. `src/dispatch/dag-transition-handler.ts` line ~5: Remove or fix the stale JSDoc reference to non-existent `gate-transition-handler.ts`.
    4. `src/service/aof-service.ts`:
       - Remove `@deprecated` notifier from `AOFServiceDependencies` interface (line ~42-43)
       - Remove the `private notifier` field (line ~64)
       - Remove the notifier assignment in constructor (line ~100)
       - Remove notifier pass-through to ProtocolRouter (line ~113) if present
       - Grep test files for `notifier` being passed to `AOFService` constructor -- if found, update test to remove it. Based on research, no callers pass notifier to AOFService directly.

    Run `npx tsc --noEmit && ./scripts/test-lock.sh run` to verify.
  </action>
  <verify>
    <automated>npx tsc --noEmit && ./scripts/test-lock.sh run && grep -c "DispatchExecutor\|ExecutorResult\|MockExecutor" src/dispatch/executor.ts | grep -q '^0$' && echo "PASS: All deprecated code removed"</automated>
  </verify>
  <done>
    - 3 deprecated type aliases removed from executor.ts
    - Deprecated alias re-exports removed from dispatch/index.ts
    - Commented-out import removed from event.ts
    - Commented-out Phase 2 block removed from promotion.ts
    - Stale gate-transition-handler reference removed from dag-transition-handler.ts
    - Deprecated notifier param removed from aof-service.ts
    - tsc --noEmit clean, vitest green
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run full phase gate:
1. `npx tsc --noEmit` -- zero errors
2. `./scripts/test-lock.sh run` -- full suite green
3. `grep -rn "DispatchExecutor\|ExecutorResult\|MockExecutor" src/dispatch/executor.ts src/dispatch/index.ts` -- zero hits
4. `grep -c "@deprecated.*notifier" src/service/aof-service.ts` -- zero hits
5. `grep -rn "gate-evaluator\|gate-conditional\|gate-context-builder" src/ --include="*.ts"` -- zero hits (final confirmation from Plan 01 + Plan 02 combined)
</verification>

<success_criteria>
- 13 unused MCP output schemas removed (~100 lines)
- 3 deprecated type aliases removed from executor.ts
- Commented-out code removed from event.ts, promotion.ts
- Stale reference fixed in dag-transition-handler.ts
- Deprecated notifier param removed from aof-service.ts
- Zero TypeScript compilation errors
- Zero test failures
</success_criteria>

<output>
After completion, create `.planning/phases/34-dead-code-removal/34-02-SUMMARY.md`
</output>
