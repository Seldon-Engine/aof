---
phase: 39-architecture-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/org-chart-config.ts
  - src/config/paths.ts
  - src/config/registry.ts
  - src/org/types.ts
  - src/org/linter.ts
  - src/org/linter-helpers.ts
  - src/store/interfaces.ts
  - src/store/task-store.ts
  - src/store/task-lifecycle.ts
  - src/projects/types.ts
  - src/projects/lint.ts
  - src/projects/lint-helpers.ts
  - src/projects/store-factory.ts
  - src/projects/manifest.ts
  - src/projects/index.ts
  - src/context/types.ts
  - src/context/assembler.ts
  - src/context/manifest.ts
  - src/memory/index.ts
  - src/memory/register.ts
  - src/mcp/shared.ts
  - src/cli/project-utils.ts
  - ARCHITECTURE.md
autonomous: true
requirements: [ARCH-03, ARCH-04, ARCH-05, ARCH-06]

must_haves:
  truths:
    - "config/ has no imports from org/ — grep returns zero hits"
    - "MCP has no imports from cli/ — grep returns zero hits"
    - "loadProjectManifest has a single implementation in projects/"
    - "memory/index.ts is a pure barrel file under 40 lines with no function definitions"
    - "The 5 remaining simple A-B cycles are broken"
    - "ARCHITECTURE.md exists with import direction rules documenting which modules must not import from which"
    - "All existing tests pass"
  artifacts:
    - path: "src/projects/store-factory.ts"
      provides: "createProjectStore moved from cli/project-utils.ts"
      exports: ["createProjectStore"]
    - path: "src/projects/manifest.ts"
      provides: "Unified loadProjectManifest implementation"
      exports: ["loadProjectManifest"]
    - path: "src/memory/register.ts"
      provides: "registerMemoryModule and all helper logic extracted from memory/index.ts"
      exports: ["registerMemoryModule"]
    - path: "src/memory/index.ts"
      provides: "Pure barrel re-exports"
    - path: "ARCHITECTURE.md"
      provides: "Import direction rules and module layering constraints"
      contains: "must not import"
  key_links:
    - from: "src/mcp/shared.ts"
      to: "src/projects/store-factory.ts"
      via: "import { createProjectStore }"
      pattern: "from.*projects/store-factory"
    - from: "src/config/org-chart-config.ts"
      to: "caller site"
      via: "validator parameter injection"
      pattern: "linter\\?.*\\(.*data\\)"
---

<objective>
Fix layering violations, relocate misplaced modules, unify duplicate implementations, split memory barrel, break the 5 remaining simple A-B circular dependency cycles, and document import direction rules.

Purpose: ARCH-03 fixes config->org upward import via dependency inversion. ARCH-04 moves createProjectStore from CLI to projects/ so MCP doesn't depend on CLI. ARCH-05 unifies duplicate loadProjectManifest implementations. ARCH-06 separates memory/index.ts registration logic from barrel exports. The 5 simple cycles (config/paths<->registry, org/linter<->helpers, store/task-store<->lifecycle, projects/lint<->helpers, context/assembler<->manifest) are broken by extracting shared types. Per user decision, import direction rules are documented in ARCHITECTURE.md.

Output: Clean module layering, single loadProjectManifest, pure memory barrel, 5 more cycles eliminated, ARCHITECTURE.md with import rules.
</objective>

<execution_context>
@/Users/xavier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-architecture-fixes/39-CONTEXT.md
@.planning/phases/39-architecture-fixes/39-RESEARCH.md

<interfaces>
<!-- Current layering violations to fix -->

From src/config/org-chart-config.ts:
```typescript
import { lintOrgChart } from "../org/linter.js";
// Used at: const lintIssues = lintOrgChart(parseResult.data);
```

From src/mcp/shared.ts:
```typescript
const { createProjectStore } = await import("../cli/project-utils.js");
// Dynamic import — hidden dependency from MCP to CLI
```

From src/store/interfaces.ts:
```typescript
export interface ITaskStore {
  // Existing methods — new save/saveToPath will be added in Plan 03
}
```

From src/store/task-store.ts:
```typescript
export interface TaskStoreHooks { ... }
// Imported by task-lifecycle.ts, causing cycle — move to interfaces.ts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Break 5 simple A-B cycles via type extraction</name>
  <files>src/config/paths.ts, src/config/registry.ts, src/org/types.ts, src/org/linter.ts, src/org/linter-helpers.ts, src/store/interfaces.ts, src/store/task-store.ts, src/store/task-lifecycle.ts, src/projects/types.ts, src/projects/lint.ts, src/projects/lint-helpers.ts, src/context/types.ts, src/context/assembler.ts, src/context/manifest.ts</files>
  <action>
Fix each of the 5 simple A-B circular dependency cycles:

1. **config/paths.ts <-> config/registry.ts:** Read both files to identify what each imports from the other. paths.ts imports getConfig from registry.ts, and registry.ts imports normalizePath from paths.ts. Fix by inlining the normalizePath logic in registry.ts (it's likely a simple path.resolve/normalize call) OR extracting the shared utility to a config/utils.ts. Choose whichever is simpler.

2. **org/linter.ts <-> org/linter-helpers.ts:** linter-helpers.ts imports a type (likely LintIssue) from linter.ts. Create src/org/types.ts with the shared type(s). Update both files to import from org/types.ts.

3. **store/task-store.ts <-> store/task-lifecycle.ts:** task-lifecycle.ts imports TaskStoreHooks type from task-store.ts. Move TaskStoreHooks interface to src/store/interfaces.ts (which already exists and has ITaskStore). Update both files. Also update store/index.ts barrel to export TaskStoreHooks from interfaces.ts.

4. **projects/lint.ts <-> projects/lint-helpers.ts:** lint-helpers.ts imports a type from lint.ts. Create src/projects/types.ts with the shared type(s). Update both files.

5. **context/assembler.ts <-> context/manifest.ts:** manifest.ts imports ContextManifest type from assembler.ts. Create src/context/types.ts with ContextManifest. Update both files.

For each cycle: identify the shared type causing the cycle, extract to a types file, update imports in both files, verify with `import type` syntax where appropriate.

Run `npx madge --circular --extensions ts src/config/ src/org/ src/store/ src/projects/ src/context/` after each fix. Run `npx vitest run` at the end.
  </action>
  <verify>
    <automated>npx madge --circular --extensions ts src/ 2>&1 | grep -c "circular" || echo "0 cycles" && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 simple A-B circular dependency cycles eliminated. Shared types extracted to dedicated type files. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix layering violations, relocate modules, split memory barrel, document import rules</name>
  <files>src/config/org-chart-config.ts, src/projects/store-factory.ts, src/projects/manifest.ts, src/projects/index.ts, src/mcp/shared.ts, src/cli/project-utils.ts, src/dispatch/assign-executor.ts, src/memory/index.ts, src/memory/register.ts, ARCHITECTURE.md</files>
  <action>
**ARCH-03: Config->org dependency inversion**

1. Read src/config/org-chart-config.ts. Find where it imports lintOrgChart from org/linter.ts.
2. Change the setConfigValue function (or whichever function calls lintOrgChart) to accept an optional `linter` parameter: `linter?: (data: unknown) => Array<{ severity: string; message: string }>`.
3. Remove the `import { lintOrgChart }` from org/linter.ts.
4. Find the caller(s) of setConfigValue — likely in CLI commands or org/ itself. Update them to pass lintOrgChart as the linter argument.
5. Verify: `grep -r "from.*org/" src/config/ --include="*.ts"` returns zero hits.

**ARCH-04: MCP->CLI dependency — move createProjectStore to projects/**

1. Read src/cli/project-utils.ts to find createProjectStore and its dependencies.
2. Create src/projects/store-factory.ts with the createProjectStore function. Ensure its imports align with the projects/ module layer (store/, schemas/ are fine; cli/ is NOT).
3. Update src/mcp/shared.ts to import from "../projects/store-factory.js" instead of "../cli/project-utils.js".
4. Update src/cli/project-utils.ts to re-export from "../projects/store-factory.js" for backward compatibility (or update CLI callers directly).
5. Add the export to src/projects/index.ts barrel if one exists.
6. Verify: `grep -r "from.*cli/" src/mcp/ --include="*.ts"` returns zero hits.

**ARCH-05: Unify duplicate loadProjectManifest**

1. Read src/dispatch/assign-executor.ts to find its loadProjectManifest implementation.
2. Read src/mcp/shared.ts to find its inline loadProjectManifest implementation.
3. Create src/projects/manifest.ts (or add to existing file in projects/) with a single unified implementation.
4. Update assign-executor.ts and mcp/shared.ts to import from projects/manifest.ts.
5. Remove the duplicate implementations. Update any re-exports (task-dispatcher.ts re-exports loadProjectManifest).
6. Verify: `grep -rn "function loadProjectManifest\|const loadProjectManifest\|async.*loadProjectManifest" src/ --include="*.ts"` shows exactly one definition.

**ARCH-06: Split memory/index.ts**

1. Read src/memory/index.ts (348 lines). Identify registerMemoryModule() function and all its helper functions/types.
2. Create src/memory/register.ts with registerMemoryModule() and all supporting functions/types (the ~270+ lines of logic).
3. Rewrite src/memory/index.ts as a pure barrel: only `export { ... } from "./register.js"` and `export { ... } from "./other-modules.js"` re-exports. Target: under 40 lines, no function definitions.
4. Ensure registerMemoryModule is re-exported from the barrel so existing callers don't need import path changes.
5. Verify: memory/index.ts has no function/const definitions, only re-exports.

**Document import direction rules (per user decision)**

1. Create ARCHITECTURE.md in the project root with a brief "Import Direction Rules" section.
2. Document the enforced layering constraints:
   - `config/` must not import from domain modules (`org/`, `dispatch/`, `projects/`, `store/`, `tools/`, `mcp/`, `cli/`, `service/`)
   - `mcp/` must not import from `cli/` — shared logic lives in `projects/` or `store/`
   - `store/` internals (serializeTask, writeFileAtomic) must not be imported outside `src/store/` — use ITaskStore methods
   - Barrel files (index.ts) must be pure re-exports — no function definitions
   - Sub-modules must not import from their own barrel (import from siblings or types file instead)
3. Keep it brief (under 50 lines). Reference the specific violations fixed in this phase as examples.

Run `npx vitest run` after all fixes.
  </action>
  <verify>
    <automated>grep -r "from.*org/" src/config/ --include="*.ts" | grep -v "test" ; grep -r "from.*cli/" src/mcp/ --include="*.ts" | grep -v "test" ; wc -l src/memory/index.ts ; test -f ARCHITECTURE.md && grep -c "must not import" ARCHITECTURE.md ; npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>config/ has zero org/ imports. MCP has zero CLI imports. loadProjectManifest exists in one place (projects/). memory/index.ts is a pure barrel under 40 lines. ARCHITECTURE.md exists with import direction rules. All tests pass.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx madge --circular --extensions ts src/` — if Plan 01 also complete, should show 0 cycles total
2. `grep -r "from.*org/" src/config/ --include="*.ts"` — zero hits
3. `grep -r "from.*cli/" src/mcp/ --include="*.ts"` — zero hits
4. `wc -l src/memory/index.ts` — under 40 lines
5. `test -f ARCHITECTURE.md` — file exists with import direction rules
6. `npx vitest run` — full suite green
</verification>

<success_criteria>
- All 5 simple A-B cycles eliminated
- config/ has no org/ imports (ARCH-03)
- MCP has no CLI imports (ARCH-04)
- Single loadProjectManifest in projects/ (ARCH-05)
- memory/index.ts is pure barrel under 40 lines (ARCH-06)
- ARCHITECTURE.md documents import direction rules
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/39-architecture-fixes/39-02-SUMMARY.md`
</output>
