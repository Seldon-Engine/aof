---
phase: 36-config-registry
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - src/config/paths.ts
  - src/projects/resolver.ts
  - src/cli/program.ts
  - src/cli/commands/memory.ts
  - src/daemon/index.ts
  - src/daemon/standalone-adapter.ts
  - src/mcp/server.ts
  - src/memory/index.ts
  - src/openclaw/openclaw-executor.ts
autonomous: true
requirements:
  - CFG-03

must_haves:
  truths:
    - "grep -r 'process.env' src/ returns zero hits outside src/config/ and the documented AOF_CALLBACK_DEPTH exception files"
    - "All modules that previously read process.env now call getConfig() for their values"
    - "Existing functionality is unchanged — all tests pass, daemon starts, CLI commands work"
  artifacts:
    - path: "src/config/paths.ts"
      provides: "resolveDataDir() sources from getConfig().core.dataDir instead of process.env"
      contains: "getConfig"
    - path: "src/projects/resolver.ts"
      provides: "AOF_ROOT replaced with getConfig().core.dataDir"
      contains: "getConfig"
    - path: "src/daemon/standalone-adapter.ts"
      provides: "OPENCLAW_* vars replaced with getConfig().openclaw"
      contains: "getConfig"
    - path: "src/openclaw/openclaw-executor.ts"
      provides: "OPENCLAW_STATE_DIR replaced with getConfig().openclaw.stateDir, CLAWDBOT_STATE_DIR removed"
      contains: "getConfig"
    - path: "src/memory/index.ts"
      provides: "OPENAI_API_KEY replaced with getConfig().integrations.openaiApiKey"
      contains: "getConfig"
  key_links:
    - from: "src/config/paths.ts"
      to: "src/config/registry.ts"
      via: "getConfig() call in resolveDataDir"
      pattern: "getConfig\\(\\)\\.core\\.dataDir"
    - from: "src/daemon/standalone-adapter.ts"
      to: "src/config/registry.ts"
      via: "getConfig() for openclaw domain"
      pattern: "getConfig\\(\\)\\.openclaw"
    - from: "src/memory/index.ts"
      to: "src/config/registry.ts"
      via: "getConfig() for integrations domain"
      pattern: "getConfig\\(\\)\\.integrations"
---

<objective>
Consolidate all scattered process.env reads across src/ to use the config registry.

Purpose: Eliminate direct process.env access outside src/config/ (CFG-03). After this plan, the registry is the single source of truth for all environment configuration. The only documented exception is AOF_CALLBACK_DEPTH in callback-delivery.ts and mcp/shared.ts (cross-process mutation).

Output: 9 files updated to import from config registry instead of reading process.env directly.
</objective>

<execution_context>
@/Users/xavier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-config-registry/36-CONTEXT.md
@.planning/phases/36-config-registry/36-RESEARCH.md
@.planning/phases/36-config-registry/36-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — the registry API that all consumers will use -->

From src/config/registry.ts (created in Plan 01):
```typescript
export type AofConfig = z.infer<typeof AofConfigSchema>;

export function getConfig(): Readonly<AofConfig>;
export function resetConfig(overrides?: DeepPartial<AofConfig>): void;
export class ConfigError extends Error {
  constructor(public readonly issues: z.ZodIssue[]);
}
```

Config shape:
```typescript
{
  core: { dataDir: string, logLevel: string, vaultRoot?: string },
  dispatch: { defaultLeaseTtlMs: number, spawnTimeoutMs: number, maxConcurrency: number, maxDispatchesPerPoll: number },
  daemon: { pollIntervalMs: number, socketPath?: string },
  openclaw: { gatewayUrl: string, gatewayToken?: string, stateDir: string, configPath?: string },
  integrations: { openaiApiKey?: string },
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate config/ and core module process.env reads to registry</name>
  <files>src/config/paths.ts, src/projects/resolver.ts, src/daemon/standalone-adapter.ts, src/openclaw/openclaw-executor.ts, src/memory/index.ts</files>
  <action>
    Migrate 5 non-CLI files from direct process.env reads to getConfig(). For each file:

    **src/config/paths.ts** (line 35):
    - `resolveDataDir()`: Replace `process.env["AOF_DATA_DIR"]` with `getConfig().core.dataDir`. Keep the `explicit` parameter override — if explicit is provided, use it; otherwise use registry value. Remove the DEFAULT_DATA_DIR fallback in the function since the registry already handles defaults.
    - Keep `DEFAULT_DATA_DIR` export (other modules may use it as a constant).
    - Import `getConfig` from `./registry.js`.
    - IMPORTANT: registry.ts already imports `normalizePath` from paths.ts. This is safe because normalizePath has no env dependency. Do NOT create a circular import — paths.ts imports from registry.ts, registry.ts imports normalizePath from paths.ts. This is a valid bidirectional import since neither triggers the other at module load time (getConfig is lazy).

    **src/projects/resolver.ts** (line 34):
    - Replace `process.env["AOF_ROOT"] || "~/.aof"` with `getConfig().core.dataDir`.
    - Import `getConfig` from `../config/registry.js`.

    **src/daemon/standalone-adapter.ts** (lines 29, 32):
    - Replace `process.env["OPENCLAW_GATEWAY_URL"] || "http://localhost:3000"` with `getConfig().openclaw.gatewayUrl`.
    - Replace `process.env["OPENCLAW_GATEWAY_TOKEN"]` with `getConfig().openclaw.gatewayToken`.
    - Import `getConfig` from `../config/registry.js`.

    **src/openclaw/openclaw-executor.ts** (lines 383-384):
    - Replace `process.env["OPENCLAW_STATE_DIR"] || process.env["CLAWDBOT_STATE_DIR"] || "~/.openclaw"` with `getConfig().openclaw.stateDir`.
    - Remove the CLAWDBOT_STATE_DIR fallback entirely (dead legacy code per CONTEXT.md).
    - Import `getConfig` from `../config/registry.js`.

    **src/memory/index.ts** (line 158):
    - Replace `process.env["OPENAI_API_KEY"]` with `getConfig().integrations.openaiApiKey`.
    - Import `getConfig` from `../config/registry.js`.

    For each file, ensure the process.env read is replaced at the USAGE SITE (inside function bodies), not at module top level. getConfig() is lazy and safe to call inside functions.
  </action>
  <verify>
    <automated>cd /Users/xavier/Projects/aof && npx tsc --noEmit && npx vitest run src/config/ src/projects/ src/daemon/ src/openclaw/ src/memory/ 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 5 files migrated from process.env to getConfig()
    - CLAWDBOT_STATE_DIR reference removed
    - No circular import issues (tsc compiles cleanly)
    - All related tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate CLI and daemon entrypoint process.env reads to registry</name>
  <files>src/cli/program.ts, src/cli/commands/memory.ts, src/daemon/index.ts, src/mcp/server.ts</files>
  <action>
    Migrate 4 CLI/entrypoint files. These are trickier because some have module-level const reads.

    **src/cli/program.ts** (line 42):
    - Replace module-level `const aofRoot = process.env["AOF_ROOT"] || ...` with a call to `getConfig().core.dataDir` inside the Commander option default callback or at the point of use. If the current pattern uses the const to set a Commander default, move the read into the default function: `.option("--root <path>", "...", () => getConfig().core.dataDir)` or similar. Check the actual usage and adjust accordingly.
    - Import `getConfig` from `../config/registry.js`.

    **src/daemon/index.ts** (lines 8, 26):
    - Line 8: Replace `process.env["AOF_ROOT"] || "~/.aof"` with `getConfig().core.dataDir`. If this is a module-level const used later, move it into the function that uses it.
    - Line 26: Replace `process.env["AOF_DAEMON_SOCKET"]` with `getConfig().daemon.socketPath`. This may be a Commander option default — handle like program.ts above.
    - Import `getConfig` from `../config/registry.js`.

    **src/mcp/server.ts** (line 7):
    - Replace `process.env["AOF_ROOT"] || "~/.aof"` with `getConfig().core.dataDir`. Move into the function body if currently at module level.
    - Import `getConfig` from `../config/registry.js`.

    **src/cli/commands/memory.ts** (lines 133, 150, 152, 300):
    - Line 133: Replace `process.env["AOF_VAULT_ROOT"] || process.env["OPENCLAW_VAULT_ROOT"]` with `getConfig().core.vaultRoot`.
    - Line 150: Same pattern — replace with `getConfig().core.vaultRoot`.
    - Line 152: Replace `process.env["OPENCLAW_CONFIG"] || "~/.openclaw/openclaw.json"` with `getConfig().openclaw.configPath`.
    - Line 300: Same vault root pattern — replace with `getConfig().core.vaultRoot`. Check if the current code has a local `root` variable fallback and preserve that logic.
    - Import `getConfig` from `../../config/registry.js`.

    IMPORTANT: Do NOT touch the AOF_CALLBACK_DEPTH reads in `src/dispatch/callback-delivery.ts` or `src/mcp/shared.ts`. These are the documented exception per CFG-03.

    After all changes, run the final verification grep to confirm zero process.env reads remain outside config/ and the exception files.
  </action>
  <verify>
    <automated>cd /Users/xavier/Projects/aof && npx tsc --noEmit && npm test 2>&1 | tail -5 && echo "--- process.env check ---" && grep -rn "process\.env" src/ --include="*.ts" | grep -v __tests__ | grep -v node_modules | grep -v "config/" | grep -v "callback-delivery" | grep -v "mcp/shared" | grep -v ".d.ts" || echo "CLEAN: no stray process.env reads"</automated>
  </verify>
  <done>
    - All 4 CLI/entrypoint files migrated from process.env to getConfig()
    - grep -r "process.env" src/ shows ONLY: src/config/registry.ts (the single reader), src/dispatch/callback-delivery.ts (exception), src/mcp/shared.ts (exception)
    - Full test suite passes (npm test)
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors
2. `npm test` — full test suite green
3. `grep -rn "process.env" src/ --include="*.ts" | grep -v __tests__ | grep -v config/ | grep -v callback-delivery | grep -v "mcp/shared"` — zero hits (CFG-03 satisfied)
4. Verify AOF_CALLBACK_DEPTH exception files are untouched
</verification>

<success_criteria>
- Zero process.env reads outside src/config/ (except documented AOF_CALLBACK_DEPTH exception)
- All 15 non-exception process.env call sites migrated to getConfig()
- CLAWDBOT_STATE_DIR dead code removed
- Full test suite passes with no regressions
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/36-config-registry/36-02-SUMMARY.md`
</output>
