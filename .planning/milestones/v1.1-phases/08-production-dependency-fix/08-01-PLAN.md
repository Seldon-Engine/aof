---
phase: 08-production-dependency-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - "MEM-06-caveat"

must_haves:
  truths:
    - "@inquirer/prompts is listed in dependencies (not devDependencies) in the root package.json"
    - "npm ci --omit=dev installs @inquirer/prompts successfully"
    - "aof memory rebuild interactive confirmation prompt resolves in a production install"
    - "package.json repository.url points to d0labs/aof"
  artifacts:
    - path: "package.json"
      provides: "Corrected dependencies and repository URL"
      contains: "@inquirer/prompts"
  key_links:
    - from: "src/cli/commands/memory.ts"
      to: "@inquirer/prompts"
      via: "dynamic import at line 512"
      pattern: "import.*@inquirer/prompts"
    - from: "src/cli/init-steps.ts"
      to: "@inquirer/prompts"
      via: "static import at line 8"
      pattern: "import.*confirm.*@inquirer/prompts"
    - from: "src/cli/init-sync.ts"
      to: "@inquirer/prompts"
      via: "static import at line 10"
      pattern: "import.*confirm.*checkbox.*@inquirer/prompts"
    - from: "src/cli/init-steps-lifecycle.ts"
      to: "@inquirer/prompts"
      via: "static import at line 6"
      pattern: "import.*confirm.*@inquirer/prompts"
---

<objective>
Fix two production-blocking issues in the root package.json: add @inquirer/prompts as a production dependency so `aof memory rebuild` works without `--yes` in production installs, and correct the repository URL from demerzel-ops/aof to d0labs/aof.

Purpose: Close the MEM-06 integration caveat — the `aof memory rebuild` interactive confirmation prompt crashes on production installs because @inquirer/prompts is not declared as a dependency and gets pruned by `npm ci --omit=dev`.

Output: Updated package.json and package-lock.json with correct dependency declaration and repository URL.
</objective>

<execution_context>
@/Users/xavier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- @inquirer/prompts is used in 5 source files via import: -->

From src/cli/commands/memory.ts (line 512, dynamic import in rebuild command):
```typescript
const { confirm } = await import("@inquirer/prompts");
```

From src/cli/init-steps.ts (line 8, static import):
```typescript
import { confirm } from "@inquirer/prompts";
```

From src/cli/init-sync.ts (line 10, static import):
```typescript
import { confirm, checkbox } from "@inquirer/prompts";
```

From src/cli/init-steps-lifecycle.ts (line 6, static import):
```typescript
import { confirm } from "@inquirer/prompts";
```

Currently installed as transitive dependency (exists in node_modules but NOT declared in package.json).
Current installed version can be found via: node -e "console.log(require('@inquirer/prompts/package.json').version)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @inquirer/prompts to dependencies and fix repository URL</name>
  <files>package.json</files>
  <action>
Edit the root package.json to make two changes:

1. **Add @inquirer/prompts to dependencies:** First check the installed version:
```bash
cd ~/Projects/AOF && node -e "console.log(require('@inquirer/prompts/package.json').version)"
```
Add `"@inquirer/prompts": "^{installed_version}"` to the `dependencies` object (alphabetically — it should be the first entry, before `@modelcontextprotocol/sdk`). Use the caret range matching the installed version.

2. **Fix repository URL:** Change:
   `"url": "https://github.com/demerzel-ops/aof.git"`
   to:
   `"url": "https://github.com/d0labs/aof.git"`

After editing package.json, run `npm install` in the project root to regenerate package-lock.json with the new dependency properly recorded.
  </action>
  <verify>
    <automated>cd ~/Projects/AOF && node -e "const p = require('./package.json'); const hasInquirer = '@inquirer/prompts' in p.dependencies; const correctUrl = p.repository.url.includes('d0labs/aof'); console.log('inquirer in deps:', hasInquirer); console.log('correct repo url:', correctUrl); if (!hasInquirer || !correctUrl) process.exit(1);"</automated>
  </verify>
  <done>@inquirer/prompts is in dependencies, repository.url points to d0labs/aof, package-lock.json updated</done>
</task>

<task type="auto">
  <name>Task 2: Verify production install resolves @inquirer/prompts</name>
  <files></files>
  <action>
Verify the fix works in a production install scenario:

1. Create a temporary directory
2. Copy the root package.json and package-lock.json into it
3. Run `npm ci --omit=dev --ignore-scripts` in the temp directory (--ignore-scripts because the prepare hook runs simple-git-hooks which is a devDependency)
4. Verify @inquirer/prompts exists in node_modules and can be required
5. Clean up the temp directory

This proves that a fresh production install will have @inquirer/prompts available, closing the MEM-06 integration caveat.
  </action>
  <verify>
    <automated>TMPDIR=$(mktemp -d) && cp ~/Projects/AOF/package.json ~/Projects/AOF/package-lock.json "$TMPDIR/" && cd "$TMPDIR" && npm ci --omit=dev --ignore-scripts --loglevel=error 2>&1 && node -e "require('@inquirer/prompts'); console.log('OK: @inquirer/prompts resolves in production install')" && rm -rf "$TMPDIR" && echo "PASS" || (rm -rf "$TMPDIR" 2>/dev/null; echo "FAIL"; exit 1)</automated>
  </verify>
  <done>npm ci --omit=dev installs @inquirer/prompts successfully — aof memory rebuild interactive confirmation will work in production installs</done>
</task>

</tasks>

<verification>
1. Root `package.json` has `@inquirer/prompts` in `dependencies` (not devDependencies)
2. Root `package.json` `repository.url` contains `d0labs/aof`
3. `npm ci --omit=dev --ignore-scripts` in a clean directory installs @inquirer/prompts
4. `npm test` still passes (no regression)
</verification>

<success_criteria>
- @inquirer/prompts declared as production dependency in root package.json
- repository.url corrected to d0labs/aof
- Production install verification passes
- package-lock.json updated
</success_criteria>

<output>
After completion, create `.planning/phases/08-production-dependency-fix/08-01-SUMMARY.md`
</output>
