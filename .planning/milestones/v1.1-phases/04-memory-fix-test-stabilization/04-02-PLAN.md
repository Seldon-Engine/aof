---
phase: 04-memory-fix-test-stabilization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/openclaw/__tests__/executor.test.ts
  - src/openclaw/__tests__/openclaw-executor-platform-limit.test.ts
  - src/openclaw/__tests__/openclaw-executor-http.test.ts
  - src/cli/__tests__/init-steps-lifecycle.test.ts
autonomous: true
requirements:
  - CI-02

must_haves:
  truths:
    - "All 11 pre-existing test failures pass green"
    - "Executor tests match fire-and-forget dispatch semantics (not blocking)"
    - "Lifecycle tests mock installService from the correct module (not deprecated daemonStart)"
    - "No test.skip added to circumvent failures — all tests fixed properly"
  artifacts:
    - path: "src/openclaw/__tests__/executor.test.ts"
      provides: "4 fixed tests matching fire-and-forget OpenClawAdapter semantics"
    - path: "src/openclaw/__tests__/openclaw-executor-platform-limit.test.ts"
      provides: "4 fixed tests matching fire-and-forget platform limit behavior"
    - path: "src/openclaw/__tests__/openclaw-executor-http.test.ts"
      provides: "1 fixed test with correct timeout minimum (300_000)"
    - path: "src/cli/__tests__/init-steps-lifecycle.test.ts"
      provides: "2 fixed tests mocking installService instead of daemonStart"
  key_links:
    - from: "src/openclaw/__tests__/executor.test.ts"
      to: "src/openclaw/openclaw-executor.ts"
      via: "tests import and mock the actual adapter"
      pattern: "OpenClawAdapter|spawnSession"
    - from: "src/cli/__tests__/init-steps-lifecycle.test.ts"
      to: "src/cli/daemon/service-file.ts"
      via: "tests mock installService from service-file module"
      pattern: "installService"
---

<objective>
Fix all 11 pre-existing test failures across 4 test files so `npm test` passes green — the prerequisite for CI going live in Phase 5.

Purpose: Tests were written against stale APIs (blocking executor, old daemonStart function) that have since been refactored. They need updating to match current fire-and-forget dispatch and installService lifecycle semantics.
Output: All 11 test failures resolved with proper fixes (not skips), test suite passes green.
</objective>

<execution_context>
@/Users/xavier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xavier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-memory-fix-test-stabilization/04-RESEARCH.md

@src/openclaw/openclaw-executor.ts
@src/openclaw/__tests__/executor.test.ts
@src/openclaw/__tests__/openclaw-executor-platform-limit.test.ts
@src/openclaw/__tests__/openclaw-executor-http.test.ts
@src/cli/__tests__/init-steps-lifecycle.test.ts
@src/cli/daemon/service-file.ts

<interfaces>
<!-- From research: current API signatures that tests must match -->

From src/openclaw/openclaw-executor.ts (OpenClawAdapter):
```typescript
// spawnSession() is FIRE-AND-FORGET:
// - Returns { success: true, sessionId: <generated-uuid> } immediately
// - Agent runs in background via void this.runAgentBackground()
// - Errors from runEmbeddedPiAgent happen in background, not in return value
// - timeout minimum: Math.max(opts.timeoutMs, 300_000) — anything below 300s is clamped
```

From src/cli/daemon/service-file.ts:
```typescript
// installService() replaced daemonStart()
// - Tests must mock "../daemon/service-file.js" with { installService }
// - Error message is "Daemon install failed" (not "Daemon start failed")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix executor, platform-limit, and HTTP test failures (9 tests)</name>
  <files>
    src/openclaw/__tests__/executor.test.ts
    src/openclaw/__tests__/openclaw-executor-platform-limit.test.ts
    src/openclaw/__tests__/openclaw-executor-http.test.ts
  </files>
  <action>
**Read the actual source first.** Before modifying any test, read `src/openclaw/openclaw-executor.ts` to understand the current fire-and-forget semantics. All fixes below are based on research analysis but must be verified against actual code.

**File: `src/openclaw/__tests__/executor.test.ts` (4 failures)**

Root cause: Tests expect blocking dispatch behavior. Actual: `spawnSession()` returns `{ success: true, sessionId }` immediately; `runEmbeddedPiAgent` runs in background.

Fix each failing test:

1. **`spawns agent session successfully`** — Change assertion from `result.sessionId === "session-12345"` (agent's mock return) to `result.success === true` and `result.sessionId` matches UUID format. The sessionId is generated by spawnSession, not returned from the agent.

2. **`handles spawn failure gracefully`** — The mock makes `runEmbeddedPiAgent` return an error in `meta.error`. But fire-and-forget means spawnSession still returns `success: true` (agent error happens in background). Fix: Either (a) test that the setup-stage doesn't throw and returns success, OR (b) if the test is testing error reporting, restructure to mock a setup-stage error (like `ensureAgentWorkspace` throwing) which WOULD cause `success: false`.

3. **`respects timeout option`** — Test passes `timeoutMs: 60000` but code clamps to `Math.max(opts.timeoutMs, 300_000)`. Fix: Either pass `timeoutMs: 600_000` (above minimum) and assert it's used, OR update the expected value to `300_000`.

4. **`handles API exceptions`** — Mocks `mockRejectedValueOnce` for the agent. Fire-and-forget catches this in background. Fix: Same approach as test 2 — either test setup-stage errors that DO surface, or verify the call was made correctly.

**File: `src/openclaw/__tests__/openclaw-executor-platform-limit.test.ts` (4 failures)**

Root cause: Same fire-and-forget issue. Tests mock `runEmbeddedPiAgent` rejection, expect `success: false` with platform limit error parsing.

Fix: Platform limit errors from `runEmbeddedPiAgent` happen in background and don't surface in `spawnSession()` return. Options:
- (a) **Preferred:** Restructure tests to mock a setup-stage function that throws with a platform limit message. Verify the error is caught and `success: false` is returned.
- (b) If platform limit parsing is in a separate utility function, test that function directly (unit test the parser, not the full dispatch).
- Read the actual implementation to determine which approach is correct.

**File: `src/openclaw/__tests__/openclaw-executor-http.test.ts` (1 failure)**

Root cause: `uses custom timeout from opts` passes `timeoutMs: 60_000`, but code applies `Math.max(opts?.timeoutMs ?? DEFAULT_TIMEOUT_MS, DEFAULT_TIMEOUT_MS)` where DEFAULT = 300_000.

Fix: Change test to either:
- Pass `timeoutMs: 600_000` and assert it's used as `600_000`
- OR update the expected value to `300_000` (showing the minimum clamp behavior, which is arguably the better test since it documents the minimum)

**General guidance:**
- Per locked decision: "Fix all ~12 pre-existing test failures properly — investigate root cause, fix the actual issue."
- Per locked decision: "If fixing a test reveals a real code bug (not just a test issue): fix the bug too."
- Do NOT use `test.skip` to work around failures.
- If a test's intended behavior no longer makes sense with fire-and-forget semantics, rewrite the test to verify the current correct behavior while preserving the test's original intent (e.g., "errors are handled" → "setup errors surface, background errors are logged").
  </action>
  <verify>
    <automated>cd /Users/xavier/Projects/aof && npx vitest run src/openclaw/__tests__/executor.test.ts src/openclaw/__tests__/openclaw-executor-platform-limit.test.ts src/openclaw/__tests__/openclaw-executor-http.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All 9 tests in the 3 executor test files pass green
    - Tests verify fire-and-forget semantics (not blocking)
    - Timeout test documents the 300_000ms minimum clamp
    - No test.skip used
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix init-steps-lifecycle test failures (2 tests)</name>
  <files>
    src/cli/__tests__/init-steps-lifecycle.test.ts
  </files>
  <action>
**Read the actual source first.** Read `src/cli/init-steps-lifecycle.ts` (or wherever `runDaemonStep` is defined) to understand the current implementation.

**File: `src/cli/__tests__/init-steps-lifecycle.test.ts` (2 failures)**

Root cause: Tests mock `daemonStart` from `"../commands/daemon.js"` but actual code dynamically imports `installService` from `"../daemon/service-file.js"`.

**Test 11: `PID file absent + user confirms → calls daemonStart, daemonRunning=true`**
Fix:
1. Change the mock target from `"../commands/daemon.js"` to `"../daemon/service-file.js"`
2. Change the mocked function from `daemonStart` to `installService`
3. Keep the test's intent: verify that when PID file is absent and user confirms, the daemon lifecycle step succeeds
4. Update assertion: verify `installService` was called (not `daemonStart`)

**Test 13: `daemonStart throws → warning added, daemonRunning=false`**
Fix:
1. Same mock target change as Test 11
2. Mock `installService` to throw
3. Update the expected warning string from `"Daemon start failed"` to `"Daemon install failed"` (matching actual code's error message)
4. Keep the test's intent: verify that when daemon installation fails, a warning is added and daemonRunning is false

**Verify the actual import path:** Before mocking, read the source to confirm the exact dynamic import path. It should be something like:
```typescript
const { installService } = await import("../daemon/service-file.js");
```
The mock must match this exact path.

**General guidance:**
- Per locked decision: "Fix all ~12 pre-existing test failures properly — investigate root cause, fix the actual issue."
- If the actual code has a bug revealed by fixing the test, fix the bug too.
  </action>
  <verify>
    <automated>cd /Users/xavier/Projects/aof && npx vitest run src/cli/__tests__/init-steps-lifecycle.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Both lifecycle tests pass green
    - Tests mock `installService` from `"../daemon/service-file.js"` (correct module)
    - Test 13 expects `"Daemon install failed"` (correct error message)
    - No test.skip used
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run src/openclaw/__tests__/executor.test.ts` — all tests pass
2. `npx vitest run src/openclaw/__tests__/openclaw-executor-platform-limit.test.ts` — all tests pass
3. `npx vitest run src/openclaw/__tests__/openclaw-executor-http.test.ts` — all tests pass
4. `npx vitest run src/cli/__tests__/init-steps-lifecycle.test.ts` — all tests pass
5. `npm test` — full suite runs with 0 failures (combined with Plan 01 tests)
</verification>

<success_criteria>
- All 11 pre-existing test failures are fixed with proper root cause fixes
- No test.skip workarounds — all tests exercise real behavior
- Executor tests match fire-and-forget semantics
- Lifecycle tests mock the correct module (installService, not daemonStart)
- Full test suite passes green: `npm test` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-memory-fix-test-stabilization/04-02-SUMMARY.md`
</output>
